- **기본 방향**
    - 현재 과제는 크게 `데이터 정리`, `환경 구현`, `제약 구현`, `휴리스틱/학습`, `실험 검증`으로 나누어 진행한다.
    - 다만 초보 학생도 실제 구현 경험을 쌓을 수 있도록, 큰 기능을 그대로 맡기지 않고 **클래스/함수 단위의 아주 작은 작업**으로 잘게 나누어 배정한다.
    - 특히 **예진, 수연, 나영**은 “보조” 역할이 아니라, 작은 함수 단위라도 직접 구현 책임을 가지는 방식으로 진행한다.
    - 현진, 영찬은 전체 구조 설계와 통합을 맡고, 초보 학생이 구현한 함수들을 연결/검토하는 역할을 수행한다.
    - 문서상으로는 현재 코드 구현 여부와 무관하게, 아래 항목들을 **앞으로 구현해야 하는 작업**처럼 나누어 관리한다.

---

## TODO list

- (A) 전체 프레임워크 및 실행 구조
    - 전체 폴더 구조 설계 및 파일 역할 정리 **`현진`**
    - 메인 실행 흐름 정의 (`show-config`, `simulate`, `trace`, `train`, `eval`) **`영찬`**
    - config, scenario, environment, trainer가 연결되는 전체 call flow 문서화 **`현진, 영찬`**
    - 결과 저장 디렉토리 및 실행 로그 규칙 정리 **`영찬`**
    - 함수 단위 작업 분배표 작성 및 일정 관리 **`현진`**

- (B) 데이터 분석 및 전처리 파트
    - 원본 데이터 컬럼 의미 정리 표 작성 **`예진`**
        - `WK_DT`, `STL_REQ_DT`, `EQP_NM`, `WK_ORD_NO` 등 컬럼별 의미 정리
        - 결측 허용 여부 표시
        - 타입(날짜/문자/실수) 정리
    - 데이터 기본 통계 함수 구현 **`예진`**
        - 행 수 세기 함수
        - 장비 종류 개수 세기 함수
        - 계열 종류 개수 세기 함수
        - 송선(stage) 빈도표 함수
    - 결측치 점검 함수 구현 **`나영`**
        - 컬럼별 결측 개수 세기 함수
        - `TACT_TIME` 결측 row 추출 함수
        - `RT_CUT_ST_DTM`, `RT_CUT_ED_DTM` 결측 row 추출 함수
    - 날짜/시간 파싱 함수 구현 **`나영`**
        - `YYYYMMDD` → 날짜 변환 함수
        - `YYYYMMDDHHMM` → datetime 변환 함수
        - 시작/종료 시각 차이(분) 계산 함수
    - 이상치 점검 함수 구현 **`수연`**
        - 음수 duration 탐지 함수
        - 과도하게 긴 duration 탐지 함수
        - `TACT_TIME == 0` row 탐지 함수
        - 상/하위 10개 이상치 출력 함수
    - 장비별 택트타임 요약 함수 구현 **`수연`**
        - 장비별 평균 택트타임 함수
        - 장비별 중앙값 함수
        - 장비별 최소/최대 함수
        - 장비별 데이터 건수 함수
    - 길이 계열 변수 요약 함수 구현 **`예진, 나영`**
        - `MARK_LTH` 분포 요약
        - `CUT_LTH` 분포 요약
        - `BVL_LTH` 분포 요약
        - `BVL_LTH == 0` 비율 계산
    - 전처리 결과를 markdown/csv 형태로 저장하는 함수 구현 **`수연`**
    - 데이터 분석 파트 통합 및 리뷰 **`현진, 영찬`**

- (C) 시나리오 입력 자료형 설계
    - Job 자료형 설계 **`현진`**
    - Machine 자료형 설계 **`영찬`**
    - DownstreamBay 자료형 설계 **`현진`**
    - AuxiliaryResource 자료형 설계 **`영찬`**
    - ScheduledOperation 자료형 설계 **`현진`**
    - 작은 단위 필드 검토 작업 **`예진, 수연, 나영`**
        - 각 자료형에 필요한 필드 목록 작성
        - 필드 타입 표 작성
        - 예시 yaml/json 샘플 작성

- (D) config / scenario loader 구현
    - 프로젝트 루트 찾기 함수 구현 **`수연`**
    - 상대경로를 절대경로로 바꾸는 함수 구현 **`수연`**
    - config yaml 로딩 함수 구현 **`나영`**
    - scenario yaml 로딩 함수 구현 **`나영`**
    - config 필수 키 검사 함수 구현 **`예진`**
    - scenario 필수 키 검사 함수 구현 **`예진`**
    - config 값 요약 출력 함수 구현 **`수연`**
    - loader 전체 연결 및 예외처리 **`영찬`**

- (E) 환경 상태(State) 구현
    - `current_time` 초기화 함수 **`나영`**
    - `unscheduled_jobs` 초기화 함수 **`예진`**
    - `machine_available_at` 초기화 함수 **`수연`**
    - `machine_slot_available_at` 초기화 함수 **`수연`**
    - `machine_loads` 초기화 함수 **`예진`**
    - `downstream_loads` 초기화 함수 **`예진`**
    - `machine_daily_loads` 초기화 함수 **`나영`**
    - `scheduled_job_count_by_day` 초기화 함수 **`나영`**
    - `resource_active_until` 초기화 함수 **`수연`**
    - `machine_last_family` 초기화 함수 **`예진`**
    - 상태 초기화 통합 함수 **`현진`**

- (F) 시간 관련 기본 유틸 함수
    - day index 계산 함수 **`예진`**
    - day key 문자열 생성 함수 **`예진`**
    - minute-of-day 계산 함수 **`수연`**
    - `HH:MM` 파싱 함수 **`수연`**
    - 시간구간 포함 여부 함수 (`12:00-13:00`, `22:00-02:00`) **`나영`**
    - 날짜 spec 매칭 함수 (`A`, `A~B`, `A..B`) **`나영`**
    - 날짜별 상태 dict 생성 함수 **`예진`**
    - 시간 유틸 통합 테스트 **`영찬`**

- (G) calendar / 운영시간 기능 구현
    - 휴무일 체크 함수 **`예진`**
    - 반일 가동 체크 함수 **`예진`**
    - 점심시간 체크 함수 **`수연`**
    - 공장 전체 shutdown 체크 함수 **`수연`**
    - 설비별 운영시간 체크 함수 **`나영`**
    - 설비별 계획정지 체크 함수 **`나영`**
    - 설비 breakdown 체크 함수 **`예진`**
    - 작업 구간 중 비가동 시간을 반영하는 finish time 보정 함수 **`영찬`**
    - calendar 기능 전체 통합 **`현진`**

- (H) 후공정 buffer / event 기능 구현
    - downstream event 자료형 정의 **`현진`**
    - 도착 이벤트 추가 함수 **`수연`**
    - 해소 이벤트 추가 함수 **`수연`**
    - 현재 시점까지 event 적용 함수 **`나영`**
    - bay별 예상 적치량 계산 함수 **`나영`**
    - bay load 음수 방지 함수 **`예진`**
    - event 정렬 함수 **`예진`**
    - 후공정 buffer 흐름 검증 trace 함수 **`영찬`**

- (I) 보조자원 / 슬롯 기능 구현
    - 자원 목록 로딩 함수 **`예진`**
    - 자원 점유 상태 초기화 함수 **`수연`**
    - 현재 시점 이전 자원 점유 제거 함수 **`수연`**
    - 작업-설비가 요구하는 자원 목록 생성 함수 **`나영`**
    - 자원 shortage 계산 함수 **`나영`**
    - 기계 슬롯 중 가장 빨리 비는 슬롯 찾기 함수 **`예진`**
    - 슬롯별 가용시각 갱신 함수 **`예진`**
    - 보조자원/슬롯 기능 최종 통합 **`현진`**

- (J) 처리시간 / setup / changeover 구현
    - job 기본 처리시간 계산 함수 **`수연`**
    - machine speed factor 반영 함수 **`수연`**
    - 직전 family와 비교하는 함수 **`예진`**
    - family changeover 시간 계산 함수 **`예진`**
    - machine type별 changeover override 반영 함수 **`나영`**
    - start_time 기준 processing/elapsed/finish 계산 함수 **`영찬`**
    - bay transfer time 반영 함수 **`나영`**
    - bay release delay 반영 함수 **`나영`**
    - candidate timing 전체 조합 함수 **`현진`**

- (K) constraint framework 구현
    - `ConstraintResult` 자료형 정의 **`현진`**
    - `ConstraintContext` 자료형 정의 **`현진`**
    - 규칙 registry 딕셔너리 정의 **`영찬`**
    - 규칙 category 표 정의 **`영찬`**
    - category on/off 확인 함수 **`수연`**
    - hard rule group 평가 함수 **`예진`**
    - soft rule group 평가 함수 **`예진`**
    - soft weight 곱하기 함수 **`나영`**
    - candidate bundle 반환 함수 **`나영`**
    - constraint framework 통합 **`현진, 영찬`**

- (L) machine constraint 함수 구현
    - machine enabled 체크 함수 **`예진`**
    - family eligibility 체크 함수 **`예진`**
    - thickness range 체크 함수 **`수연`**
    - table length limit 체크 함수 **`수연`**
    - machine slot availability 체크 함수 **`나영`**
    - daily capacity limit 체크 함수 **`나영`**
    - daily job cap limit 체크 함수 **`예진`**
    - auxiliary resource available 체크 함수 **`수연`**
    - constraint unit test 작성 **`영찬`**

- (M) downstream / priority / preference constraint 구현
    - downstream capacity 체크 함수 **`예진`**
    - downstream warning penalty 함수 **`예진`**
    - downstream priority penalty 함수 **`수연`**
    - due date urgency penalty 함수 **`수연`**
    - preferred machine type penalty 함수 **`나영`**
    - load balance preference penalty 함수 **`나영`**
    - soft penalty weight 검증 **`영찬`**

- (N) candidate 생성 및 action mapping 구현
    - unscheduled job loop 함수 **`수연`**
    - available machine loop 함수 **`수연`**
    - constraint context 생성 함수 **`예진`**
    - constraint evaluation 호출 함수 **`예진`**
    - 하드 제약 실패 candidate 제거 함수 **`나영`**
    - action_id 생성 함수 (`job@machine`) **`나영`**
    - candidate list → action map 변환 함수 **`예진`**
    - candidate 생성 전체 통합 **`현진`**

- (O) observation / feature builder 구현
    - env-level feature 계산 함수 **`예진`**
    - machine load ratio 계산 함수 **`예진`**
    - downstream load ratio 계산 함수 **`수연`**
    - due date slack 계산 함수 **`수연`**
    - action one-hot index 부여 함수 **`나영`**
    - candidate row 1개 생성 함수 **`나영`**
    - adjacency 연결 규칙 함수 **`예진`**
    - tensor observation 변환 함수 **`영찬`**
    - observation schema 검토 및 통합 **`현진`**

- (P) step / simulation core 구현
    - step 전 makespan 계산 함수 **`예진`**
    - load imbalance 계산 함수 **`예진`**
    - scheduled operation 생성 함수 **`수연`**
    - operation을 state에 반영하는 함수 **`수연`**
    - machine last family 갱신 함수 **`나영`**
    - downstream event 등록 함수 **`나영`**
    - reward 계산 함수 연결 **`영찬`**
    - terminated / truncated 판정 함수 **`예진`**
    - step 결과 observation/info 패키징 함수 **`수연`**
    - simulation core 전체 통합 **`현진`**

- (Q) 시간 전진 / decision epoch 구현
    - 다음 설비 완료시각 찾기 함수 **`예진`**
    - 다음 자원 해제시각 찾기 함수 **`예진`**
    - 다음 downstream event 시각 찾기 함수 **`수연`**
    - 임시 차단 상태 판단 함수 **`수연`**
    - 1분 전진 함수 **`나영`**
    - 이벤트 시각 점프 함수 **`나영`**
    - decision epoch 도달까지 반복 전진 함수 **`영찬`**

- (R) 휴리스틱 구현
    - SPT 정렬 key 함수 **`예진`**
    - priority 정렬 key 함수 **`수연`**
    - load_balance 정렬 key 함수 **`나영`**
    - 후보가 비었을 때 처리 함수 **`예진`**
    - 휴리스틱 이름 분기 함수 **`수연`**
    - EDD 추가 구현(옵션) **`나영`**
    - 휴리스틱 trace 비교 스크립트 **`영찬`**

- (S) 학습 알고리즘 및 네트워크
    - Transition 자료형 정의 **`현진`**
    - discounted return 계산 함수 **`예진`**
    - device 선택 함수 **`예진`**
    - observation 차원 추론 함수 **`수연`**
    - MLP builder 함수 **`수연`**
    - adjacency 기반 GNN layer 함수 **`나영`**
    - candidate encoder 구현 **`나영`**
    - env encoder 구현 **`예진`**
    - policy/value forward 연결 **`영찬`**
    - REINFORCE loss 계산 **`현진`**
    - PPO ratio/clipping 계산 **`영찬`**
    - self-labeling imitation loss 계산 **`현진`**
    - checkpoint 저장/로드 함수 **`수연`**

- (T) 실행 명령 및 평가
    - `show-config` 출력 함수 **`예진`**
    - `simulate` 출력 함수 **`수연`**
    - `trace` 출력 함수 **`나영`**
    - `analyze-tact` 데모 함수 **`예진`**
    - `generate-scenario` 실행 함수 **`수연`**
    - `train` runner 함수 **`영찬`**
    - `eval` runner 함수 **`영찬`**
    - argparse parser 정리 **`현진`**

- (U) 문서화 / 검증 / 리뷰
    - 함수별 입출력 표 작성 **`예진`**
    - config 설명 문서 정리 **`수연`**
    - 데이터 컬럼 설명 문서 정리 **`나영`**
    - 실험 체크리스트 문서화 **`예진`**
    - 초보학생 구현 함수 코드리뷰 1차 **`영찬`**
    - 전체 통합 리뷰 및 리팩토링 **`현진`**
    - 발표/회의용 역할분담 요약 슬라이드 초안 **`현진, 영찬`**

---

## 초보학생(예진, 수연, 나영) 운영 원칙

- 한 번에 큰 클래스 전체를 맡기지 않고, **짧은 함수 1~3개 단위**로 배정한다.
- 각 함수는 반드시 다음 4가지를 같이 제출한다.
    - 함수 설명 2~3줄
    - 입력 인자 예시
    - 출력 예시
    - 간단한 테스트 1개
- 초보학생 구현 함수는 바로 main branch에 합치지 않고, 현진/영찬 검토 후 통합한다.
- 구현 난이도는 아래 순서로 점진적으로 올린다.
    - 1단계: 파싱/집계/출력 함수
    - 2단계: 단일 규칙 체크 함수
    - 3단계: 상태 갱신 함수
    - 4단계: 여러 함수를 묶는 통합 함수

---

## 역할 분담 요약

- **현진**
    - 전체 구조 설계
    - 환경/시뮬레이션 통합
    - 자료형/상태 설계
    - 최종 통합 리뷰

- **영찬**
    - 실행 흐름 및 학습 러너
    - feature / network / 알고리즘 연결
    - 초보학생 구현 코드 검토
    - 실험 및 평가 체계 정리

- **예진**
    - 컬럼 정리, 통계, 파싱, 간단 constraint 함수
    - state 초기화, 작은 계산 함수, 요약 출력 함수

- **수연**
    - 길이/택트타임 요약, loader 보조 함수, machine/downstream 관련 단위 함수
    - observation row, heuristic key, 시뮬레이션 보조 함수

- **나영**
    - 날짜/시간 처리, soft constraint 단위 함수, event/resource 관련 단위 함수
    - trace 출력, action id / mapping 보조 함수
