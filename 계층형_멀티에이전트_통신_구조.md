# 계층형 멀티에이전트 통신 구조

작성일: 2026-07-06

이 문서는 현재 PMSP 절단 과제에서 Phase 1과 Phase 2를 어떻게 나누고, 두 agent가 어떤 정보만 주고받아야 하는지 정리한다. master checklist는 `현재과제_진행체크리스트.md`이며, 이 문서는 근거 논문과 구현 계약을 보조한다.

---

## 1. 현재 결론

현재 문제는 한 agent가 모든 것을 한 번에 결정하는 flat RL보다 계층형 구조가 맞다.

- Phase 1 agent: 블록을 어느 절단 Bay에 배정할지 결정한다.
- Phase 2 agent: Phase 1에서 정한 Bay 안에서 W/O batch를 만들고 설비에 배정한다.
- Phase 1과 Phase 2는 전체 상태를 전부 공유하지 않는다.
- Phase 1은 block-level 계획만 Phase 2에 전달한다.
- Phase 2는 feasibility/load/repair feedback만 Phase 1에 되돌린다.

이 구조를 쓰는 이유는 다음과 같다.

- Phase 1의 목적은 일별 Bay 부하평준화다.
- Phase 2의 목적은 Bay 내부 설비, W/O batch, 길이합 55,000, W/O 3개 제약을 만족하는 것이다.
- Phase 2가 모든 W/O/machine 상태를 Phase 1에 계속 전달하면 full connection이 되어 학습과 해석이 어려워진다.
- 현업 설명상 1차 중간발표 핵심은 Phase 1이고, Phase 2는 다음 단계 확장이다.

---

## 2. MDP 분리

### 2.1 Phase 1 MDP

목표: 일별 블록 물량을 Bay 22, 23, 24에 배정한다.

상태:

- 후보 블록 특성: 강재수량, 절단장, 베벨수량, 장척 여부, 후보 Bay one-hot.
- 환경 특성: 현재까지 Bay별 강재수량, 절단장, 베벨수량, block count, 진행률.

행동:

- `(block, bay)` pair 선택.

목적함수:

```text
score = (steel_gap, cut_gap, bevel_gap, long_cut_bay24_count)
```

낮을수록 좋고, 사전식 순서로 비교한다.

### 2.2 Phase 2 MDP

목표: Phase 1이 정한 Bay 내부에서 W/O를 batch로 묶고 설비에 배정한다.

상태:

- Phase 1에서 받은 block/Bay plan message.
- 해당 block의 W/O 후보 특성: W/O ID, 길이, 두께, 택트타임, 절단장, 베벨수량, 설비 가능 조건.
- Bay 내부 설비 상태: 설비별 현재 batch load, 후보 batch의 W/O 수, 후보 batch의 길이합.

행동:

- `open_batch:job_id@machine_id`
- `add_to_batch:batch_id:job_id`
- `close_batch:batch_id@machine_id`

hard 제약:

- 한 batch W/O 수 <= 3
- 한 batch 길이(LTH) 합 <= 55,000
- 같은 블록의 모든 W/O는 Phase 1이 정한 같은 Bay에 배정
- 광폭(BTH > 4,500)은 Bay 22, 23만 가능

---

## 3. 통신 방식

### 3.1 Phase 1 -> Phase 2

파일:

```text
phase1_to_phase2_messages.jsonl
```

메시지 단위:

```text
1 row = 1 block assignment
```

포함 정보:

- `block_set_id`
- `assigned_bay`
- `candidate_bays`
- `job_ids`
- `wo_count`
- `steel_quantity_sum`
- `cut_length_sum`
- `bevel_quantity_sum`
- `plate_length_sum`
- `plate_length_max`
- `plate_width_max`
- `wide_plate_flag`
- `long_cut_over_1000`

의도적으로 보내지 않는 정보:

- Phase 1 policy logits
- 전체 Bay search tree
- 전체 machine timeline
- Phase 2 후보 action 전체

### 3.2 Phase 2 -> Phase 1

파일:

```text
phase2_to_phase1_feedback.jsonl
phase2_feedback_summary.csv
```

메시지 단위:

```text
1 row = 1 block feedback
```

포함 정보:

- `status`: `ok`, `repair_required`, `infeasible`
- `reason_codes`
- `recommended_bays`
- `estimated_batch_count`
- `estimated_machine_count`
- `estimated_machine_load_gap`
- `hard_violation_count`
- `wide_bay_violation_count`
- `missing_machine_bay_count`
- `single_wo_length_violation_count`
- `repair_priority`

의도:

- Phase 2가 Phase 1에게 “이 Bay 배정은 내부 설비/batch 관점에서 가능한가?”만 알려준다.
- Phase 2의 raw timeline 전체를 Phase 1에 넘기지 않는다.
- 학습 단계에서는 이 feedback을 self-labeling 후보 평가 또는 repair candidate 생성에 사용할 수 있다.

---

## 4. 현재 구현

추가된 파일:

```text
Utils/phase1_phase2_communication.py
tests/test_phase1_phase2_communication.py
tests/test_constraints_future_rules.py
```

수정된 파일:

```text
main.py
Train/algorithm/phase1_pair_self_labeling.py
현재과제_진행체크리스트.md
```

CLI:

```bash
python main.py phase1-phase2-communicate --config config_np_100.yaml --phase1-plan output/phase1/phase1_block_bay_plan.json --output-dir output/phase1_phase2_communication --wide-bth-threshold 4500 --batch-max-wo-count 3 --batch-max-length-sum 55000
```

Phase 2 scenario에 message를 적용하는 CLI:

```bash
python main.py apply-phase1-messages-to-phase2 --config config_np_100.yaml --phase1-messages output/phase1_phase2_communication/phase1_to_phase2_messages.jsonl --assignment-mode allowed_bay_ids --output-scenario output/generated/phase2_from_phase1_messages.yaml
```

출력:

```text
output/phase1_phase2_communication/phase1_to_phase2_messages.jsonl
output/phase1_phase2_communication/phase2_to_phase1_feedback.jsonl
output/phase1_phase2_communication/phase2_feedback_summary.csv
output/phase1_phase2_communication/communication_manifest.json
```

현재 구현은 Phase 2의 최종 스케줄러가 아니다. 현재 단계는 두 agent가 주고받는 정보 경계를 고정하고, Phase 2가 Phase 1 계획에 대해 최소 feasibility/load feedback을 생성하는 vertical slice다.
또한 `phase1_to_phase2_messages.jsonl`을 Phase 2 scenario의 `allowed_bay_ids` 또는 `cut_bay`로 주입하는 경로까지 구현되어 있다.

Phase 1 pair self-labeling에는 feedback score hook을 추가했다.

```text
learning_score = phase2_feedback_score + (steel_gap, cut_gap, bevel_gap, long_cut_bay24_count)
```

기본 학습 명령은 `phase2_feedback_scorer=None`이므로 기존 4개 목적함수 그대로 동작한다. Phase 2 feedback scorer를 주입하면 hard violation, infeasible, repair count 같은 Phase 2 관점의 불가능성이 Phase 1 Bay 평준화보다 먼저 비교된다.

현재 registry에 확인된 미래 제약 primitive:

- `plate_width_range`: BTH/광폭 제약용.
- `allowed_bay_ids`: Phase 1이 정한 Bay를 Phase 2 후보 mask로 주입.
- `machine_priority_tier_policy`: 계열/블록별 설비 선호 tier 확장용.
- `bay_priority_tier_policy`: 계열별 Bay 선호 tier 확장용.

---

## 5. 근거 논문

| 논문 | 핵심 아이디어 | 현재 과제 반영 |
| --- | --- | --- |
| CommNet: Learning Multiagent Communication with Backpropagation, Sukhbaatar et al., 2016 | agent 간 continuous communication을 학습한다. | 모든 agent가 평균 메시지를 공유하는 baseline 관점으로만 참고. 현재 구조에는 full broadcast를 그대로 쓰지 않는다. |
| IC3Net: Learning When to Communicate at Scale, Singh et al., 2019 | communication gate로 언제 통신할지 학습한다. | Phase 2 feedback을 항상 전체 공유하지 않고 repair/violation 발생 시 중요 메시지로 보내는 방향의 근거. |
| TarMAC: Targeted Multi-Agent Communication, Das et al., 2019 | 무엇을 보낼지와 누구에게 보낼지를 target attention으로 학습한다. | Phase 1 -> Phase 2, Phase 2 -> Phase 1 directed message 구조의 근거. |
| SchedNet: Learning to Schedule Communication in MARL, Kim et al., 2019 | 제한된 bandwidth에서 어떤 agent가 메시지를 보낼지 스케줄링한다. | 모든 상태를 보내지 않고 block assignment와 feedback summary만 보내는 제한 통신 근거. |
| ATOC: Learning Attentional Communication for Multi-Agent Cooperation, Jiang and Lu, 2018 | 필요한 시점에 communication group을 만든다. | Phase 2가 특정 block repair 필요 시에만 Phase 1에 feedback을 보내는 구조의 장기 확장 근거. |
| I2C: Learning Individually Inferred Communication, Ding et al., 2020 | agent 간 communication 필요성을 inference하여 overhead를 줄인다. | Phase 2 feedback을 학습 기반으로 선택적으로 보내는 future direction. |
| MAGIC: Multi-Agent Graph-Attention Communication, Niu et al., 2021 | scheduler와 graph attention으로 언제/누구와/어떻게 통신할지 학습한다. | 장기적으로 block, Bay, machine, W/O를 graph node로 두는 구조의 근거. |
| G2ANet: Multi-Agent Game Abstraction via Graph Attention Neural Network, Liu et al., 2020 | agent interaction graph와 attention으로 중요한 관계를 선택한다. | 계열/선호/설비 제약이 늘어날 때 full connection 대신 관계 graph를 쓰는 근거. |

링크:

- CommNet: https://arxiv.org/abs/1605.07736
- IC3Net: https://arxiv.org/abs/1812.09755
- TarMAC: https://arxiv.org/abs/1810.11187
- SchedNet: https://arxiv.org/abs/1902.01554
- ATOC: https://arxiv.org/abs/1805.07733
- I2C: https://arxiv.org/abs/2006.06455
- MAGIC: https://yaruniu.com/assets/pdf/magic_mair2.pdf
- G2ANet: https://arxiv.org/abs/1911.10715

---

## 6. 다음 구현 단계

1. Phase 2 W/O batch environment를 현재 `open_batch -> add_to_batch -> close_batch` action과 완전히 연결한다.
2. 실제 BTH, 계열별 우선순위, 설비별 선호 block 데이터가 오면 scenario/context 매핑을 추가한다.
3. Phase 2 feedback scorer를 실제 Phase 2 scheduler 결과 기반으로 교체한다.
4. 실제 모든 계열 데이터 수신 후 Bay 25 포함 scenario를 별도로 만든다.
5. learned communication은 먼저 deterministic message gate로 검증한 뒤, IC3Net/TarMAC/MAGIC 계열로 확장한다.
