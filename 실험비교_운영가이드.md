# 실험 비교(validation test) 운영 가이드

여러 사람이 각자 머신에서 서로 다른 학습 설정(arm)을 돌린 뒤, 결과를 합쳐 어느 설정이 더 좋은지
판정하기 위한 절차다. 비교 항목(무엇을 arm으로 둘지)은 캠페인마다 다르며, 이 문서는 **항목과 무관한
공통 체계**만 정의한다.

---

## 1. 비교가 성립하기 위한 4조건

| 조건 | 근거 | 강제 방법 |
| --- | --- | --- |
| 동일 문제 | `validation/grid_contract.json` (`phase2_validation_grid_v1`, 문제별 jobs SHA-256) | manifest의 `validation.main.contract_sha256` 대조 |
| 동일 목적/정책 계약 | checkpoint·summary의 `run_spec` (`phase2_run_spec_v4_event_clock`) | resume/eval에서 exact match 강제 |
| 동일 평가 randomness | validation sampling seed + `--validation-temperature` | 캠페인 공통 플래그로 고정 |
| 동일 코드 | `run_manifest.json`의 `code.code_fingerprint.digest` | base commit에서 분기, dirty 금지 |

arm은 "이 4조건을 만족한 채 CLI 인자 몇 개만 다른 run들"이다. arm 변수 외의 차이는 전부 교란이다.

## 2. run_manifest.json (모든 run이 시작 시점에 남기는 증명)

schema `fab_run_manifest_v1`, 위치 `<output-dir>/run_manifest.json`. 첫 episode 이전에 기록되므로
중간에 죽은 run도 감사할 수 있다. 같은 디렉터리로 다시 실행할 때 `experiment_id` / `arm_group` /
`arm_label` / `code_fingerprint`가 다르면 fallback 없이 실패하고, 같으면 이전 manifest를
`run_manifest_history.jsonl`로 넘긴다.

| 섹션 | 내용 |
| --- | --- |
| `identity` | `experiment_id`, `arm_group`, `arm_label`, `owner`, `run_note`, `host`, `created_at_utc` |
| `invocation` | `phase`, `command`, `cli_argv`, `resolved_args`(argparse 전체), `output_dir` |
| `code` | git commit/branch/dirty/diff sha256 + `code_fingerprint`(main.py·Phase1·Phase2·Utils·Environment·Train의 모든 `.py` 해시) |
| `env` | python / platform / device / cpu_count / torch / numpy |
| `data` | `Utils/data/multi_series_generation_profile.json` sha + 실적 원천 2종 sha256 |
| `contract` | `run_spec` 전체와 `run_spec_source` |
| `validation` | main/generalization grid의 `contract_sha256`·`problem_ids_sha256`·문제 수·block size·Type 수·seed, `validation_temperature` |
| `extra` | seed, episodes, temperature 계열, lr, hidden_dim, resume 정보 등 |

`git.commit`보다 `code_fingerprint`가 중요하다. dirty tree로 돌려도 학습 코드가 바이트 동일했음을
증명할 수 있는 값은 fingerprint뿐이다.

## 3. `--validation-temperature`

Phase 2 validation/eval에서 `agent_sample` 후보를 뽑는 온도를 학습 스케줄에서 분리한다.

- 생략하면 기존 동작(그 episode의 annealed 온도)을 그대로 쓴다.
- **캠페인에서는 모든 arm이 같은 값(권장 `1.0`)을 반드시 지정한다.** 지정하지 않으면 temperature 계열
  arm은 같은 가중치라도 서로 다른 온도로 채점되어, 성능 차이인지 채점 조건 차이인지 구분할 수 없다.
- `run_spec`에는 넣지 않는다. `run_spec`에 필드를 추가하면 기존 checkpoint가 전부 거절된다.
  manifest와 eval `summary.json`에 기록하고 비교 단계에서 동일성을 검사한다.

## 4. 캠페인 절차

### 4.0 소유자(H) — 사양 동결

1. Stage 0 코드가 들어간 커밋에 태그: `git tag exp-base-<arm_group>`
2. 아래를 공표한다. 이후 arm 담당자는 이 값을 한 글자도 바꾸지 않는다.
   - `--experiment-id`, `--arm-group`
   - grid 플래그: `--main-validation-min-blocks/--main-validation-max-blocks/--main-validation-block-gap/--main-validation-episodes`,
     `--validation-min-blocks/--validation-max-blocks/--validation-block-gap/--validation-episodes`
   - `--seed`, `--validation-temperature`, `--episodes`, `--min-blocks`, `--max-blocks`,
     `--heuristic-algorithms`, `--phase2-score-mode`, `--hidden-dim`, `--rollout-samples`,
     `--rollout-samples-validation`, `--phase1-heuristic`(또는 `--phase1-checkpoint`)
   - **arm이 바꿔도 되는 플래그 화이트리스트**(캠페인마다 다름) 와 `--arm-label` 명명 규칙

### 4.1 arm 담당자 — 학습

```bash
git checkout -b exp/<arm_group>/<arm_label> exp-base-<arm_group>
git status --porcelain          # 비어 있어야 한다. dirty tree로 돌리지 않는다.
conda activate accord_env

python main.py phase2-train-batch-machine-self-labeling \
  --experiment-id <ID> --arm-group <group> --arm-label <label> --owner <name> \
  <공표된 공통 플래그 전부> \
  <이 arm만의 플래그> \
  --output-dir output/exp_<group>/<label> 2>&1 | tee output/exp_<group>/<label>.log
```

시작 직후 확인:

```bash
python -c "import json;m=json.load(open('output/exp_<group>/<label>/run_manifest.json'));\
print(m['validation']['main']['contract_sha256'], m['validation']['main']['problem_count'],\
m['code']['code_fingerprint']['digest'][:12], m['code']['git']['dirty'])"
```

`contract_sha256`가 공표값과 다르면 즉시 중단한다. 문제집합이 다르면 그 run은 비교에 쓸 수 없다.

### 4.2 arm 담당자 — 제출

`experiments/<arm_group>/<arm_label>/` 아래에 작은 산출물만 커밋한다(`.gitignore`가 `/output/`만 막으므로
`experiments/`는 추가 설정 없이 추적된다).

| 제출 | 대략 크기 |
| --- | --- |
| `run_manifest.json`, `summary.json` | 수 KB |
| `validation/grid_contract.json` | 8 KB |
| `validation/validation_parent_history.csv`, `validation_rank_history.csv` | 수십 KB |
| `validation/evaluations/ep_*/aggregate/*.csv` (3종) | ~400 KB / cycle |
| `metrics.csv` | 1–5 MB |
| `phase2_best.pt`, `checkpoints/*.pt` | **125 KB/개** — 중앙 재평가에 필요하므로 반드시 제출 |
| `.log` (tail 2000줄) | <1 MB |

제출하지 않는다: `validation/problems/**/jobs.csv`, `validation/**/bay_*/candidate_summary.csv`,
`validation/validation_candidate_latest.csv`, Phase 1의 `candidate_summary.csv`(GB급),
`validation_candidate_summary.csv`(GB급), `best_action_table.jsonl`(수백 MB).

```bash
git add experiments/<arm_group>/<arm_label>
git commit -m "exp(<arm_group>): <arm_label> 결과 제출"
git push -u origin exp/<arm_group>/<arm_label>
```

### 4.3 소유자(H) — 중앙 재평가(최종 근거)

각자 머신의 학습 중 validation은 **곡선 참고용**이다. 최종 판정은 한 머신에서 같은 grid·같은 온도로
다시 채점한 결과로 한다(머신·라이브러리 비결정성을 채점 단계에서 제거).

```bash
python main.py phase2-train-batch-machine-self-labeling \
  --eval-only --resume-checkpoint experiments/<group>/<label>/checkpoints/<ckpt>.pt \
  <공표된 공통 grid 플래그> --validation-temperature 1.0 \
  --experiment-id <ID> --arm-group <group> --arm-label <label> --owner <H> \
  --output-dir output/experiments/<group>/rescore/<label>/<ckpt>
```

`--eval-only`는 가중치를 바꾸지 않고 optimizer도 만들지 않으며 best checkpoint도 저장하지 않는다.
`validation_contract` 불일치는 허용하지만 `run_spec`은 강제하므로, 옛 checkpoint를 새 grid로 채점할 수 있다.
eval `summary.json`에는 `run_spec`, `validation_contract`, `validation_temperature`, `eval_temperature`가
함께 기록되어 그 디렉터리만으로 채점 조건을 증명한다.

### 4.4 소유자(H) — 비교

Stage 1의 `scripts/compare_experiment_runs.py`가 들어오면 그것으로, 그 전까지는 재평가 디렉터리의
`validation/evaluations/ep_*/aggregate/method_problem_scores.csv`(문제×방법 점수)와
`validation_rank_history.csv`(승/무/패·평균 rank)를 직접 대조한다. 결과는
`experiments/<group>/comparison/`에 커밋한다.

## 5. 캠페인 시작 전 통제 실험

- **NULL-ARM control**: 같은 arm·같은 seed를 다른 디렉터리에 두 번 실행 → 두 run의
  `validation/validation_parent_history.csv`가 완전히 동일해야 한다. 서로 다른 두 사람의 머신에서도 한 번
  수행해 그 차이를 **잡음 바닥**으로 기록한다. 실제 arm 효과는 이 바닥을 넘어야 의미가 있다.
- **NEGATIVE control**: `--seed`(또는 grid 플래그)만 다른 run을 만들어 `contract_sha256`가 실제로
  달라지는지 확인한다. 달라지지 않으면 게이트가 동작하지 않는 것이다.

## 6. 현재 한계

- **Phase 1은 아직 비교 대상이 아니다.** 고정 validation grid도, `run_spec`도, `--eval-only`도 없고,
  validation 후보 sampling seed에 train episode가 들어가 checkpoint마다 다른 표본이 뽑힌다. Phase 1
  manifest는 이 사실을 `run_spec_source=phase1_run_spec_absent_parity_pending`,
  `validation.main.fixed_grid=false`로 남긴다. Phase 1 arm 비교는 parity 작업 이후에 한다.
- arm당 seed 1개면 arm 효과와 seed 효과가 분리되지 않는다. 리포트에는 "방향성만"으로 적는다.
- 머신 간 torch/BLAS 비결정성으로 같은 seed라도 비트 단위 재현은 되지 않는다. 4.3의 중앙 재평가가
  채점 측 비결정성만 제거한다(학습 측은 남는다).
