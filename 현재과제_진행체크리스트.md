# 현재과제 진행 체크리스트

이 파일 하나를 현재 과제의 master checklist로 사용한다. 과거 누적 md는 근거 archive로만 본다.

## 문서 우선순위와 현재 완료 경계 (2026-07-16)

- [x] 현재 실행 코드의 state, action, feature, score, self-labeling, 합성데이터, DES, CLI 계약을 `다계열_Phase1_정책_및_합성데이터_생성_계약.md` 하나에 통합한다.
- [x] `README.md`는 빠른 실행 진입점, 이 파일은 진행 상태, 나머지 루트 md는 과거 설계 근거 archive로 구분한다.
- [x] 공개 학습 경로는 MIXED Phase 1과 merged Phase 2만 유지한다. NP-only pair-policy, imitation, 분리 Phase 2.1/2.2/3 실행 경로는 삭제한다.
- [x] 코드로 검증 가능한 MIXED 생성, Phase 1/2 smoke, full-flow, NP100 DES/replay 회귀검증을 완료했다.
- [x] 실제 다계열 설비 계약: `EQP_3`을 제외한 마스킹 ID를 PLS/PLP 15대에 매핑하고, Phase 1/2 학습 환경은 이 15대의 Bay·계열 eligibility를 공통 사용한다. `EQP_3`은 실적에서 `NC + trans`로만 관측된 별도 설비이므로 원본 identity를 보존하되 planning 후보에서는 제외한다.
- [x] 장시간 학습 실행 계약을 `다계열_Phase1_Phase2_학습_실행_가이드.md`에 고정한다. Phase 2는 Phase 1의 W/O·절단장·베벨 우선 휴리스틱을 각각 고정한 3개 실험과 frozen Phase 1 agent best-of-K 실험을 서로 다른 output에서 수행한다.
- [ ] 실제 다계열 평가 완료 조건: 신규 W/O 전체를 실제 시간축으로 replay하고, `EQP_3` 실적 전용 행과 그 외 매핑 행을 분리한 machine identity/Bay 비교를 완료한다. 코드·정적 계약 완료와 장시간 actual 평가 완료를 같은 의미로 쓰지 않는다.
- [ ] 연구 성능 완료 조건: 최종 episode 수의 장시간 Phase 1/2 학습과 holdout/full-scale 성능 검증을 수행한다. 코드 구현 완료와 장시간 학습 완료를 같은 의미로 쓰지 않는다.

## MIXED 물리 블록 공동분포 생성 및 legacy_np 제거 (2026-07-15)

### 문제 정정

- [x] 기존 MIXED 생성기는 `PROJ_NO+BLK_NO`의 계열 조합만 실적 비율로 표본화하고, 조합 안의 NP/FN/FL/NC 블록과 W/O는 계열별 독립 난수 스트림으로 생성한다는 사실을 확인했다.
- [x] 따라서 기존 계열별 W/O/블록 heatmap 8개는 각 계열의 주변분포만 검증하며, 같은 물리 블록 안의 계열별 `WO_QTY/LTH/THK/BTH/CUT_LTH/BV_QTY` 공동분포를 검증하지 못한다.
- [x] 실적 1,044개 물리 블록에서 계열 쌍별 공동 표본 수와 상관관계가 실제로 존재함을 확인했다. 예: NP-FL 335개, NP-FN 138개, NP-NC 119개, FN-FL 41개, FN-NC 40개, FL-NC 26개다.
- [x] 이전의 “MIXED 합성데이터 완료” 표시는 계열별 주변분포까지만 완료된 것으로 정정한다. MIXED 학습데이터의 공동분포 보존과 실제 EQP-PLS/PLP 매핑은 현재 모두 코드 계약으로 반영됐다.

### 구현 계약

- [x] 1. 물리 블록 단위 donor profile은 실적의 계열 조합과 계열별 `WO_QTY/LTH/THK/BTH/CUT_LTH/BV_QTY` percentile 벡터를 함께 보존한다.
- [x] 2. NP/FN/FL/NC의 개별 블록과 W/O는 기존 확정 수식으로 생성한다. 실적 블록/W/O 값을 생성값으로 복사하지 않는다.
- [x] 3. 수식으로 생성한 계열별 block 묶음은 실적 donor percentile과 최소비용 일대일 매칭하여 같은 물리 블록 identity에 결합한다. 이때 계열별 생성 주변분포와 W/O-block 집계 identity는 변경하지 않는다.
- [x] 4. 같은 seed는 동일 donor와 동일 매칭 결과를 내며, 계열 조합·물리 블록 수·W/O identity가 일치해야 한다. 매칭 불능이나 필수 특성 결측은 fallback 없이 실패시킨다.
- [x] 5. 물리 블록 기준 계열 간 공동분포 검증을 추가한다. 계열 쌍·특성 쌍별 실적/생성 Pearson 상관, 공동 표본 수, 절대오차를 CSV로 저장하고 실적/생성 비교 heatmap을 생성한다.
- [x] 6. toy 공동분포에서 독립 배정보다 percentile 일대일 매칭의 상관오차가 작아지는 회귀시험을 먼저 실패시킨 뒤 구현한다.
- [x] 7. 실제 데이터의 6개 계열 쌍과 6개 핵심 특성에 대해 공동분포 metric이 누락 없이 생성되는지 검증한다. 표본 3개 미만 상관은 숫자를 만들지 않고 `insufficient_pairs`로 명시한다.

### MIXED-only 정리 계약

- [x] 8. Phase 1 공개 pair-policy 학습은 `multi_series_260711`, `joint_five_bay_v2_mapped_eqp`, pair 16차원/env 5차원, `wo_first`만 허용한다.
- [x] 9. `legacy_np` rule profile, 10차원 pair feature schema, 구 checkpoint 계약 자동추론, NP-only pair-policy 학습 분기를 삭제한다. 구 checkpoint는 자동 변환하지 않고 schema 불일치로 명시적으로 거부한다.
- [x] 10. NP 고정 수식 생성기와 NP 100건 DES/replay 기준은 MIXED 내부 NP 생성과 공장 회귀검증에 필요하므로 유지한다. 이를 `legacy_np` 학습 경로와 혼동해 삭제하지 않는다.
- [x] 11. 삭제 후 caller/import/CLI/test를 정리하고 MIXED 공개 CLI, Phase 1/2 동일 episode identity, 전체 unit test, NP 100건 SPT/load-balance, actual replay identity, `py_compile`, `git diff --check`를 검증한다.

### 현재 상태

- 완료: seed `20260715`, 물리 블록 1,044개에서 MIXED 계열 block 1,651개/W/O 11,668개를 생성했다. 계열별 8개 heatmap과 물리 블록 공동 heatmap 1개, 계열·특성 쌍 216개 CSV를 생성했고 유효 상관 216개의 평균 절대오차는 `0.132376`이다.
- 완료: 실적에서 `STL_QTY=0`이 유효함을 반영하고, NP 공개 생성기의 W/O 행 수와 `STL_QTY`를 분리했다. 블록 `STL_QTY=sum(W/O STL_QTY)` identity를 추가했으며 기존 PDF 핵심 난수열은 변경하지 않았다.
- 완료: 공개 학습·생성·full-flow CLI는 MIXED만 허용한다. `legacy_np` Phase 1 profile, 10차원 schema, 구 checkpoint 자동추론, NP-only 학습 CLI와 관련 live module/caller를 삭제했다. 구 checkpoint 입력은 `invalid_policy_contract`로 실패한다.
- 삭제 승인: 사용자가 `legacy_np` Phase 1 학습 호환 경로의 삭제를 명시적으로 승인했다.
- 보호 범위: `변경사항/` 및 `input/` 원본, PDF, `output/`, notebook 산출물은 삭제·수정하지 않는다.

### 완료 검증

- 현재 코드로 seed `20260715`, 실적 물리 블록 1,044개를 다시 생성해 계열 block 1,651개/W/O 11,668개를 확인했다.
- 계열별 W/O/블록 heatmap 8개와 물리 블록 공동 heatmap 1개를 생성했고, 6개 계열 쌍 × 6×6 특성 쌍인 공동분포 관계 216개의 평균 상관 절대오차는 `0.132376`이다.
- MIXED 공개 CLI smoke에서 Phase 1 `multi_series_260711`/`joint_five_bay_v2_mapped_eqp`/pair 16차원/env 5차원과 Phase 2 PLS/PLP 15대 설비 계약을 확인했다. 구 `joint_five_bay_v1` checkpoint는 4/4/3 용량비를 포함할 수 있으므로 자동 재개하지 않고 계약 불일치로 거부한다.
- 전체 unit test `163개`, 관련 모듈 `py_compile`, `git diff --check`를 통과했다. NP100 SPT/load-balance는 각각 100건 전부 배정됐고 actual replay identity error는 0이다. 과거 실적 planning constraint audit 1건은 identity 오류와 분리해 유지한다.

### 현재 cleanup 경계

- `EDIT_PATHS`: `main.py`, `Phase1/`, `Phase2/`, 실제 실행 경로에서 import하는 `Utils/`, MIXED 계약 회귀시험, 불필요해진 실행 스크립트, 본 checklist로 한정한다.
- `PROTECTED_PATHS`: `input/`, `output/`, `변경사항/`, PDF, notebook, `데이터분석/`, 기존 분석·발표 산출물, 사용자가 별도 수정한 비관련 파일은 편집·삭제하지 않는다.
- 삭제 조건: 공개 CLI·live module·MIXED 회귀시험의 import 참조가 0이고, 정전 NP 수식 생성기와 NP100 DES/replay에서도 사용하지 않는 파일만 삭제한다.

## Phase 1 다계열 pair-policy 학습 연결 (2026-07-15)

### 다계열 joint episode 정정

- [x] 0-1. `MIXED` 한 episode에서 NP·NC·FN·FL W/O를 제거하지 않고 모두 유지하며, Phase 1 graph에 Bay `22/23/24/25/trans` 다섯 노드를 동시에 구성한다.
- [x] 0-2. 각 block-Bay edge는 계열 eligibility와 NP 광폭/CNT/장척 hard mask를 그대로 적용한다. 존재하지 않는 edge를 fallback으로 복원하지 않는다.
- [x] 0-3. `NP`, `NC`, `FN+FL` 평준화 계약은 유지한다. 세 그룹의 설비 수 정규화 gap을 지표별로 합산한 뒤 `W/O 수 -> CUT_LTH -> BV_QTY` 사전식 score로 joint episode 후보를 비교한다.
- [x] 0-4. rollout·휴리스틱 teacher·validation·checkpoint inference가 같은 5-Bay joint state와 score를 사용하는 회귀시험을 통과시킨다.
- [x] 0-5. 현재 유일한 MIXED profile, Phase 1/2 사전식 평가, 합성데이터 생성·집계·검증 흐름을 `다계열_Phase1_정책_및_합성데이터_생성_계약.md`에 기록한다.

- [x] 1. `Phase1PairPointerPolicy`는 `multi_series_260711=16차원 pair/5차원 env` 계약만 허용한다. 과거 10차원 NP-only 계약은 공개 실행 경로에서 제거한다.
- [x] 2. 다계열 학습의 모든 rollout·휴리스틱 teacher·validation 후보가 `PROJ_NO+GYEL+BLK_NO`, 계열별 Bay eligibility, NP 광폭/CNT/장척 hard mask와 `W/O 수 -> CUT_LTH -> BV_QTY` score를 동일하게 사용하게 한다.
- [x] 3. 다계열 env state는 진행률·잔여율과 설비 수 정규화 `W/O/CUT/BV` 현재 gap을 사용하고, 기존 NP env의 강재수량 gap과 혼용하지 않는다.
- [x] 4. checkpoint에 `rule_profile`, `score_mode`, pair/env feature 이름을 저장하고, profile/schema가 다른 checkpoint resume·inference는 fallback 없이 실패시킨다. 기존 NP-only checkpoint는 자동 변환하거나 읽지 않는다.
- [x] 5. 실제 학습 호출 경로에서 다계열 16차원 후보, hard mask, W/O-first score가 동일하게 사용되는 회귀시험을 통과시킨다. NP100은 pair-policy가 아니라 DES/replay 회귀 기준으로만 유지한다.
- [x] 6. `TACT_TIME` 생성식은 이 작업과 독립이므로 변경하지 않고 전 계열 공통 NP Case 6 계약을 유지한다.

- 검증: 다계열 공개 CLI smoke에서 `multi_series_260711`, `wo_first`, pair 16차원/env 5차원, `joint_five_bay_v2_mapped_eqp` 계약을 checkpoint에서 재확인했다. 과거의 단일-Bay-scope 및 `joint_five_bay_v1` checkpoint는 명시적으로 거부된다.
- 검증: 전체 unit test `263개`, 관련 모듈 `py_compile`, `git diff --check`를 통과했다. 기존 NP 100건 SPT/load-balance는 각각 100건 전부 배정됐고 actual replay identity error는 0이다.
- 경계: EQP-PLS/PLP 정적 매핑과 planning 환경 검증은 완료했다. 남은 항목은 신규 W/O 전체 actual time-axis replay와 장시간 학습/holdout 성능 검증이다.

## BTH 수식·다계열 조합 합성 전환 (2026-07-14)

- [x] 1. 신규 실적의 `PROJ_NO+BLK_NO`별 계열 집합 분포를 authoritative population으로 고정하고, 물리 블록마다 `NP/FN/FL/NC` 일부가 존재하거나 빠질 수 있는 조합을 같은 비율로 표본화한다.
- [x] 2. W/O `BTH`를 donor 복사가 아닌 공통 로그선형식 `ln(BTH)=b0+sum(bk*ln(1+xk))+epsilon`으로 생성한다. 입력 특성 집합과 식 형태는 전 계열이 같고, 계수·잔차·지원 규격만 NP/FN/FL/NC 실적으로 각각 적합한다.
- [x] 3. 블록 `BTH=max(W/O BTH)` identity를 유지하고, 생성값은 해당 계열에서 관측된 폭 규격으로 반올림한다. 누락·비양수 입력이나 적합 불능은 대체하지 않고 실패시킨다.
- [x] 4. NP의 발표자료 고정 생성 흐름과 기존 핵심 수식은 유지한다. PDF에 BTH 식이 없으므로 BTH만 신규 실적 적합식을 추가하고, BTH 난수 stream을 분리해 같은 seed의 기존 NP 핵심 특성을 바꾸지 않는다.
- [x] 5. FN/FL/NC는 NP와 동일한 `block -> W/O -> block 집계` 흐름과 동일한 식 종류를 사용하되 계열별 계수·잔차분포를 사용한다.
- [x] 6. `TACT_TIME`은 신규 FN/FL/NC 정답 열이 없으므로 임의 계열 계수를 만들지 않는다. 검증된 Case 6 공통식 `0.3037*CUT_LTH + 0.1325*MARK_LTH + 0.4790*THK + 0.3840*PTLST_QTY`을 전 계열에 적용하고 두께 `THK` 항을 회귀시험으로 고정한다.
- [x] 7. Phase 1과 merged Phase 2가 같은 seed에서 동일한 다계열 block/W/O DataFrame과 job identity를 받도록 공용 episode generator 하나로 연결한다.
- [x] 8. 검증은 계열 조합 허용집합·비율, 계열별 BTH 식/지원범위, W/O-block 집계 identity, TACT 식, Phase 1/2 exact identity, 관련 unit test와 100건 회귀 기준으로 수행한다.

- 실적 물리 블록 1,044개에서 13개 계열 조합을 확정했다. 1계열 546개, 2계열 399개, 3계열 98개, 4계열 1개이며 단일계열·다계열·특정 계열 부재를 모두 생성할 수 있다.
- BTH 식 설명력은 NP `R²=0.665831`, FN `0.617106`, FL `0.252072`, NC `0.356277`이다. 낮은 설명력을 숨기기 위해 값을 조작하지 않고 계열별 잔차와 관측 규격을 그대로 생성 계약에 포함한다.
- seed 20260714로 물리 블록 1,044개를 생성한 결과 실적 대비 최대 계열 비율 오차는 series-block `1.1684%p`, W/O `0.8648%p`였다.
- 비NP 길이 병합확률의 적합/생성 위치를 동일한 zero-based bin으로 통일했다. 접근 가능한 미관측 bin과 허용 두께 규격 부재는 임의값으로 대체하지 않고 오류를 출력한 뒤 실패시킨다.

> 기존 10-6의 `BTH donor` 방식은 이번 작업에서 폐기한다. `STL_QTY` 조건부 생성은 별도 계약이며 BTH 수식 전환과 혼합하지 않는다.

## NP PPT 고정 산식 단일화 (2026-07-14)

- [x] `[제출본]가공공장_중간보고.pdf` 10·20·22~27쪽의 NP 고정 산식과 집계 규칙을 상수·오차분포·집계 identity 회귀시험으로 고정했다.
- [x] PDF에 없는 `0.7 * LTH`, 계열별 재적합 계수, 잔차 공분산, `BV_QTY` 추가 TACT 식을 NP 학습 생성 경로에서 제거했다. TACT는 Case 6의 `0.3037*CUT_LTH + 0.1325*MARK_LTH + 0.4790*THK + 0.3840*PTLST_QTY`만 사용한다.
- [x] NP canonical 생성기는 `Utils/data/report_formula_data_generator.py` 하나만 사용한다. `데이터분석/shipyard_data_generator.py --series NP`도 이 생성기로 위임하며 empirical `ShipyardGenerator`의 NP 사용은 예외로 차단한다.
- [x] empirical 생성기는 입력 Excel을 읽기 전에 계열을 검사한다. `NP` 또는 계열 미지정은 즉시 실패시키고, 명시한 `FN/FL/NC`만 empirical 적합으로 진입하므로 NP가 실적 재적합 경로로 우회할 수 없다.
- [x] Phase 1 synthetic 학습·synthetic validation이 block bootstrap 대신 canonical NP 생성기의 block/W/O를 episode마다 생성한다. 과거 bootstrap 모듈과 테스트를 삭제했다.
- [x] Phase 2 synthetic 학습·synthetic validation이 같은 canonical NP 생성기를 사용한다.
- [x] PDF 집계 규칙을 적용한다: `LTH/THK/TACT_TIME=max`, `CUT_LTH/MARK_LTH/BVL_LTH/BV_QTY/PTLST_QTY=sum`, `STL_QTY=W/O 수`.
- [x] 같은 seed와 block 수에서 Phase 1과 Phase 2의 W/O·block DataFrame과 job identity가 정확히 일치하는 회귀시험을 추가했다. seed `181836732` smoke에서 양쪽 모두 block 3개/W/O 20개로 일치했다.
- [x] 전체 unit test 253개, 40 seed·4,000 block sweep, Phase 1/2 10-seed exact identity sweep, 공개 NP/MIXED 생성 CLI readback, NP 100건 SPT/load-balance, actual replay identity error 0, 전체 `py_compile`, `git diff --check`를 통과했다. 과거 실적 planning constraint audit 1건은 identity 오류와 분리해 유지한다.

## 신규 변경사항 검토 (2026-07-13)

- [x] `변경사항/제약,목적함수_정리_260711.pdf`의 계열별 Bay/설비 우선순위와 제약을 원문 기준으로 추출한다.
- [x] `변경사항/절단블록_데이터.xlsx`, `변경사항/절단WO_데이터.xlsx`의 컬럼, 계열 분포, 결측, 참조 무결성을 검증한다.
- [x] 광폭 제약은 Phase 1 action mask에서 `NP && BTH > 4500 -> Bay 22/23`으로 적용한다. 신규 NP 854블록의 BTH 최댓값은 4395라 실제 표본 검증은 불가능하므로 경계/초과 synthetic test가 필요하다.
- [x] 계열별 Bay eligibility와 NP 광폭/CNT는 Phase 1 action mask, 계열별 설비 eligibility는 Phase 2 action mask에 적용한다.
- [x] 기존 config/constraint/state/action과 실제 EQP-PLS/PLP 매핑을 반영했다. `EQP_3`은 actual-only NC/trans 예외로 분리한다.

### 2026-07-13 추가 Q&A 확정사항

- [x] 다계열 블록 동일 Bay 단위는 `PROJ_NO+GYEL+BLK_NO`다. 같은 프로젝트/블록이라도 계열이 다르면 서로 다른 Bay로 갈 수 있다.
- [x] 한 batch 안에 서로 다른 계열의 W/O를 섞을 수 있다. 단 선택한 설비의 계열 eligibility는 모두 만족해야 한다.
- [x] 계열별 평준화 그룹은 `NP`, `FN+FL`, `NC` 세 그룹이다. 서로 다른 그룹의 Bay 총합 평준화는 목적이 아니다.
- [x] 평준화는 Bay의 설비 수를 분모로 반영한다. raw Bay 총 W/O/블록 수를 동일하게 맞추지 않는다.
- [x] PDF의 목적함수 `블록 수` 표기는 `W/O 수` 오타다.
- [x] PDF의 `BVL_QTY` 표기는 데이터의 `BV_QTY`(베벨수량) 오타다.
- [x] Phase 2 목적함수 우선순위는 `makespan -> CUT_LTH 평준화 -> W/O 수 평준화 -> BV_QTY 평준화`다.
- [x] 평준화 기준은 일별이며 주간 합계 목적은 사용하지 않는다.
- [x] 신규 `RET_QTY`는 사용하지 않는 입력 컬럼이다. 모델 입력/목적/제약에 넣지 않는다.
- [x] 신규 W/O 처리시간은 전 계열에 동일한 공통 산식을 사용한다. 공통식은 기존 NP 정답으로 적합한 식이며 actual elapsed time으로 대체하지 않는다.
- [x] 확정 PLS/PLP identity 기준 설비 수는 Bay22=4, Bay23=3, Bay24=4, Bay25=2, trans=2다. Phase 1/2 용량비는 코드에 중복 기입하지 않고 이 identity 집합에서 계산한다.
- [x] NP `CUT_LTH >= 1000 -> Bay22/23`은 preference objective가 아니라 Phase 1 Bay hard constraint로 적용한다.
- [x] Phase 1 공통 목적함수의 사전식 순서는 `W/O 수 -> CUT_LTH -> BV_QTY`다.
- [x] FL 조건1은 `PTLST_QTY >= 13 또는 CUT_LTH >= 450` 중 하나라도 참이면 절단 1일을 추가한다. 둘 다 기준 미만일 때만 절단 추가일이 없다.
- [x] FL 조건2는 조건1과 독립적으로 `BVL_LTH >= 100m`이면 베벨 1일을 추가한다. 조건1/조건2 조합에 따라 `PAN_ST_DT-3/-4/-4/-5일`이 된다.
- [x] FL 조건1의 CUT_LTH 경계는 `>= 450`으로 확정한다.
- [x] EQP 마스킹 ID를 PLS/PLP 15대에 매핑하고 신규 전체 데이터에 적용했다. `EQP_3` 403건은 모두 NC/trans이며 planning 후보에서 제외된다.

### 기존 절단협의자료와 착수일 규칙 비교

- `ACT_ST_DT`는 차감 기준일이 아니라 산출 결과다. NP 일반 블록은 `SASS_ACT_STDT`, NP 직송/CNT는 `ASS_ACT_STDT`, FN/NC는 `ASS_ACT_STDT`, FL은 `PAN_ST_DT`에서 소요일을 뺀다.
- 신규 PDF의 FL 조건1은 최신 Q&A 경계 해석을 반영해 `PTLST_QTY >= 13 또는 CUT_LTH >= 450`이면 절단 1일 추가다. 따라서 추가 없음은 그 여집합인 `PTLST_QTY < 13 그리고 CUT_LTH < 450`이다.
- PDF의 "추가일 없음" 행에 적힌 `또는`은 위 고부하 OR 규칙의 여집합과 모순되는 표기다. 실행 규칙은 사용자가 확인한 고부하 OR 조건을 기준으로 한다.
- 신규 FL 470행 중 혼합 사례는 `PTLST_QTY>=13 && CUT_LTH<450` 25행, `PTLST_QTY<13 && CUT_LTH>450` 56행이며, 두 유형 모두 조건1에 따라 절단 1일을 추가한다. `CUT_LTH=450` 행은 0건이다.

### 확인된 신규 데이터 계약

- 블록 데이터는 1,642행, W/O 데이터는 11,646행이며 계열은 NP/FN/FL/NC다.
- 다계열 데이터의 블록 식별자는 `PROJ_NO+GYEL+BLK_NO`다. 기존 `PROJ_NO+BLK_NO`는 598행에서 충돌한다.
- 위 식별자로 블록 1,642개와 W/O 소속 블록 1,642개가 정확히 일치하고, `WK_ORD_NO` 중복은 0건이다.
- 신규 다계열 블록은 W/O 기준 `LTH/BTH/THK/WGT/MARK_LTH` 최댓값과 `CUT_LTH/BVL_LTH/PTLST_QTY/STL_QTY/BV_QTY/CURVE_QTY` 합계가 1,642개 전부 정확히 일치한다.
- `MARK_LTH` 집계 정의는 데이터 버전에 따라 다르다. 신규 `변경사항/절단*_데이터.xlsx`는 W/O 최댓값(1,642/1,642), 과거 260618 NP 수정 데이터는 W/O 합계(787/787)다. 신규 다계열 생성·검증에는 신규 최댓값 계약을 사용하고 과거 NP 재현에만 과거 합계 계약을 사용한다.
- W/O 수 11,646과 강재수량 합 11,084는 같지 않다. W/O 수와 `STL_QTY`를 같은 지표로 취급하지 않는다.
- 신규 W/O 파일에는 `TACT_TIME`이 없다. actual elapsed time으로 대체하지 않고, 사용자 확정에 따라 NP 정답으로 적합한 공통식을 전 계열에 사용한다.
- PDF의 `BVL_QTY` 표기는 데이터 컬럼 `BV_QTY`의 오타로 확인됐다.
- W/O의 `RET_QTY`는 데이터 사전에 없고 134행에서 1이지만, 현업 답변에 따라 모델 입력/목적/제약에서 제외한다.
- FL 조건1은 `PTLST_QTY >= 13 또는 CUT_LTH >= 450`이면 절단 1일 추가로 확정했다. PDF 저부하 행의 `또는`은 논리적 여집합과 맞지 않는 문구 오류로 기록한다.
- 신규 설비표는 Bay22 PLS21~24, Bay23 PLS31~33, Bay24 PLS41~44, Bay25 PLS51~52, trans PLP01~02 총 15대다. MIXED planning capacity는 이 identity 집합에서 직접 계산한 `4/3/4/2/2`를 사용한다. `config_np_100.yaml`은 과거 NP100 DES/replay 회귀 기준이므로 MIXED 설비 계약으로 덮어쓰지 않는다.
- 신규 데이터의 마스킹 ID 매핑은 `EQP_1->PLS51`, `EQP_2->PLS52`, `EQP_4~7->PLS21~24`, `EQP_8~10->PLS31~33`, `EQP_11~14->PLS41~44`, `EQP_15~16->PLP01~02`로 확정했다.
- `EQP_3`은 403건 전부 `GYEL=NC`, `CUT_BAY=trans`인 실적 전용 별도 설비다. 실제 행에서는 `EQP_3` identity와 `trans`를 보존하지만, 현재 planning에서는 NC를 Bay 22/23/24로 재배정하므로 Phase 1/2 후보 설비에 `EQP_3`을 포함하지 않는다.

### 신규 다계열 전환 구현 순서

> 2026-07-13 구현 착수: `feature/multi-series-constraints-260713` 브랜치의 원격 스냅샷
> `76a60d1` 이후 확정 규칙만 구현한다. 2026-07-16에 EQP-PLS/PLP 매핑과
> `EQP_3` actual-only 예외를 확정했다. `TACT_TIME`은 전 계열 공통식을 사용한다.

- [x] 1. 신규 schema를 strict loader에 추가한다: `ACT_ST_DT/ACT_ED_DT`, `EQP_NM`, `BTH`, `ASS_YN`, `SASS_ACT_STDT`, `ASS_ACT_STDT`, `PAN_ST_DT`, `CURVE_QTY`; `RET_QTY`는 읽더라도 모델 필드로 만들지 않는다. 신규 전체 데이터에서 block 1,642건/W/O 11,646건, FK mismatch 0건을 검증했다. 신규 `MARK_LTH`는 W/O 최댓값과 1,642/1,642건 일치하며, 합계와 일치한 194건은 대부분 W/O 1건 또는 나머지 값이 0인 우연한 일치다.
- [x] 2. 모든 block identity 생성/집계/join을 `PROJ_NO+GYEL+BLK_NO`로 통일했다. loader, Phase 1 episode, Phase 2 candidate workbook, report-formula generator의 생성 경로와 관련 47개 테스트를 통과했다.
- [x] 3. working-day 기반 착수일 계산기를 계열별 규칙으로 구현했다. NP/FN/FL/NC의 조건1·조건2를 독립 적용하고 원본 `ACT_ST_DT`를 덮어쓰지 않는 audit를 `output/generated/multi_series_260711/` 아래에 생성했다. 한국 법정·대체 공휴일 반영 후 전체 1,642건 중 수식 일치는 236건(14.37%)이므로 원본 일자와 산출 일자를 별도 필드로 유지한다.
- [x] 4. Phase 1 후보는 산출된 일자별로 하나의 `MIXED` joint 문제를 구성한다. 한 문제에 NP·NC·FN·FL과 Bay `22/23/24/25/trans`를 모두 유지하되, 평준화 gap은 `NP`, `FN+FL`, `NC` 그룹별 유효 Bay에서 설비 수를 분모로 계산한다.
- [x] 5. Phase 1 hard mask를 구현했다: 계열별 Bay eligibility, NP `BTH>4500`, NP `CNT_BLK`, NP `CUT_LTH>=1000 -> Bay22/23`. 전체 1,642 block 배정에서 위반 0건이며, 실제 NP 광폭 표본 0건은 synthetic 경계 test로 검증했다. 실제 NP CNT 3건과 장척 154건의 Bay24 배정도 0건이다.
- [x] 6. Phase 1 공통 score를 평준화 그룹별·설비 수 정규화 기준 `W/O 수 gap -> CUT_LTH gap -> BV_QTY gap`으로 변경했다.
- [x] 7. Phase 1 state/edge feature에 계열 그룹, 광폭 여부, CNT 여부, 후보 Bay eligibility, 설비 수 정규화 누적 부하와 투입 후 gap을 반영했다.
- [x] 8. Phase 2 family eligibility를 hard mask로 활성화했다. disabled/계열 비호환 edge를 graph와 policy state에서 제외하고, mixed-family batch의 모든 W/O를 공통 제약 엔진으로 재검증한다. planning factory는 확정 PLS/PLP 15대만 사용하고 `EQP_3`은 포함하지 않는다.
- [x] 9. Phase 2 teacher score를 확정 순서 `hard violation -> makespan -> CUT_LTH gap -> W/O count gap -> BV_QTY gap`으로 통일했다. occupancy gap은 score 마지막 진단값으로만 남겼다.
- [x] 9-1. 신규 block/W/O Excel을 strict loader로 읽어 날짜 audit과 일별·평준화 그룹별 Phase 1 계획을 한 번에 저장하는 `phase1-plan-multi-series` CLI를 제공했다. 한국 법정·대체 공휴일은 `holidays` 패키지로 읽고, 패키지가 없으면 원인을 출력하고 실패시킨다.
- [x] 10. FN/FL/NC empirical synthetic generator는 계열별 실제 분포를 사용하고, `TACT_TIME`은 발표자료 NP Case 6 고정식을 전 계열에 동일하게 적용한다. NP 생성은 empirical 적합에서 제외하고 canonical PDF 생성기로 단일화했다. 신규 데이터는 `MARK_LTH=max(W/O MARK_LTH)`, 과거 260618 NP는 `MARK_LTH=sum(W/O MARK_LTH)`으로 입력 전체를 검사해 계약을 확정하며, 판별 불가·양쪽 모두 일치하는 모호한 입력은 실패시킨다.
  - [x] 10-1. 과거에는 발표자료의 생성 흐름을 NP/FN/FL/NC에 계열별로 독립 적합해 비교했지만, 이 결과는 분석 archive로만 유지한다. 현재 NP 학습·검증·공개 생성은 재적합 결과를 사용하지 않고 고정 PDF 생성기만 사용한다. FN/FL/NC empirical 분석은 계열별 적합을 유지한다.
  - [x] 10-2. 신규 데이터의 `STL_QTY`와 `WO_QTY`를 분리해 검증했다. 동일 비율은 NP 49.18%, FN 97.58%, FL 98.72%, NC 60.78%이므로 상관이 높아도 동일 변수로 사용하지 않는다. 생성기 상용화 시 두 분포를 별도로 생성해야 한다.
  - [x] 10-3. NC는 최초 두 개 길이비 구간에 관측 W/O가 없어 기존 적합이 `NaN`으로 실패했다. 관측된 8개 구간만 곡선 적합에 사용하고, 3개 미만이면 예외를 발생시키는 규칙과 회귀시험을 추가했다. 빈 구간의 값은 대체하지 않았다.
  - [x] 10-4. 신규 W/O에 정답 `TACT_TIME`이 없어 상관구조 채택 판정에서는 처리시간을 제외했다. `BTH`와 `STL_QTY`도 기존 생성 흐름에 없었으므로 10-6 검증 전까지 미확정으로 유지했다.
  - [x] 10-5. 검증 가능한 물리 특성 생성기는 동일한 `block -> W/O -> block 집계` 계층 흐름만 공유하고, 계열별 block 사슬 계수·잔차분포와 W/O 수 분포를 독립 적합한다. 다계열 입력은 `GYEL` 지정 없이 혼합 적합하지 않으며 원인을 출력하고 실패시킨다.
    - [x] 10-5-1. `블록 LTH 분포 -> MARK_LTH -> CUT_LTH -> WO_QTY` 식의 형태는 공유하고 평균·계수·절편·잔차 표준편차·지원 범위를 NP/FN/FL/NC별로 적합한다.
    - [x] 10-5-2. W/O 수 1개인 실제 블록도 생성 가능하게 하고, 생성 block의 `WO_QTY`와 생성 W/O 행 수가 NP/FN/FL/NC smoke와 공개 CLI에서 모두 일치함을 확인했다.
    - [x] 10-5-3. 20개 seed 재검증 결과 W/O 수 편향은 FN `+214.04% -> +8.56%`, FL `+22.20% -> +1.23%`, NC `+182.92% -> +3.58%`로 감소했다. W/O/block 상관 MAE는 FN `0.250/0.159 -> 0.180/0.127`, FL `0.174/0.242 -> 0.147/0.099`, NC `0.212/0.204 -> 0.176/0.145`로 개선됐다. 신규 NP는 `0.063/0.063`, W/O 수 편향 `+10.22%`이며 결과는 `output/analysis/multi_series_shipyard_generator_series_fitted/`에 저장했다.
    - [x] 10-5-4. `STL_QTY`를 `WO_QTY`로 대체하지 않는 계약을 확정했다. `BTH`, `STL_QTY`, `TACT_TIME`은 10-6/10-7의 별도 검증을 통과한 뒤 W/O·블록 출력 schema에 추가했다.
  - [x] 10-6. `BTH`와 `STL_QTY`를 별도 생성한다. `BTH`는 전 계열 공통 로그선형식에 계열별 계수·잔차·관측 규격을 적용하고, `STL_QTY`는 계열별 조건부 범주확률과 주변분포를 결합한다.
    - [x] 10-6-1. 블록 `WO_QTY`는 W/O 행 수, 블록 `STL_QTY`는 W/O `STL_QTY` 합, 블록 `BTH`는 W/O `BTH` 최댓값으로 집계한다. 두 수량은 서로 대체하지 않는다.
    - [x] 10-6-2. NP/FN/FL/NC 실제 block 수 규모 공개 CLI와 20개 seed에서 생성 `BTH`, `STL_QTY`가 계열별 실적 지원 집합 안에 있고 모든 블록 집계 identity가 100% 일치했다. 20-seed 평균 STL 주변분포 TV 거리는 NP `0.000031`, FN `0.000868`, FL `0.000043`, NC `0.000657`이다.
  - [x] 10-7. W/O `TACT_TIME`은 발표자료 27쪽 최종 Case 6 고정식 `0.3037*CUT_LTH + 0.1325*MARK_LTH + 0.4790*THK + 0.3840*PTLST_QTY`을 사용한다. PDF에 없는 `BV_QTY` 항과 과거 재적합 계수는 실행 경로에서 제거했다.
    - [x] 10-7-1. 생성 W/O의 `TACT_TIME`은 모두 양수이며 블록 `TACT_TIME`은 W/O 최댓값으로 집계한다. `BV_QTY`만 바꿔도 TACT가 변하지 않는 회귀시험을 추가했다.
    - [x] 10-7-2. FN/FL/NC empirical 분석에도 같은 Case 6 고정식을 적용하며 metadata에 `tact_formula_scope=all_series_shared_np_ppt_case6`를 기록한다.
  - [x] 10-8. NP/FN/FL/NC별 W/O·블록 실적/생성 Pearson 상관관계 비교 히트맵을 신규 `MARK_LTH` 최댓값 계약의 seed 2026 산출물로 다시 생성했다. 최종 8개 PNG는 `output/analysis/multi_series_generator_heatmaps/`에 있다.
    - [x] 10-8-1. 각 PNG는 왼쪽 실적, 오른쪽 생성 데이터를 동일한 특성 순서와 색 범위 `[-1, 1]`로 표시한다. W/O는 `LTH/BTH/THK/MARK_LTH/CUT_LTH/BVL_LTH/BV_QTY/PTLST_QTY/STL_QTY/TACT_TIME`, 블록은 여기에 `WO_QTY`를 추가했다.
    - [x] 10-8-2. 실적 W/O `TACT_TIME`은 발표자료 Case 6 전 계열 공통식으로 산출하고, 실적 블록 `WO_QTY`와 `TACT_TIME`은 `PROJ_NO+GYEL+BLK_NO`별 W/O 행 수와 W/O `TACT_TIME` 최댓값으로 strict 집계했다. 누락 조인·결측·상수 특성은 원인을 출력하고 실패시키는 회귀시험을 추가했다.
    - [x] 10-8-3. 최댓값 계약으로 재생성한 8개 파일에서 행렬 대칭성·대각선 1, 공통 색 범위 `[-1,1]`, 한글 폰트와 이미지 렌더링을 다시 검증했다.
  - [x] 10-9. 신규 다계열 생성기의 블록 `MARK_LTH`를 W/O 최댓값 계약으로 전환했다.
    - [x] 10-9-1. block chain 적합, 생성 W/O 보정, 생성 블록 집계, 실제 비교 집계에 입력에서 전수 판별한 `max/sum` 계약을 공통 적용했다. strict 신규 loader의 최댓값 컬럼에도 `WGT/MARK_LTH`를 추가했다.
    - [x] 10-9-2. 목표 블록 `MARK_LTH`가 생성 W/O 최댓값과 정확히 같도록 순서 보존 affine 보정을 적용했다. FN의 0 마킹 블록은 W/O 전체 0으로 보존하고, 양수는 계열별 관측 최소 양수 지원범위 이상만 생성한다. NP/FN/FL/NC 80회 생성에서 identity 위반 0건이다.
    - [x] 10-9-3. seed 2026~2045 상관 MAE는 W/O 기준 NP `0.0552`, FN `0.1674`, FL `0.1219`, NC `0.1446`, 블록 기준 NP `0.0853`, FN `0.1512`, FL `0.1147`, NC `0.1253`이다. seed별 결과와 과거 합계 계약 비교표는 `output/analysis/multi_series_shipyard_generator_mark_max/`에 저장했다.
- [x] 11. legacy 정리 전 회귀 suite 253개를 통과했다. NP 100건 SPT/load-balance는 각각 scheduled 100·unscheduled 0, actual replay identity error 0을 확인했다. canonical NP 생성기는 40 seed·4,000 block에서 식/집계 위반 0이며 Phase 1/2 10-seed exact identity와 공개 NP/MIXED CLI readback도 통과했다. 신규 전체 데이터는 block 1,642·W/O 11,646·Phase 1 mask 위반 0으로 audit했고, synthetic all-series machine contract의 merged Phase 2 1-episode smoke도 통과했다. 현재 legacy 삭제 후 회귀 suite 기준은 아래 2026-07-16 검증의 168개다. 과거 실적 planning constraint audit 1건은 identity 오류와 분리해 유지한다.

### 실적 매핑 적용 후 남은 검증

- [x] `EQP_3`을 제외한 `EQP_1`~`EQP_16`과 PLS/PLP 15대 매핑, Bay 위치, 계열 eligibility를 코드 계약으로 고정한다.
- [x] `EQP_3`은 `NC + trans` 실적 전용으로 검증하고, NC planning은 Bay 22/23/24만 허용한다.
- [x] FN/FL/NC `TACT_TIME`은 별도 계열식을 기다리지 않고 NP 정답으로 적합한 전 계열 공통식을 사용하기로 확정했다.
- [x] 2026-07-16 신규 전체 데이터 readback에서 block 1,642건/W/O 11,646건, `EQP_3` 403건 전부 NC/trans, planning W/O 11,243건을 확인했다. source Bay와 mapped home Bay가 다른 W/O 1,736건은 원본을 바꾸지 않고 actual audit 대상으로 분리한다.
- [x] 2026-07-16 전체 unit test 168개, 관련 모듈 `py_compile`, Phase 1/2 1-episode smoke, 15대 full-flow 10 W/O/4 batch/hard violation 0/event 40건, NP100 SPT/load-balance scheduled 100·unscheduled 0, actual replay identity error 0, `git diff --check`를 통과했다.
- [ ] 신규 W/O 실제 시간축 replay와 설비별 실적 비교에서 15대 매핑 행과 `EQP_3` 행을 분리해 최종 audit한다.

## 0. 이번 작업 범위

현재 요청 기준으로 구조를 다시 정리한다. 목표는 Phase 1은 유지하고, 기존 Phase 2.1(W/O -> Machine)과 Phase 2.2/3(batch/sequence)을 삭제한 뒤 `W/O batch + Machine`을 한 action으로 고르는 merged Phase 2로 합치는 것이다.

- [x] 1. 현재 파일/코드 inventory 작성
- [x] 2. 삭제 후보 확정
- [x] 3. 불필요 파일 삭제
- [x] 4. Phase 1 / merged Phase 2 구조 정리
- [x] 5. 기존 Phase 2/3 분리 코드 삭제 및 병합
- [x] 6. merged Phase 2 합성데이터 생성
- [x] 7. 기존 Phase 3 public path 제거
- [x] 8. Phase 1/merged Phase 2 feedback 기반 계층 학습. Frozen Phase 2 score + checkpoint SHA256/RunSpec pairing으로 확정했고, 동시 gradient joint update는 별도 미검증 알고리즘으로 제외한다.
- [x] 9. 최종 CLI/config 정리. 삭제한 generic PPO/REINFORCE/GNN stack의 미사용 YAML section을 제거하고 Phase별 CLI를 공개 실행면으로 확정했다.
- [x] 10. merged Phase 2 report/event log 연결. CSV/JSON report와 공통 event schema까지 연결했으며, 별도 HTML/Pygame UI 추가는 이전 사용자 지시에 따라 범위에서 제외한다.
- [ ] 11. 장시간 학습/전체 검증
- [x] 12. `Agent/`, `Train/algorithm/`, `Train/network/`, `Utils/` live/dead 코드 재분류
- [x] 13. Phase 1 전용 학습 코드를 `Phase1/`로 이동
- [x] 14. 사용하지 않는 legacy PPO/REINFORCE/common/factory stack 삭제
- [x] 15. Phase 1 ACTUAL_ validation에 실적 Bay 배정 후보(`actual_assignment`) 동시 비교 추가
- [x] 16. Phase 1 hard-case synthetic episode 생성 옵션 추가 및 검증
- [x] 17. merged Phase 2 action 후보를 Bay별 subproblem으로 분할
- [x] 18. merged Phase 2 dispatching 휴리스틱 강화
- [x] 19. merged Phase 2 장시간 학습 candidate summary 저장 병목 제거
- [x] 20. merged Phase 2 dispatch loop 반복 graph/feature 계산 캐시 적용
- [x] 21. Phase 1 pair rollout 반복 block/graph 계산 캐시 적용
- [x] 22. merged Phase 2 raw/normalized score mode CLI와 Bay별 subproblem metrics CSV 추가
- [x] 23. merged Phase 2 `--resume-checkpoint latest|path` 재개 학습 추가

## 1. 절대 규칙

- [x] fallback으로 조용히 성공 처리하지 않는다.
- [x] 컬럼, Bay, 설비, 날짜, 처리시간 누락은 print로 원인을 남기고 실패시킨다.
- [x] 실제 데이터 검증 기준 actual 8 days는 `착수일 후보 블록.xlsx`를 사용한다.
- [x] `output/`은 생성 산출물 영역이므로 이번 cleanup 대상에서 제외한다.
- [x] `input/절단03~04_NP물량_마스킹_WO_수정_260618.xlsx`는 기준 WO 데이터로 보호한다.
- [x] `input/절단03~04_NP물량_마스킹_블록_수정_260618.xlsx`는 Phase 1 학습 기준 block 데이터로 보호한다.
- [x] `AGENTS.md`, `README.md`, `현재과제_진행체크리스트.md`는 보호한다.
- [x] 코드 수정 후 최소 `py_compile`과 관련 unit test를 실행한다.

## 2. 현재 Phase 정의

### 2.1 Phase 1: Block -> Bay

- 입력: 블록 단위 feature.
- 결정: 각 블록을 Bay 22/23/24/추후 Bay 25 중 하나로 배정.
- 현재 목적: Bay별 설비 수를 반영한 부하 평준화.
- 현재 다계열 주요 지표: 설비 수로 정규화한 W/O 수 gap, `CUT_LTH` gap, `BV_QTY` gap. NP 광폭/CNT/장척 Bay24 배정은 hard mask다.
- 네트워크: 후보 edge scorer/pointer 계열. 현재는 full message-passing GNN이 아니라 graph candidate feature 기반 scorer다.

### 2.2 Phase 2: Batch-Machine Scheduling

- 입력: Phase 1의 block -> Bay 결과와 W/O feature.
- 결정: 매 step마다 `(W/O batch, Machine)` 후보 중 하나를 선택한다.
- hard 제약: batch당 W/O 1~3개, W/O 길이(LTH) 합 55,000 이하.
- batch 처리시간: batch 내부 W/O별 TACT_TIME 중 최대값.
- 현재 목적: hard violation 0, makespan 최소화 1순위, 이후 Bay 내부 설비별 `CUT_LTH`/W/O 수/`BV_QTY` gap을 사전식으로 최소화한다. 점유시간 gap은 마지막 진단값이다.
- 주요 출력: W/O별 machine assignment, batch list, machine timeline, event log, batch rule audit.
- 기존 Phase 2.1/Phase 3 분리 구조는 public path에서 삭제한다.

## 3. 현재 파일 inventory

### 3.1 보호 파일

- `AGENTS.md`
- `README.md`
- `현재과제_진행체크리스트.md`
- `main.py`
- `config_np_100.yaml`
- `config_np_full.yaml`
- `config_clean_*.yaml`
- `input/절단03~04_NP물량_마스킹_WO_수정_260618.xlsx`
- `input/절단03~04_NP물량_마스킹_블록_수정_260618.xlsx`
- `착수일 후보 블록.xlsx`

### 3.2 유지 코드

- `Environment/`: DES core, 제약, event, playback 대상 환경.
- `Utils/`: 데이터 로딩, scenario, Phase 간 통신, graph MDP, report helper.
- `Phase1/`: 신규 Phase 1 graph workflow.
- `Phase2/`: 신규 merged Phase 2 batch-machine workflow.
- `Train/`: `Train/network/mlp.py` 공통 network helper만 유지한다. Phase 전용 학습 코드는 각 `Phase*/` 패키지로 이동했다.
- `tests/`: 회귀검증 유지.
- `scripts/`: 분석/plot/export 실행 helper. 중복 script만 삭제 후보로 둔다.

### 3.3 삭제 후보

- `__pycache__/` 전체: 실행 캐시이므로 삭제.
- `*.pyc` 전체: 실행 캐시이므로 삭제.
- `docs/phase1_phase2_graph_mdp_design.md`: 핵심 내용은 이 checklist에 통합하고 삭제.
- `docs/phase_agent_independent_training_plan.md`: 핵심 내용은 이 checklist에 통합하고 삭제.
- `codex_skill_auto_apply_guide.md`: 프로젝트 코드 기준 문서가 아니므로 삭제.
- `phase1_self_labeling_commands.txt`: 실행 명령은 README/checklist에 통합 후 삭제.
- `GPT_중간발표_Phase1_설명.txt`: 발표 초안 archive 성격이므로 코드 cleanup 대상에서 삭제.

### 3.4 보류 archive

- `문헌조사_SoTA_MDP_개발방향.md`: 외부 근거 문서라 유지.
- `계층형_멀티에이전트_통신_구조.md`: 외부/설계 근거 문서라 유지.
- `메일내용/`: Q&A 근거라 유지.
- `데이터분석/`: 데이터 생성/분석 근거라 유지.
- `회의*.txt`, `회의록.txt`: 회의 근거라 유지.

## 4. 1~5번 실행 계획

### 4.1 Step 1 Inventory

- [x] Python/MD/YAML/JSON/TXT 파일 목록 확인.
- [x] Phase 2.2 / Phase 3 관련 참조 위치 확인.
- [x] 주요 파일 라인 수 확인.

### 4.2 Step 2 삭제 후보 확정

- [x] 실행 캐시 삭제 확정.
- [x] 중복 docs 삭제 확정.
- [x] skill guide/명령 txt/발표 초안 txt 삭제 확정.
- [x] 삭제 전후 git status 확인.

### 4.3 Step 3 삭제 실행

- [x] `__pycache__`, `*.pyc` 삭제.
- [x] 삭제 후보 md/txt 삭제.
- [x] 삭제 후 `find`로 잔여 캐시 확인.

### 4.4 Step 4 Phase 구조 정리

- [x] 기존 `Phase3/` public path 삭제.
- [x] `main.py` CLI 이름을 `phase2-train-batch-machine-self-labeling`으로 정리.
- [x] full workflow 문구를 `Phase 1 -> merged Phase 2`로 정리.
- [x] 테스트 import를 merged Phase 2 기준으로 갱신.

### 4.5 Step 5 기존 코드 이동/병합

- [x] Phase 2 public path는 batch-machine 학습/평가 중심으로 정리한다.
- [x] 기존 W/O -> Machine only 학습/평가 본체는 삭제한다.
- [x] 기존 batch/sequence only 학습/평가 본체는 삭제한다.
- [x] README에 Phase 1/merged Phase 2 실행 명령을 정리한다.
- [x] 관련 테스트 통과 확인.

## 5. 이번 범위 검증 명령

아래 명령은 이번 1~5번 작업 중간/종료 시 실행한다.

```bash
python -m py_compile main.py Phase1/*.py Phase2/*.py Utils/*.py Environment/*.py
python -m unittest tests.test_phase1_bay_balancer -v
python -m unittest tests.test_phase_orchestrators -v
python main.py phase1 --config config_np_100.yaml --bay-ids 22,23,24 --algorithm priority_sweep_balanced --output-dir output/phase1_cleanup_check
python main.py phase2-train-batch-machine-self-labeling --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --episodes 1 --rollout-samples 1 --heuristic-algorithms min_makespan --hidden-dim 32 --output-dir output/phase2_merged_cleanup_smoke
python main.py phase2-run-full-workflow --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --batch-machine-heuristic min_makespan --synthetic-source report_formula --synthetic-blocks 3 --gyel NP --output-dir output/phase2_merged_full_smoke
git diff --check
```

### 5.1 이번 실행 결과

- [x] `python3 -m py_compile main.py Phase1/*.py Phase2/*.py Utils/*.py Environment/*.py` 통과.
- [x] `python3 -m unittest tests.test_phase_orchestrators -v` 26개 통과.
- [x] `python3 main.py phase1 --config config_np_100.yaml --bay-ids 22,23,24 --algorithm priority_sweep_balanced --output-dir output/phase1_cleanup_check` 통과.
- [x] `python3 main.py phase2-train-batch-machine-self-labeling --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --episodes 1 --rollout-samples 1 --heuristic-algorithms min_makespan --hidden-dim 32 --output-dir output/phase2_merged_cleanup_smoke` 통과.
- [x] `python3 main.py phase2-run-full-workflow --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --batch-machine-heuristic min_makespan --synthetic-source report_formula --synthetic-blocks 3 --gyel NP --output-dir output/phase2_merged_full_smoke` 통과.
- [x] `git diff --check` 통과.
- [x] `output/` 밖 `__pycache__` 잔여 0건 확인.

### 5.2 legacy 학습 스택 삭제 후 검증 결과

- [x] `python3 -m py_compile main.py Agent/*.py Phase1/*.py Phase2/*.py Utils/*.py Train/network/*.py` 통과.
- [x] `python3 -m unittest tests.test_phase_orchestrators tests.test_phase1_self_labeling tests.test_phase1_pair_self_labeling tests.test_phase1_mdp tests.test_phase1_episode_dataset tests.test_phase1_pair_sampling_eval -v` 54개 통과.
- [x] `python3 -m unittest discover -v` 115개 통과.
- [x] `python3 main.py --help`에서 legacy `train`, `eval` subcommand 제거 확인.
- [x] `python3 main.py phase2-train-batch-machine-self-labeling --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --episodes 1 --rollout-samples 1 --heuristic-algorithms min_makespan --hidden-dim 32 --output-dir output/phase2_merged_cleanup_smoke_after_legacy_delete` 통과.
- [x] `git diff --check` 통과.

### 5.3 PDF 수식 기반 merged Phase 2 합성데이터 검증 결과

- [x] `python3 -m py_compile main.py Utils/data/report_formula_data_generator.py Phase2/merged.py tests/test_report_formula_data_generator.py` 통과.
- [x] `python3 -m unittest tests.test_report_formula_data_generator -v` 3개 통과.
- [x] `python3 main.py phase2-train-batch-machine-self-labeling --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --episodes 1 --min-blocks 3 --max-blocks 3 --rollout-samples 1 --heuristic-algorithms min_makespan --hidden-dim 16 --output-dir output/phase2_merged_report_formula_smoke_check` 통과.
- [x] `python3 main.py phase2-run-full-workflow --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --batch-machine-heuristic min_makespan --synthetic-source report_formula --synthetic-blocks 3 --gyel NP --seed 0 --output-dir output/phase2_merged_full_report_formula_smoke` 통과, batch rule violation 0.

### 5.4 Phase 1 ACTUAL_ validation 실적 후보 추가 검증 결과

- [x] `actual_8days` validation episode에서 `source_cut_bay` 기반 `actual_assignment` 후보가 `validation_candidate_summary.csv`에 추가된다.
- [x] `actual_assignment`는 학습 pseudo-label 후보가 아니라 validation 비교 후보로만 사용한다.
- [x] `actual_assignment`는 현재 hard mask를 재생하지 않고 실적 Bay load만 계산한다. 따라서 실적의 장척 Bay24 배정도 비교표/그래프에 그대로 드러난다.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase1_pair_self_labeling.Phase1PairSelfLabelingTest.test_actual_8days_validation_adds_actual_assignment_candidate -v` 통과.

### 5.5 Makespan-first merged Phase 2 검증 결과

- [x] Phase 1은 유지하고, 기존 Phase 2.1(W/O -> Machine)과 Phase 2.2/3(batch/sequence) public path를 merged Phase 2로 통합했다.
- [x] merged Phase 2 action은 매 step `(W/O batch, Machine)`이며, batch 작업시간은 batch 내부 W/O `TACT_TIME`의 max로 계산한다.
- [x] merged Phase 2 score 우선순위는 `hard_violation_count -> makespan -> Bay 내부 설비 부하 gap`이다.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile main.py Phase1/*.py Phase2/*.py Utils/*.py Environment/*.py Train/network/*.py` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators tests.test_report_formula_data_generator -v` 22개 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest discover -v` 113개 통과.
- [x] `python3 main.py phase2-train-batch-machine-self-labeling --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --episodes 1 --min-blocks 3 --max-blocks 3 --rollout-samples 1 --heuristic-algorithms min_makespan --hidden-dim 16 --output-dir output/phase2_merged_final_smoke` 통과.
- [x] `python3 main.py phase2-run-full-workflow --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --batch-machine-heuristic min_makespan --synthetic-source report_formula --synthetic-blocks 3 --gyel NP --seed 0 --output-dir output/phase2_merged_full_final_smoke` 통과, batch rule violation 0.
- [x] `git diff --check` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase1_pair_self_labeling -v` 11개 통과.

### 5.6 Phase 2 actual-8days candidate workbook 평가 경로

- [x] actual 8days Phase 2 평가는 W/O 08:00 재구성이 아니라 `착수일 후보 블록.xlsx`의 `{YYYYMMDD}_BLK` sheet를 block universe로 사용한다.
- [x] Phase 2는 W/O scheduling 문제이므로 후보 workbook의 block key(`PROJ_NO::GYEL::BLK_NO`)를 기준으로 `input/절단03~04_NP물량_마스킹_WO_수정_260618.xlsx`에서 해당 block의 전체 W/O를 확장한다.
- [x] 확장된 W/O row는 기존 `build_scenario_from_cutting_records()` 경로로 scenario job을 만든다. 따라서 merged Phase 2의 `plate_length`, `cut_length`, `bevel_quantity`, `TACT_TIME` 사용 방식이 기존 scenario와 같다.
- [x] `RT_CUT_ST_DTM`, `RT_CUT_ED_DTM`, `RT_EQP_NM` 공백은 generated planning 필수값이 아니므로 실패시키지 않는다. 단 actual source machine load 비교에서는 `RT_EQP_NM` 공백/공장 외 장비를 `actual_source_machine_load_excluded.csv`에 명시적으로 남긴다.
- [x] `scripts/evaluate_phase2_candidate_workbook.py` 추가.
- [x] 출력 파일:
  - `phase2_candidate_workbook_summary.csv`: 날짜/방법론별 score와 rank.
  - `phase1_assignments.csv`: Phase 1 block -> Bay 배정.
  - `phase2_assignment_detail.csv`: W/O -> Machine 배정.
  - `phase2_batch_detail.csv`: batch 구성.
  - `phase2_timeline_detail.csv`: machine timeline.
  - `actual_source_machine_load_summary.csv`: 실적 원본 장비 기준 load 집계.
  - `actual_source_machine_load_excluded.csv`: 실적 원본 장비 비교 제외 row.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase2_candidate_workbook -v` 4개 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile Phase2/merged.py Utils/data/phase2_candidate_workbook.py scripts/evaluate_phase2_candidate_workbook.py` 통과.
- [x] `python3 scripts/evaluate_phase2_candidate_workbook.py --config config_np_100.yaml --wo-xlsx input/절단03~04_NP물량_마스킹_WO_수정_260618.xlsx --candidate-xlsx "착수일 후보 블록.xlsx" --workdays 20260424 --bay-ids 22,23,24 --phase1-heuristic bevel_first_balanced --heuristic-algorithms min_makespan,best_fit_lth --output-dir output/phase2_candidate_workbook_smoke_20260424` 통과.
- [x] `python3 scripts/evaluate_phase2_candidate_workbook.py --config config_np_100.yaml --wo-xlsx input/절단03~04_NP물량_마스킹_WO_수정_260618.xlsx --candidate-xlsx "착수일 후보 블록.xlsx" --workdays 20260331,20260407,20260408,20260413,20260414,20260415,20260424,20260429 --bay-ids 22,23,24 --phase1-heuristic bevel_first_balanced --heuristic-algorithms min_makespan,best_fit_lth --action-pool-limit 8 --output-dir output/phase2_candidate_workbook_eval_8days_smoke` 통과.
- [x] 8일 smoke 산출 count: summary 16행, Phase 1 assignment 167행, Phase 2 assignment 2862행, batch/timeline 1914행, actual source machine load 88행, actual source machine excluded 1행.

### 5.7 Phase 1 validation best-of-K sampling 적용

- [x] Phase 1 학습의 `--rollout-samples` 값을 validation에도 동일하게 적용한다.
- [x] 예: `--rollout-samples 64`이면 validation 문제마다 `agent_greedy` 1개와 `agent_sample_2`~`agent_sample_64`까지 총 64개 agent 후보를 평가한다.

### 5.17 merged Phase 2 Bay별 subproblem 분할

- [x] Phase 2 action 의미는 기존처럼 `(W/O batch, Machine)`으로 유지한다.
- [x] 단일 W/O action 또는 `open/add/close` action으로 바꾸지 않는다. 같은 batch 안의 W/O 순서는 makespan을 바꾸지 않으므로 CE label만 불필요하게 흔들릴 수 있다.
- [x] Phase 1 block -> Bay 결과를 기준으로 W/O를 Bay별 subproblem으로 먼저 나눈다.
- [x] 각 Bay subproblem은 해당 Bay에 속한 W/O와 해당 Bay machine만 후보 action으로 사용한다.
- [x] Phase 2 agent/model은 Bay별로 따로 만들지 않고 하나를 공유한다.
- [x] 한 parent episode 안에서도 Bay별 subproblem을 독립 문제로 보고, 각 Bay마다 candidate bank를 만들고 best trajectory로 별도 CE update를 수행한다.
- [x] batch 크기 규칙은 Bay 내부 machine 수 기준으로 유지한다. 예: 한 Bay에 W/O 8개, machine 3대면 첫 wave는 `3/3/2`가 된다.
- [x] 검증 추가: 기존 통합 후보군에서는 첫 transition 후보가 24개였던 2-Bay 예제가 Bay별 분할 후 12개로 줄어드는지 확인한다.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_candidate_solves_each_bay_as_subproblem -v` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators -v` 29개 통과.
- [x] `python3 main.py phase2-train-batch-machine-self-labeling --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --episodes 1 --min-blocks 3 --max-blocks 3 --rollout-samples 1 --rollout-samples_validation 1 --heuristic-algorithms min_makespan --hidden-dim 16 --action-pool-limit 8 --validation-every 1 --validation-episodes 1 --device cpu --output-dir output/phase2_bay_subproblem_smoke` 통과.
- [x] `git diff --check -- Phase2/merged.py tests/test_phase_orchestrators.py 현재과제_진행체크리스트.md` 통과.
- [x] validation summary의 agent score/rank는 greedy 1개가 아니라 64개 agent 후보 중 가장 좋은 후보 기준으로 계산한다.
- [x] 추가 검증: training self-label도 전체 episode 단일 best가 아니라 Bay별 subproblem best로 수행한다.
- [x] training print/metrics의 `best_source`는 `22:source|23:source`처럼 Bay별 best source를 표시한다.
- [x] `candidate_summary.csv`에 `bay_id` 컬럼을 추가해 각 Bay subproblem 후보 비교 결과를 확인할 수 있게 했다.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_training_self_labels_each_bay_subproblem -v` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators -v` 30개 통과.
- [x] `python3 main.py phase2-train-batch-machine-self-labeling --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --episodes 1 --min-blocks 3 --max-blocks 3 --rollout-samples 1 --rollout-samples_validation 1 --heuristic-algorithms min_makespan --hidden-dim 16 --action-pool-limit 8 --validation-every 1 --validation-episodes 1 --device cpu --output-dir output/phase2_bay_independent_label_smoke` 통과.
- [x] smoke `candidate_summary.csv` 확인: 후보 9행, `bay_id` 22/23/24, source `min_makespan`, `agent_greedy`, `agent_sample_1`.
- [x] Phase 2 학습 upstream Phase 1 배정을 휴리스틱 대신 frozen Phase 1 pair-pointer checkpoint로 만들 수 있게 했다. 이 기능은 joint 학습이 아니며 Phase 1 model은 gradient 없이 inference만 수행한다.
- [x] `phase2-train-batch-machine-self-labeling`에 `--phase1-checkpoint`, `--phase1-samples`, `--phase1-temperature`, `--phase1-score-mode` 옵션을 추가했다.
- [x] `--phase1-checkpoint`는 checkpoint 파일 또는 output directory를 받는다. directory면 `phase1_pair_pointer_best.pt`를 명시적으로 사용하고 없으면 실패한다.
- [x] Phase 1 checkpoint mode도 Bay별 설비 수 기반 capacity weight를 유지한다. 예: config NP 기준 22/23/24 weight는 `4/3/4`.
- [x] 로그 추가: Phase 1 frozen agent 선택 결과는 `[CHECK][main._phase1_agent_assignment_builder] assignment_seed=... source=... samples=... score=...`로 출력된다.
- [x] smoke 검증: `python3 main.py phase2-train-batch-machine-self-labeling --config config_np_100.yaml --phase1-checkpoint output/phase1_pair_v9_capacity_actual8_ppb_lcp6 --phase1-bay-ids 22,23,24 --phase1-samples 2 --episodes 1 --min-blocks 3 --max-blocks 3 --rollout-samples 0 --rollout-samples_validation 0 --heuristic-algorithms min_makespan --hidden-dim 16 --validation-episodes 0 --device cpu --output-dir output/phase2_phase1_agent_upstream_smoke` 통과.
- [x] 회귀검증: `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators -v` 31개 통과.
- [x] Phase 2 장시간 학습 병목 완화: `metrics.csv`, `candidate_summary.csv`는 append 저장으로 바꾸고, `best_batches.csv`, `best_timeline.csv`, `best_machine_assignment.csv`는 최종 best만 저장한다. `candidate_summary.csv` 전체 누적 재작성으로 인한 O(N^2) 디스크/메모리 사용을 제거했다.
- [x] 검증: `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_training_writes_metrics_and_checkpoint tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_training_self_labels_each_bay_subproblem tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_training_writes_validation_with_separate_sampling -v` 통과.

### 5.18 merged Phase 2 dispatching 휴리스틱 강화

- [x] local search/CP 없이 현재 action 후보만 보고 결정하는 dispatching 휴리스틱 `workload_makespan_dispatch`를 추가했다.
- [x] 첫 W/O는 긴 TACT 작업을 우선 선택해 병렬 machine makespan을 줄인다.
- [x] 같은 batch의 후속 W/O는 batch max 시간을 늘리지 않는 W/O를 우선 채워 batch 효율을 높인다.
- [x] machine 선택은 earliest machine을 우선하고, 동률이면 현재 W/O/cut/bevel load가 적은 machine을 선택한다.
- [x] hard 제약은 기존 후보 생성 경로가 보장하는 `W/O 1~3개`, `LTH 합 55,000 이하`를 그대로 따른다.
- [x] 새 휴리스틱은 Phase 2 self-label 후보 bank와 CLI에서 명시적으로 사용할 수 있다.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_workload_makespan_dispatch_starts_batch_with_long_tact_job -v` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile main.py Phase2/merged.py Phase2/set_pointer_policy.py tests/test_phase_orchestrators.py` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators -v` 32개 통과.
- [x] `python3 main.py phase2-train-batch-machine-self-labeling --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --episodes 1 --min-blocks 3 --max-blocks 3 --rollout-samples 1 --rollout-samples_validation 1 --heuristic-algorithms workload_makespan_dispatch,min_makespan,lookahead_min_makespan --hidden-dim 16 --action-pool-limit None --checkpoint-every 1 --validation-every 1 --validation-episodes 1 --device cpu --output-dir output/phase2_workload_dispatch_smoke` 통과.
- [x] `git diff --check -- Phase2/merged.py tests/test_phase_orchestrators.py 현재과제_진행체크리스트.md` 통과.

### 5.19 merged Phase 2 장시간 학습 candidate summary 저장 병목 제거

- [x] 장시간 학습에서 `candidate_summary.csv`는 학습 자체에 필요하지 않으므로 기본 저장을 중지했다.
- [x] 기존 로그 기준 `candidate_count=72`, Bay 3개, 20,000 episode면 약 432만 candidate row가 누적되므로 디스크 병목이 된다.
- [x] 기본 출력은 `metrics.csv`, checkpoint, `validation_summary.csv`, `validation_candidate_summary.csv`, validation PNG, best assignment/batch/timeline만 유지한다.
- [x] 상세 train candidate audit가 필요하면 CLI에서 `--write-candidate-summary`를 명시적으로 켠다.
- [x] `train_phase2_batch_machine_self_labeling(..., write_candidate_summary=True)`는 기존 테스트처럼 Bay별 candidate row를 저장한다.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile main.py Phase2/merged.py tests/test_phase_orchestrators.py` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_training_writes_metrics_and_checkpoint tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_training_self_labels_each_bay_subproblem tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_training_can_use_frozen_phase1_checkpoint_upstream -v` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators -v` 32개 통과.
- [x] `python3 main.py phase2-train-batch-machine-self-labeling --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --episodes 1 --min-blocks 3 --max-blocks 3 --rollout-samples 1 --rollout-samples_validation 1 --heuristic-algorithms workload_makespan_dispatch,lpt_batch --hidden-dim 16 --action-pool-limit None --checkpoint-every 1 --validation-every 1 --validation-episodes 1 --device cpu --output-dir output/phase2_no_candidate_summary_smoke` 통과.
- [x] smoke 검증: 기본값에서 `output/phase2_no_candidate_summary_smoke/candidate_summary.csv`는 생성되지 않고, `validation_candidate_summary.csv`는 생성된다.

### 5.20 merged Phase 2 dispatch loop 캐시 최적화 검증 결과

- [x] `build_phase2_wo_machine_graph()`의 feasible edge는 후보 실행 중 변하지 않는 phase1 Bay/machine eligibility 기준이므로 Bay subproblem마다 1회만 계산하도록 캐시했다.
- [x] W/O 처리시간, LTH, 절단길이, 베벨수량도 후보 실행 중 변하지 않는 값이므로 `_Phase2DispatchCache`에 저장하고 action 생성에서 재사용한다.
- [x] lookahead makespan lower bound는 Bay별 남은 processing sum을 action마다 다시 합산하지 않고, open batch 선택분만 차감한다.
- [x] action 후보 정의, 휴리스틱 정렬 key, score 우선순위는 변경하지 않았다.
- [x] 고정 문제 회귀 테스트 `test_phase2_merged_dispatch_cache_preserves_fixed_candidate_results` 추가. `lpt_batch`, `workload_makespan_dispatch`, `lookahead_min_makespan`의 score/batch 결과가 기존 기준과 동일함을 확인한다.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile Phase2/merged.py tests/test_phase_orchestrators.py` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_dispatch_cache_preserves_fixed_candidate_results -v` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators -v` 33개 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest discover -v` 137개 통과.
- [x] 동일 smoke 기준 `phase2-train-batch-machine-self-labeling` wall time이 약 7.15초에서 4.89초로 감소했다. 실행 환경/파일 캐시에 따라 절대값은 달라질 수 있으나, score와 출력 구조는 동일했다.
- [x] `git diff --check -- Phase2/merged.py tests/test_phase_orchestrators.py 현재과제_진행체크리스트.md` 통과.

### 5.8 Phase 1 hard-case synthetic episode (과거 bootstrap 실험, 현재 학습 경로에서 제거)

- [x] 목적: 20260407/20260429처럼 강재수량을 잘 맞춰도 절단장이 따로 노는 문제를 synthetic 학습에 더 자주 노출한다.
- [x] 구현 방향: 실제 block bootstrap+jitter 후 `CUT_LTH`만 순열로 섞어 `corr(STL_QTY, CUT_LTH)`를 낮춘다.
- [x] 당시 CLI 옵션은 `--hard-case-ratio`, `--hard-case-mode cut_shuffle`, `--hard-case-target-corr`, `--validation-hard-case-ratio`였다. 2026-07-14 NP PPT 고정 산식 단일화로 active Phase 1 학습 CLI에서 제거했다.
- [x] 현재 Phase 1 synthetic 학습/validation은 bootstrap·hard-case 변형 없이 NP PPT 고정 산식만 사용한다.
- [x] 실제 8일 validation: synthetic hard-case 옵션과 무관하게 계속 `착수일 후보 블록.xlsx` 기준을 유지한다.
- [x] 검증: generator 단위 correlation 감소 확인, `phase1-train-pair-self-labeling` 1 episode smoke test, `py_compile`.
- [x] hard-case 생성은 먼저 강재수량 오름차순과 절단장 내림차순을 매칭해 어려운 문제를 만들고, target 미달 시 추가 random shuffle을 시도한다.
- [x] `metrics.csv`, `validation_summary.csv`에 `case_type`, `hard_case_mode`, hard-case 전후 `corr(STL_QTY,CUT_LTH)`를 기록한다.
- [x] Phase 1 resume 시 기존 `candidate_summary.csv`의 긴 JSON 필드가 Python 기본 CSV 제한을 넘지 않도록 field size limit을 확장했다.
- [x] Phase 1 resume 시 중단/손상된 audit CSV에 NUL byte가 있으면 개수를 출력하고 제거한 뒤 읽는다. 학습 checkpoint는 그대로 사용하고 audit CSV는 다음 write 때 정상 형태로 다시 저장된다.
- [x] Phase 1 resume 시 중단/손상된 `best_action_table.jsonl` row를 원인 출력 후 제외하고 학습 checkpoint 기준으로 이어간다.

### 5.8 Phase 1/2 validation plot 및 Phase 2 고정 문제 overfit 검증

- [x] Phase 1 validation PNG에서 `agent_sample_*` raw 후보를 직접 표시하지 않고 `Proposed(best-of-K)` 한 점으로 압축한다.
- [x] Phase 1 raw `validation_candidate_summary.csv`는 감사/디버깅을 위해 그대로 유지한다.
- [x] Phase 2 validation PNG를 추가한다: hard violation, makespan, W/O gap, 절단장 gap, 베벨 gap, 점유시간 gap, best source count, policy rank.
- [x] Phase 2 validation PNG에서도 `agent_sample_*` raw 후보를 `Proposed(best-of-K)`로 압축한다.
- [x] Phase 2 validation summary에 `Proposed(best-of-K)`와 `agent_greedy`를 별도 저장한다.
- [x] Phase 2 고정 소형 문제 overfit test를 추가했다. 고정 문제에서는 loss가 감소하므로 CE/model 경로는 정상이고, 장시간 학습 loss flat은 moving self-label 데이터와 문제 크기 변동 영향으로 분리해서 봐야 한다.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile Phase1/pair_self_labeling.py Phase2/merged.py main.py tests/test_phase1_pair_self_labeling.py tests/test_phase_orchestrators.py` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase1_pair_self_labeling -v` 12개 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators -v` 26개 통과.
- [x] `validation_candidate_summary.csv`에는 agent 64개, 휴리스틱 후보, actual assignment 후보가 모두 남는다.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase1_pair_self_labeling -v` 11개 통과.
- [x] 과거 bootstrap sampling64 smoke는 당시 기준으로 통과했다. 해당 `--block-xlsx/--noise-ratio` 학습 명령은 2026-07-14 제거했으며 현재 명령은 README의 NP PPT 고정 산식 명령을 사용한다.
- [x] sampling64 smoke 산출 검증: validation candidate 71행 = agent 64개 + 휴리스틱 6개 + actual 1개.
- [x] Phase 2 재검증: `tests.test_phase2_candidate_workbook`, `tests.test_phase_orchestrators`, `tests.test_report_formula_data_generator` 26개 통과.
- [x] Phase 2 actual 8days 재검증: `scripts/evaluate_phase2_candidate_workbook.py` 8일 candidate workbook 평가 통과.

### 5.8 Phase 1 train/validation sampling 분리

- [x] 학습 sampling은 `--rollout-samples`로 유지한다.
- [x] validation sampling은 `--rollout-samples_validation`으로 분리한다.
- [x] CLI alias로 `--rollout-samples-validation`도 같이 지원한다.
- [x] validation sampling 값을 생략하면 기존 호환성을 위해 `--rollout-samples` 값을 따른다.
- [x] `python3 main.py phase1-train-pair-self-labeling ... --rollout-samples 1 --rollout-samples_validation 4 ...` smoke 통과.
- [x] split smoke 산출 검증: summary `rollout_samples=1`, `validation_rollout_samples=4`, validation candidate 11행 = agent 4개 + 휴리스틱 6개 + actual 1개.
- [x] Phase 2 fresh 검증:
  - [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase2_candidate_workbook tests.test_phase_orchestrators tests.test_report_formula_data_generator -v` 26개 통과.
  - [x] `python3 main.py phase2-train-batch-machine-self-labeling ... --episodes 1 ...` 통과.
  - [x] `python3 scripts/evaluate_phase2_candidate_workbook.py ... --workdays 20260424 ...` 통과.
  - [x] `python3 scripts/evaluate_phase2_candidate_workbook.py ... --workdays 20260331,20260407,20260408,20260413,20260414,20260415,20260424,20260429 ...` 8일 평가 통과.

### 5.9 Phase 2 train/validation sampling 분리

- [x] Phase 2 학습 sampling은 `--rollout-samples`로 유지한다.
- [x] Phase 2 validation sampling은 `--rollout-samples_validation`으로 분리한다.
- [x] CLI alias로 `--rollout-samples-validation`도 같이 지원한다.
- [x] validation sampling 값을 생략하면 기존 호환성을 위해 `--rollout-samples` 값을 따른다.
- [x] Phase 2 validation은 학습 episode와 별도 seed의 `report_formula` holdout episode를 사용한다.
- [x] 출력 파일: `validation_summary.csv`, `validation_candidate_summary.csv`.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile main.py Phase2/merged.py tests/test_phase_orchestrators.py` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_training_writes_validation_with_separate_sampling -v` 통과.
- [x] `python3 main.py phase2-train-batch-machine-self-labeling --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --episodes 1 --min-blocks 3 --max-blocks 3 --rollout-samples 1 --rollout-samples_validation 3 --heuristic-algorithms min_makespan --hidden-dim 16 --validation-every 1 --validation-episodes 1 --output-dir output/phase2_validation_split_smoke` 통과.
- [x] smoke 산출 검증: validation summary 1행, validation candidate 5행 = agent 4개 + 휴리스틱 1개.

### 5.10 Phase 1/Phase 2 학습 GPU device 옵션

- [x] Phase 1 `phase1-train-pair-self-labeling`에 `--device cpu|cuda|cuda:0` 옵션을 추가했다.
- [x] Phase 2 `phase2-train-batch-machine-self-labeling`에 `--device cpu|cuda|cuda:0` 옵션을 추가했다.
- [x] 요청 device가 CUDA인데 현재 PyTorch에서 CUDA가 보이지 않으면 CPU fallback 없이 오류를 출력하고 중단한다.
- [x] checkpoint resume 시 optimizer tensor state도 선택 device로 이동한다.
- [x] 현재 WSL 검증 환경은 `torch 2.4.1+cpu`, `torch.cuda.is_available() == False`, `torch.version.cuda == None`으로 확인했다.
- [x] Phase 1 CPU smoke: `--device cpu`로 1 episode 학습 및 validation 산출 통과.
- [x] Phase 2 CPU smoke: `--device cpu`로 1 episode 학습 및 validation 산출 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase1_pair_self_labeling tests.test_phase_orchestrators -q` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_report_formula_data_generator tests.test_phase2_candidate_workbook -q` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile main.py Phase1/pair_self_labeling.py Phase2/merged.py tests/test_phase_orchestrators.py` 통과.
- [x] `git diff --check -- main.py Phase1/pair_self_labeling.py Phase2/merged.py tests/test_phase_orchestrators.py 현재과제_진행체크리스트.md` 통과.

### 5.11 Phase 2 checkpoint every 및 Phase 1 Proposed plot 수정

- [x] Phase 2 `phase2-train-batch-machine-self-labeling`에 `--checkpoint-every` 옵션을 추가했다.
- [x] `--checkpoint-every N`이면 `output_dir/checkpoints/phase2_batch_machine_policy_epXXXXX.pt`를 N episode마다 저장한다.
- [x] Phase 2 최종 checkpoint와 중간 checkpoint는 동일한 feature contract payload를 사용한다.
- [x] Phase 1 plot은 `candidate_summary.csv`에서 `agent_greedy`와 `agent_sample_*` 중 `learning_score_json`이 가장 좋은 후보를 Proposed로 사용한다.
- [x] Phase 1 plot의 score curve는 overall teacher best가 아니라 Proposed best-of-K의 `score_json`으로 그린다.
- [x] `proposed_source_counts.png`와 `training_quick_status.json`의 `proposed_source_counts`를 추가했다.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_plot_phase1_pair_training tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_training_writes_periodic_checkpoints -v` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile main.py Phase2/merged.py scripts/plot_phase1_pair_training.py tests/test_phase_orchestrators.py tests/test_plot_phase1_pair_training.py` 통과.
- [x] `python3 main.py phase2-train-batch-machine-self-labeling ... --checkpoint-every 1 ... --output-dir output/phase2_checkpoint_every_smoke` 통과.
- [x] `python3 scripts/plot_phase1_pair_training.py output/phase1_pair_v9_capacity_actual8_ppb_lcp6 --window 100` 통과.
- [x] smoke 산출 검증: `output/phase2_checkpoint_every_smoke/checkpoints/phase2_batch_machine_policy_ep00001.pt` 생성 확인.
- [x] plot 산출 검증: `training_quick_status.json`에 `agent_sample_*`가 Proposed source로 집계됨을 확인.

### 5.12 Phase 2 report-formula episode on-demand 생성

- [x] 기존 Phase 2 CLI는 `--episodes 20000`이면 `build_report_formula_episode_jobs(episode_count=20000)`로 학습 시작 전에 전체 합성 데이터를 미리 생성했다.
- [x] Phase 2 `report_formula` 학습 경로를 `episode_job_factory` 방식으로 변경했다.
- [x] 이제 각 episode에서 `문제 생성 -> Phase 1 휴리스틱 block-to-Bay 배정 -> merged Phase 2 batch-machine 학습` 순서로 실행된다.
- [x] validation도 시작 전에 전체 holdout list를 만들지 않고 validation 시점에 `validation_episode_job_factory`로 생성한다.
- [x] validation seed는 train episode와 독립적으로 고정해 validation 시점마다 동일 holdout 문제를 재사용한다.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_training_generates_episode_jobs_on_demand tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_training_writes_validation_with_separate_sampling tests.test_report_formula_data_generator.ReportFormulaDataGeneratorTest.test_episode_jobs_train_merged_phase2_smoke -v` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile main.py Phase2/merged.py tests/test_phase_orchestrators.py Utils/data/report_formula_data_generator.py` 통과.
- [x] `python3 main.py phase2-train-batch-machine-self-labeling ... --episodes 2 ... --output-dir output/phase2_on_demand_smoke` 통과.
- [x] smoke 로그에서 시작 전 대량 생성이 사라지고 episode별 생성/학습 순서로 출력되는 것을 확인했다.

### 5.13 Phase 2 후보 조합 속도 제어

- [x] 아래 조합형 후보 생성 방식은 5.17에서 `SELECT_MACHINE -> SELECT_WO` 순차 action으로 대체했다.
- [x] 구버전 Phase 2 merged action은 machine별 top W/O pool에서 1~3개 batch 조합을 만들었다.
- [x] 구버전 내부 기본값 `action_pool_limit=18`은 machine당 `18C1+18C2+18C3=987`개 후보를 만들기 때문에 11대 기준 step당 약 1만 action 후보가 생겼다.
- [x] Phase 2 CLI에 `--action-pool-limit` 옵션을 추가했다.
- [x] CLI 기본값은 8로 낮췄다. machine당 후보는 `8C1+8C2+8C3=92`개로 줄어든다.
- [x] 빠른 장시간 학습 권장값: `--action-pool-limit 6`, 속도 우선 smoke: `--action-pool-limit 4`.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile main.py Phase2/merged.py` 통과.
- [x] `python3 main.py phase2-train-batch-machine-self-labeling ... --action-pool-limit 4 ... --output-dir output/phase2_action_pool_smoke` 통과.
- [x] `git diff --check -- main.py Phase2/merged.py 현재과제_진행체크리스트.md` 통과.

### 5.14 Phase 2 machine-count-aware batch 크기 결정

- [x] 기존 `3개 가능하면 3개 우선` 방식은 마지막 wave에서 설비가 여러 대 남아도 `3/1`, `3/2/1/1/1` 같은 불균형 batch를 만들 수 있어 수정했다.
- [x] Phase 2 action 생성은 전체 설비가 아니라 Bay별로 현재 `machine_clock`이 가장 작은 설비, 즉 해당 Bay에서 지금 먼저 채워야 하는 설비들만 후보로 본다.
- [x] 현재 step의 목표 batch 크기는 Bay별로 `min(3, ceil(해당 Bay의 남은 W/O 수 / 해당 Bay에서 현재 가장 먼저 빈 설비 수))`로 계산한다.
- [x] 예: W/O 4개, 현재 빈 설비 3대이면 첫 batch 목표 크기 2가 되어 최종 `2/1/1` 흐름을 허용한다.
- [x] 예: W/O 8개, 현재 빈 설비 3대이면 첫 wave에서 `3/3/2` 흐름을 허용한다.
- [x] 길이(LTH) 합 55,000 제약 때문에 목표 크기 batch가 불가능하면 1개씩 줄여 가능한 batch 후보를 만든다.
- [x] 다른 Bay의 설비 수가 특정 Bay의 batch 크기 계산에 섞이지 않는지 다중 Bay 회귀 테스트를 추가했다.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_candidate_uses_machine_count_aware_batch_size tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_candidate_uses_remaining_machine_slots_for_eight_jobs tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_batch_size_is_calculated_per_bay -v` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_candidate_bank_contains_heuristics_and_agent_samples tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_training_writes_validation_with_separate_sampling -v` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile Phase2/merged.py tests/test_phase_orchestrators.py` 통과.

### 5.15 Phase 2 전체 action pool 및 lookahead 휴리스틱

- [x] `--action-pool-limit None`의 조합 전수생성 의미는 5.17에서 제거했다. 현재는 open batch에 추가할 feasible W/O 전체를 선형 후보로 평가한다.
- [x] Phase 2 `--action-pool-limit`에 `None`, `all`, `full` 값을 허용한다.
- [x] 구버전 `--action-pool-limit None`은 machine별 top-K slice 없이 현재 feasible W/O 전체에서 batch 조합을 만들었다.
- [x] 현재 integer 값을 주면 open batch에 추가할 W/O 후보를 상위 K개로 제한해 장시간 학습 속도를 제어한다.
- [x] `lookahead_min_makespan` 휴리스틱을 추가했다.
- [x] `lookahead_min_makespan`은 현재 batch 투입 후 projected makespan에 더해, Bay별 남은 TACT 부하를 해당 Bay 설비 수와 batch 최대 W/O 수로 나눈 cheap lower bound를 함께 본다.
- [x] Phase 2 checkpoint와 `summary.json`에 `action_pool_limit`, `heuristic_algorithms`를 함께 저장해 학습 재현 파라미터를 남긴다.
- [x] 5.17 이후 `None`은 조합 폭발 없이 feasible W/O 전체를 보지만, 아주 큰 Bay subproblem에서는 후보 평가 시간을 보고 필요 시 integer 제한값을 사용한다.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile main.py Phase2/merged.py tests/test_phase_orchestrators.py` 통과.
- [x] 구버전 검증: `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_action_pool_none_uses_all_feasible_jobs tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_candidate_bank_accepts_lookahead_min_makespan -v` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators.PhaseOrchestratorTest.test_phase2_merged_training_writes_periodic_checkpoints -v` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators -q` 28개 통과.
- [x] `python3 main.py phase2-train-batch-machine-self-labeling --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --episodes 1 --min-blocks 3 --max-blocks 3 --rollout-samples 1 --rollout-samples_validation 1 --heuristic-algorithms lookahead_min_makespan,min_makespan --hidden-dim 16 --action-pool-limit None --checkpoint-every 1 --validation-every 1 --validation-episodes 1 --device cpu --output-dir output/phase2_lookahead_none_smoke` 통과.
- [x] `git diff --check -- main.py Phase2/merged.py tests/test_phase_orchestrators.py 현재과제_진행체크리스트.md` 통과.

### 5.16 Phase 1 loss-only plot

- [x] `candidate_summary.csv`가 일부 episode 누락/손상되어도 `metrics.csv`만으로 loss curve를 그릴 수 있게 별도 script를 추가했다.
- [x] 명령: `python3 scripts/plot_phase1_loss_only.py output/phase1_pair_v9_capacity_actual8_ppb_lcp6 --window 100`
- [x] 출력: `loss_curve.png`, `loss_curve_normalized.png`, `loss_only_status.json`
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_plot_phase1_loss_only -v` 2개 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile scripts/plot_phase1_loss_only.py tests/test_plot_phase1_loss_only.py` 통과.
- [x] 실제 output 기준 실행 통과: metrics rows 4158, latest episode 4158.
- [x] `git diff --check -- scripts/plot_phase1_loss_only.py tests/test_plot_phase1_loss_only.py 현재과제_진행체크리스트.md` 통과.

### 5.17 Phase 2 순차 action 전환

- [x] 기존 `(W/O batch 조합, Machine)` 전체 조합 후보를 제거하고 `SELECT_MACHINE -> SELECT_WO` 순차 후보로 바꿨다.
- [x] batch close는 별도 action으로 두지 않고 목표 batch 크기 또는 길이 제약 도달 시 환경이 자동 처리한다.
- [x] 목표 batch 크기는 Bay별 현재 가장 먼저 빈 설비 수와 남은 W/O 수로 계산해 `4개/3대 -> 2/1/1`, `8개/3대 -> 3/3/2` 흐름을 유지한다.
- [x] `--action-pool-limit None`에서도 조합 전수생성이 아니라 W/O 후보 선형 개수만 평가되게 했다.
- [x] Phase 2 sequential action의 `action_pool_limit` 기본값을 `None`으로 바꿔 기본 학습은 feasible W/O 전체를 보게 했다.
- [x] Phase 2 feature contract는 12차원 sequential action feature로 갱신했다.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest tests.test_phase_orchestrators -q` 31개 통과.
- [x] `python3 main.py phase2-train-batch-machine-self-labeling --config config_np_100.yaml --phase1-heuristic bevel_first_balanced --phase1-bay-ids 22,23,24 --episodes 1 --min-blocks 3 --max-blocks 3 --rollout-samples 1 --rollout-samples_validation 1 --heuristic-algorithms min_makespan,lookahead_min_makespan --hidden-dim 16 --action-pool-limit None --checkpoint-every 1 --validation-every 1 --validation-episodes 1 --device cpu --output-dir output/phase2_sequential_action_smoke` 통과.
- [x] smoke 산출 검증: `summary.json` 기준 `edge_feature_dim=12`, `action_pool_limit=None`, validation PNG와 checkpoint 생성 확인.
- [x] smoke batch hard capacity 확인: `best_batches.csv` 기준 `max_wo_count=3`, `max_length_sum=46399.499036`.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile main.py Phase2/merged.py Phase2/set_pointer_policy.py tests/test_phase_orchestrators.py` 통과.
- [x] `git diff --check -- Phase2/merged.py Phase2/set_pointer_policy.py main.py tests/test_phase_orchestrators.py 현재과제_진행체크리스트.md` 통과.
- [x] `rg -n "[ \t]+$" Phase2/merged.py Phase2/set_pointer_policy.py main.py tests/test_phase_orchestrators.py 현재과제_진행체크리스트.md` 결과 없음.

## 6. 이후 작업: 이번 턴 제외

- [x] Phase 2 합성데이터 생성.
  - [x] `[제출본]가공공장_중간보고.pdf`의 고정 수식 기준으로 W/O 단위 합성 데이터를 만든다.
  - [x] Phase 2 학습 CLI에서 `report_formula` 합성 episode를 선택할 수 있게 한다.
- [x] 기존 Phase 3 합성데이터 경로를 merged Phase 2 경로로 흡수.
  - [x] batch 제약은 W/O 1~3개, 길이(LTH) 합 55,000 이하, batch 시간은 내부 TACT_TIME max로 유지한다.
- [x] merged Phase 2 full-flow에서 `report_formula` synthetic W/O를 `scenario["jobs"]`로 주입하는 경로를 추가한다.
- [x] Phase 1/merged Phase 2 계층 학습은 frozen Phase 2 feedback 방식으로 확정한다. 동시 gradient joint update는 현재 검증 계약이 아니므로 추가하지 않는다.
- [x] merged Phase 2는 공통 DES event log와 report까지 연결한다. 별도 playback UI 개발은 현재 범위에서 제외한다.
- [x] config 정리: 삭제된 generic RL/GNN 설정을 YAML에서 제거하고 JSON 산출물은 RunSpec/report schema로 검증한다.
- [x] legacy `Train/algorithm/phase1_*` 제거 여부 판단 및 Phase1 패키지 이동 완료.
- [ ] 장시간 학습과 전체 NP scale 검증.

## 7. 현재 cleanup 판단

- [x] `Agent/heuristics.py`: live. `main.py`, `Environment/gym_wrapper.py`, `Utils/reporting/share_report_builder.py`, tests가 사용한다. Phase1/merged Phase2 cleanup에서 삭제하지 않는다.
- [x] `Agent/policy.py`: placeholder. live caller가 없으므로 삭제한다.
- [x] `Train/algorithm/phase1_imitation.py`: Phase 1 전용이므로 `Phase1/imitation.py`로 이동한다.
- [x] `Train/algorithm/phase1_self_labeling.py`: Phase 1 전용이므로 `Phase1/self_labeling.py`로 이동한다.
- [x] `Train/algorithm/phase1_pair_self_labeling.py`: Phase 1 전용이므로 `Phase1/pair_self_labeling.py`로 이동한다.
- [x] `Train/network/phase1_pointer.py`: Phase 1 전용이므로 `Phase1/pointer_policy.py`로 이동한다.
- [x] `Train/algorithm/common.py`, `factory.py`, `ppo.py`, `reinforce.py`, `self_labeling.py`: old generic environment RL stack. 현재 Phase 1/merged Phase 2 self-labeling 경로와 분리되어 있으므로 삭제한다.
- [x] `Train/network/feature_builder.py`, `gnn.py`, `policy_value.py`: old generic environment RL stack에서만 사용한다. generic stack 삭제와 함께 삭제한다.
- [x] `Train/network/mlp.py`: live. Phase 1/merged Phase 2 scorer와 pointer policy가 공통 사용하므로 유지한다.
- [x] `Utils/`: 대부분 live. `main.py`, Phase1/Phase2, tests, scripts에서 직접 import하므로 이번 cleanup에서는 이름만 보고 삭제하지 않는다.

## 8. 공통 DES·RunSpec·계층 학습 통합 작업

### 8.1 성공 조건

- [x] Phase 2 학습 checkpoint에 feature/metric/score/제약/Phase 1 용량비/action pool/sampling 조건을 포함한 단일 RunSpec을 저장한다.
- [x] Phase 2 재개 학습은 model·optimizer·RunSpec이 모두 정확히 일치할 때만 허용한다.
- [x] 기존 legacy edge checkpoint와 RunSpec 없는 checkpoint는 자동 변환 없이 원인을 출력하고 실패시킨다.
- [x] actual 8일 평가는 반드시 `착수일 후보 블록.xlsx`의 8개 sheet를 authoritative block universe로 사용한다.
- [x] actual 8일 평가의 Phase 1 용량비, 장척 hard mask, Phase 2 score, action pool, 제약 profile, sampling 수는 checkpoint RunSpec과 동일해야 한다.
- [x] Phase 2 후보는 동일한 post-Phase1 공통 환경 snapshot을 fork해 평가하고, 최종 hard violation은 공통 constraint engine audit만 사용한다.
- [x] full-flow 학습·평가·report가 동일한 Phase 2 metric schema와 Bay별 metric 정의를 사용한다.
- [x] Phase 1 feedback 학습은 frozen Phase 2 평가 결과를 명시적으로 score에 포함하고, 비활성화 시 기존 Phase 1 score와 동일해야 한다.
- [x] 100건 Phase 1 회귀, Phase 2 고정 문제, actual 8일 smoke, 전체 unit test, 기본 DES CLI를 통과한다.

### 8.2 구현 순서

- [x] 1. 불완전한 RunSpec checkpoint 저장·재개 함수 서명과 용량비 해석을 완결한다.
- [x] 2. public Phase 2 checkpoint loader에서 RunSpec을 필수 검증한다.
- [x] 3. actual 8일 evaluator를 checkpoint RunSpec 기반으로 실행하고 manifest에 effective RunSpec을 저장한다.
- [x] 4. full-flow 결과 score와 학습 candidate score를 같은 함수로 재계산해 동일성을 검증한다.
- [x] 5. Bay별 설비 metric과 공통 constraint audit을 full-flow CSV/report에 저장한다.
- [x] 6. frozen Phase 2 feedback을 Phase 1 self-label 후보 평가에 연결한다.
- [x] 7. 계층 학습 방식은 frozen feedback으로 확정한다. Alternating joint update는 검증되지 않은 별도 optimizer 결합을 만들지 않기 위해 현재 범위에서 추가하지 않는다.
- [x] 8. README 실행 명령과 checkpoint 비호환 규칙을 갱신한다.
- [x] 9. 관련 unit/smoke/100건/actual 8일/DES 검증을 모두 재실행한다.

### 8.4 RunSpec 및 actual 평가 검증 결과

- [x] Phase 2 checkpoint에 `run_spec`을 저장하고 재개 시 exact match를 검사한다.
- [x] optimizer state, RunSpec, set-pointer feature schema 중 하나라도 없으면 원인을 출력하고 실패한다.
- [x] Phase 1 Bay 용량비는 명시값의 Bay key를 엄격히 검사하고, 명시값이 없을 때만 현재 enabled machine 수로 확정한다.
- [x] actual evaluator의 과거 고정값 `action_pool_limit=18`을 제거했다. checkpoint 평가에서는 validation sampling, heuristic bank, score mode, batch limit, action pool, Phase 1 용량비/장척 mask, 제약 profile을 RunSpec과 동일하게 강제한다.
- [x] actual evaluator manifest에 `effective_run_spec`을 저장하고 `phase2_constraint_audit.csv`를 추가했다.
- [x] `tests.test_phase2_run_spec`, Phase 2 metric/profile/set-pointer/orchestrator 관련 55개 테스트 통과.
- [x] 20260424 휴리스틱 smoke: candidate block 12개, 전체 W/O 121건, Bay 용량비 4/3/4, 공통 constraint audit hard 위반 0건.
- [x] checkpoint actual smoke: checkpoint validation samples 2 + greedy 1 + 휴리스틱 2 = 후보 5개를 생성하고 RunSpec을 그대로 사용했다.

### 8.5 frozen feedback 및 full-flow 검증 결과

- [x] Phase 1 feedback checkpoint에 frozen Phase 2 checkpoint 절대경로, 파일 SHA256, 정규화된 RunSpec을 함께 저장한다.
- [x] Phase 1 재개 학습은 feedback checkpoint SHA256/RunSpec까지 exact match일 때만 허용한다.
- [x] feedback scorer와 feedback contract 중 하나만 주어지면 원인을 출력하고 실패한다.
- [x] feedback이 꺼진 Phase 1 학습은 기존 Phase 1 score contract를 그대로 사용한다.
- [x] full-flow checkpoint 추론은 greedy와 RunSpec의 validation sample 수만큼 후보를 생성하고 같은 lexicographic score로 best-of-K를 선택한다.
- [x] feedback으로 학습한 Phase 1 checkpoint는 학습 당시 frozen Phase 2 checkpoint와 SHA256/RunSpec이 같은 경우에만 full-flow에서 사용한다.
- [x] full-flow 산출물에 `bay_metric_summary.csv`, `phase2_constraint_audit.csv`, `effective_run_spec`을 저장한다.
- [x] actual split-block 실적은 한 Bay로 임의 보정하지 않는다. 실제 W/O-level Bay 부하는 유지하고 `comparison_only=1`, split block 수를 기록하며 Phase 2 feedback rank에서는 제외한다.
- [x] 관련 Phase 1/Phase 2/constraint/orchestrator targeted unit test 104개 통과.
- [x] heuristic full-flow, checkpoint best-of-K full-flow, Phase 1 feedback 학습, actual feedback 20260424, strict checkpoint pairing smoke 통과.

### 8.3 편집 경계

- 편집 허용: `Phase1/`, `Phase2/`, `Environment/`, 관련 `Utils/`, `main.py`, 관련 `scripts/`, `tests/`, `README.md`, 이 checklist.
- 보호: `input/`, 기존 `output/`, PDF, 회의/메일/데이터분석 근거, 이번 작업과 무관한 dirty 파일.
- 삭제 금지: legacy 여부가 호출 그래프와 테스트로 입증되지 않은 파일.

### 8.6 2026-07-10 공통 DES 최종 audit

- [x] `Phase1_Phase2_공통DES_통합_개발체크리스트.md`를 현재 코드/테스트 증거와 동기화했다.
- [x] Phase 1 `capacity_weight` 누락을 1.0으로 대체하는 silent fallback을 제거했다.
- [x] Phase 2 final audit가 W/O 수, LTH 합, batch max TACT, interval을 W/O 원본에서 독립 재계산한다.
- [x] Phase 1/Phase 2 후보가 공통 planning/runtime snapshot과 Phase 2 공통 constraint/metric 경로를 사용함을 회귀 테스트했다.
- [x] runtime import graph을 검토해 호출 0건인 구 GraphEdgePolicy 학습/평가 경로와 `Utils/training_report.py`를 제거했다. 공통 graph state/export는 live로 유지했다.
- [x] 공통 환경·Phase 2 state가 비정상 수치(`NaN/inf`), 비정수 W/O/강재수량, 누락 machine load를 기본값으로 대체하지 않고 실패하도록 회귀 테스트를 추가했다.
- [x] 최종 재검증: unit 190개, 100건 Phase 1/SPT/load-balance, factory replay identity 0, playback hard 0, actual 8일 hard 0, strict checkpoint full-flow hard 0 통과.

### 8.7 2026-07-10 최종 실행 증거

- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile main.py Agent/*.py Phase1/*.py Phase2/*.py Environment/*.py Environment/constraints/*.py Utils/*.py Train/network/*.py scripts/*.py` 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest discover -q` 190개 통과.
- [x] `python3 main.py phase1 --config config_np_100.yaml --bay-ids 22,23,24 --algorithm priority_sweep_balanced --output-dir /tmp/pmsp_phase1_100_final` 통과: 100 W/O, 18 block, 누락 0, 장척 Bay24 0.
- [x] `python3 main.py simulate --config config_np_100.yaml --heuristic spt` 통과: scheduled 100, unscheduled 0, event 800.
- [x] `python3 main.py simulate --config config_np_100.yaml --heuristic load_balance` 통과: scheduled 100, unscheduled 0, event 800.
- [x] `python3 main.py factory-replay --config config_np_100.yaml` 통과: identity error 0. 과거 실적 constraint audit 1건은 비교/데이터 품질 결과로 보존.
- [x] `python3 main.py playback --config config_np_100.yaml --heuristic spt` 통과: hard violation 0, event identity error 0.
- [x] actual 8일 strict RunSpec checkpoint 평가 통과: 8문제, 40후보, audit 실패 0.
- [x] feedback Phase 1 + paired Phase 2 strict full-flow 통과: 34 W/O, 13 batch, score identity 일치, hard violation 0, event 136.
- [x] actual 8일 최종 재실행: summary 40행, constraint audit 24,038행, 실패 0.
- [x] YAML 7개에서 삭제된 `network/train/algorithm/eval/device` section이 없고 root/paths mapping이 유효함을 검증했다.

### 8.8 2026-07-10 최종 코드·설정 정리

- [x] 삭제된 generic PPO/REINFORCE/GNN stack만 참조하던 YAML 설정을 제거한다.
- [x] README의 실제 폴더 구조와 Phase 1/merged Phase 2 공개 경계를 현재 코드에 맞춘다.
- [x] Phase 2 공개 module/docstring에서 과거 Phase 2/3 분리 표현을 제거한다.
- [x] runtime import graph와 CLI/config key 사용처를 재검사해 live 코드는 보존한다. Relative import로 연결되는 DES data/state와 constraint rule 모듈도 삭제하지 않는다.
- [x] 최종 편집 후 compile, 전체 unit test 190개, `git diff --check`를 다시 실행했다.

## 9. 2026-07-11 Environment/Utils 구조 정리

### 9.1 실행 모델 확인

- [x] `Environment/` 전체 Python 모듈의 production import 경로를 확인했다.
- [x] 현재 환경은 SimPy가 아니라 `CuttingSimulation`이 decision epoch마다 다음 event 시각으로 clock을 직접 이동하는 custom event-driven DES다.
- [x] 단일 runtime class는 아니다. `simulate/replay/Gym`은 full DES인 `CuttingSimulation`, Phase 1/merged Phase 2 학습은 batch 상태와 machine clock을 직접 갱신하는 `CommonHierarchicalEnvironment`를 사용한다.
- [x] 두 runtime은 `Environment.data/events/constraints` 계약을 공유하지만 transition loop는 별도다. 따라서 현재 상태를 “SimPy 기반 단일 엔진”이라고 설명하지 않는다.
- [x] `Environment/environment.py`는 scenario를 dataclass로 변환하고 `CuttingSimulation`의 `reset/step`을 노출한다.
- [x] `Environment/simulation.py`는 candidate 생성, 제약 평가, machine availability, event 발생, decision epoch 전진을 소유한다.
- [x] `Environment/hierarchical.py`는 Phase 1 planning state와 merged Phase 2 batch runtime을 공유하는 계층 실행 상태다.
- [x] `Environment/data.py`, `state.py`, `events.py`, `reward.py`, `gym_wrapper.py`, `metrics.py`, `constraints/`는 모두 live 호출자가 있으므로 삭제하지 않는다.

### 9.2 Utils 재배치 계약

- [x] `Utils/data/`: 원본 로딩, scenario/factory 생성, 합성데이터, 평가 workbook 입력을 배치한다.
- [x] `Utils/phase1/`: Phase 1 balancing, block 생성, episode 생성, MDP를 배치한다.
- [x] `Utils/learning/`: phase graph, checkpoint, Phase 간 통신, learning package를 배치한다.
- [x] `Utils/reporting/`: playback, Pygame, HTML 공유, tact 분석을 배치한다.
- [x] `Utils/config.py`만 공통 root 설정 loader로 유지한다.
- [x] 구 경로 compatibility wrapper를 만들지 않고 production/test/script import를 모두 새 경로로 변경한다.
- [x] import 0건인 Python 파일만 삭제한다. 최종 audit 결과 `Environment` 20개, `Utils` 26개 모듈 모두 production import가 있어 추가 삭제 대상은 0개다.
- [x] README의 폴더 구조와 공개 import 설명을 새 경로로 갱신한다.
- [x] compile, 전체 unit test, 100건 SPT/load-balance, actual replay, strict full-flow, `git diff --check`를 재실행한다.

### 9.3 재배치 후 검증 증거

- [x] 전체 Python compile 통과.
- [x] `python3 -X pycache_prefix=/tmp/pmsp_pycache -m unittest discover -q`: 190개 통과.
- [x] 100건 SPT/load-balance: 각각 scheduled 100, unscheduled 0, event 800.
- [x] factory replay: actual identity error 0, event identity error 0. 과거 실적 constraint audit 1건은 그대로 보존.
- [x] 새 경로에서 Phase 2 checkpoint 생성과 strict full-flow 통과: 20 W/O, 11 batch, hard violation 0, event 80.
- [x] actual 8일 checkpoint 평가 통과: 8문제, audit 9,620행 전부 passed=True.
- [x] 구 `Utils.<module>` import 잔여 0건, `git diff --check` 통과.
