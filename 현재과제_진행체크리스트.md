# 현재과제 진행 체크리스트

이 파일은 현재 상태와 다음 작업만 기록하는 단일 master checklist다. 완료된 상세 이력은 Git history에서 확인하며, 폐기된 설계와 과거 실행 로그는 이 파일에 누적하지 않는다.

## 1. 현재 공개 경로

- [x] Phase 1 공개 학습: `phase1-train-pair-self-labeling`
- [x] merged Phase 2 공개 학습: `phase2-train-batch-machine-self-labeling`
- [x] 전체 실행: `phase2-run-full-workflow`
- [x] 합성데이터 생성: `generate-phase1-blocks`
- [x] 공장 DES/replay: `simulate`, `playback`, `actual-replay`, `factory-replay`
- [x] 현재 계약 문서: `다계열_Phase1_정책_및_합성데이터_생성_계약.md`
- [x] Notion용 합성데이터 상세 문서: `다계열_학습데이터_생성_Notion_정리.md`
- [x] 학습 실행 문서: `다계열_Phase1_Phase2_학습_실행_가이드.md`
- [x] Agent logits·greedy/sampling·local index·실제 해 복원 5-step 상세 문서: `Phase1_Phase2_CE_Target_5Step_완전예제.md`
- [x] 빠른 진입점: `README.md`
- [x] legacy CLI·코드·설정·중복 문서와 생성 산출물 정리

## 2. 데이터 계약

- [x] 100건 회귀 기준은 `config_mixed.yaml`과 `input/절단03~04_NP물량_마스킹_WO_수정_260618.xlsx`다.
- [x] 전체 NP scale 기준은 `config_mixed.yaml`이며 100건 회귀 통과 후 사용한다.
- [x] MIXED 합성 원천은 전처리본 `input/260724_절단블록_데이터_None.xlsx`, `input/260724_절단WO_데이터_None.xlsx`다.
- [x] `WK_ORD_NO`가 W/O scheduling identity다.
- [x] `CUT_BAY`와 `downstream_bay`를 구분한다.
- [x] `TACT_TIME`은 분 단위 planning 처리시간이다. actual elapsed time으로 대체하지 않는다.
- [x] 필수 컬럼·수식·날짜·매핑 오류는 출력 후 예외를 발생시키며 silent fallback을 사용하지 않는다.

## 3. MIXED 합성데이터 계약

- [x] 실행 시 Excel 적합을 제거하고, 검증된 고정 `multi_series_generation_profile.json`만 읽도록 전환
- [x] profile 재생성은 별도 명령에서만 Excel을 읽으며 source SHA256과 schema version을 기록
- [x] profile 누락·schema 불일치·계열 파라미터 누락은 fallback 없이 실패
- [x] 생성 순서는 `물리 블록 특성 -> 전체 WO_QTY -> 계열 조합 -> 계열별 WO_QTY -> 계열별 W/O 특성 -> block-series 역집계`다.
- [x] 계열 조합과 계열별 W/O 수 벡터는 실적 물리 블록의 조건부 공동분포에서 선택한다.
- [x] 선택 계열의 W/O 수는 1 이상이고 `계열별 WO_QTY 합 = 물리 블록 전체 WO_QTY = 생성 W/O 행 수`다.
- [x] `WO_QTY`와 `STL_QTY`는 별도 변수다.
- [x] block-series 집계는 `WO_QTY=count`, `LTH/BTH/THK/TACT_TIME=max`, `CUT_LTH/MARK_LTH/BVL_LTH/STL_QTY/BV_QTY/PTLST_QTY=sum`이다.
- [x] NP는 발표자료 고정 수식을 사용한다.
- [x] FN/FL/NC는 같은 생성 흐름과 식 형태를 사용하되 계열별 계수·잔차·지원 규격을 사용한다.
- [x] W/O BTH는 계열별 로그선형식으로 생성하고 block BTH는 W/O BTH 최댓값이다.
- [x] 전 계열 TACT_TIME은 공통 Case 6 식 `0.3037*CUT_LTH + 0.1325*MARK_LTH + 0.4790*THK + 0.3840*PTLST_QTY`를 사용한다.
- [x] 사후 block swap, 계열 교환, 상관관계 맞춤용 값 재배치를 사용하지 않는다.
- [x] Phase 1과 Phase 2는 같은 seed에서 동일 episode job identity를 사용한다.

## 4. 공장·설비 계약

- [x] MIXED Phase 1/2 planning machine은 총 15대다.
- [x] Bay 22: `PLS21`, `PLS22`, `PLS23`, `PLS24`
- [x] Bay 23: `PLS31`, `PLS32`, `PLS33`
- [x] Bay 24: `PLS41`, `PLS42`, `PLS43`, `PLS44`
- [x] Bay 25: `PLS51`, `PLS52`
- [x] trans: `PLP01`, `PLP02`
- [x] 실제 `EQP_1..16`은 확정 PLS/PLP identity로 변환한다.
- [x] `EQP_3`은 실적에서 NC/trans로만 관측된 actual-only 설비이며 planning 후보에서는 제외한다.
- [x] machine 수와 Bay capacity weight는 위 machine identity에서 계산하며 별도 숫자로 중복 관리하지 않는다.
- [x] `config_mixed.yaml`/`config_mixed.yaml`의 NP DES/replay는 실적 scope의 13대 factory를 사용하며 MIXED planning 15대와 분리한다.

## 5. Phase 1 계약

- [x] action은 `(block-series, Bay)` pair 선택이다.
- [x] 동일 `PROJ_NO+GYEL+BLK_NO`의 W/O는 같은 Bay로 간다.
- [x] 한 episode는 NP/FN/FL/NC와 Bay `22/23/24/25/trans`를 함께 포함할 수 있다.
- [x] 계열별 eligibility와 NP 광폭/CNT/장척 규칙은 action mask에서 적용한다.
- [x] NP 장척은 동일 `PROJ_NO+GYEL+BLK_NO` W/O의 `CUT_LTH` 합 `>= 1000`으로 판정하며 Bay 22/23 hard constraint로 적용한다.
- [x] NP `BTH > 4500`은 Bay 22/23 hard constraint다.
- [x] 한 물리 episode는 학습 직전에 `NP_NC(22/23/24)`와 `FN_FL(25/trans)` 두 독립 자원군 subproblem으로 분리한다.
- [x] `shared_and_series`는 각 자원군 안에서만 `자원군 전체 gap -> 자원군 내 계열별 gap 합`을 W/O, CUT_LTH, BV_QTY 순서로 비교한다.
- [x] `series_only`는 `NP gap + NC gap` 또는 `FN gap + FL gap`을 W/O, CUT_LTH, BV_QTY 순서로 비교하며 다른 자원군 gap을 teacher score에 넣지 않는다.
- [x] Phase 1 checkpoint의 model/optimizer를 다른 objective scope로 이어서 학습할 수 있게 하고, 전환 시 기존 best validation score를 재사용하지 않으며 전환 사실을 로그/checkpoint에 남긴다.
- [x] 학습 실행 가이드에 두 objective scope의 최초 실행·재개·validation·Phase 2 연동 계약을 명시한다.
- [x] policy는 현재 MIXED pair/env feature schema와 checkpoint contract를 엄격히 검사한다.
- [x] 서로 다른 자원군의 후보와 gap을 같은 teacher 비교에 섞지 않는다.
- [x] 한 정책을 공유하되 두 subproblem이 모두 존재하면 `NP_NC teacher -> CE update`, `FN_FL teacher -> CE update`를 순서대로 수행한다.
- [x] 없는 subproblem과 없는 계열은 `0`으로 채우지 않고 학습 row·validation field에서 미존재/빈 값으로 기록한다.
- [x] validation을 `NP`, `NC`, `NP+NC`, `FN`, `FL`, `FN+FL` view로 분리하고 계열별 gap과 자원군 전체 gap을 CSV/PNG로 저장한다.
- [x] frozen Phase 1 agent와 full-flow 추론은 두 subproblem에서 best-of-K를 독립 선택한 뒤 assignment를 합친다.
- [x] FN/FL 구분 state와 subproblem checkpoint contract를 `resource_pool_subproblems_v1`, pair/env `20+8`로 고정하고 이전 scope checkpoint를 명시적으로 거절한다.
- [x] Phase 1 subproblem 단위 회귀시험, 전체 unit test, NP100 DES/replay 회귀를 통과했다.

## 6. merged Phase 2 계약

- [x] 입력은 Phase 1의 block-series Bay 배정 결과다.
- [x] 한 episode 안에서 Bay별 subproblem을 독립 self-labeling update로 학습한다.
- [x] 학습 action은 `SELECT_WO` 하나만 사용하고 `SELECT_MACHINE` transition/CE target은 생성하지 않는다.
- [x] 환경 dispatcher는 현재 전역 시각의 실행 가능한 유휴 설비 중 `machine_id` 오름차순으로 설비를 확정하고, 없을 때만 전체 설비의 다음 최소 완료 시각으로 event jump한다.
- [x] 선택된 설비는 학습 action이 아니라 W/O pointer query와 state에 들어가는 환경 context다.
- [x] open batch는 환경 상태에 유지되며 선택할 때마다 W/O 수, LTH, CUT_LTH, BV_QTY, TACT 점유가 갱신된다.
- [x] batch는 W/O 최대 3개, LTH 합 최대 55,000이다.
- [x] batch 처리시간은 batch 안 W/O의 최대 TACT_TIME이다.
- [x] 한 batch에 다른 블록·계열 W/O가 섞일 수 있지만 선택 machine의 계열 eligibility를 모두 만족해야 한다.
- [x] Phase 2 사전식 score는 `hard violation -> makespan -> Bay 내부 CUT_LTH gap 합 -> W/O 수 gap 합 -> BV_QTY gap 합 -> 점유시간 gap 합`이다.
- [x] raw/normalized score mode, RunSpec, checkpoint resume exact-match 검증을 지원한다.
- [x] Phase 1은 고정 휴리스틱 또는 frozen checkpoint best-of-K로 공급할 수 있다.
- [x] Phase 1 frozen feedback 방식의 계층 학습을 지원한다.
- [x] Phase 2 Set-Pointer state/policy에서 stage one-hot과 Machine 후보 scoring 분기를 제거하고 W/O 후보만 scoring한다.
- [x] 기존 2-stage Phase 2 checkpoint는 새 state schema와 호환되지 않으므로 새 학습이 필요하다는 계약을 checkpoint/문서에 명시한다.
- [x] 전체 유휴 상태와 가용시간 동률에서 동일 설비가 재현성 있게 선택되고, batch 1~3개·LTH 55,000·machine clock 갱신이 유지되는 회귀시험을 통과한다.
- [x] `Environment/gym_wrapper.py`의 2-stage adapter는 baseline/debug 전용이며 merged Phase 2 공개 학습 경로에서 호출하지 않는다.
- [x] Phase 2 `current_time`을 실제 전역 event clock으로 사용하고, batch 예약만으로 미래 종료시각까지 이동하지 않는다.
- [x] 현재 시각에 실행 가능한 유휴 설비가 있으면 같은 시각에 병렬 배정하고, 없으면 전체 설비의 다음 최소 완료시각으로 event jump한다.
- [x] 여러 Bay의 첫 batch는 t=0에서 병렬 시작하며 Bay 순회 순서 때문에 직렬화되지 않는다.
- [x] 모든 W/O 배정 후 남은 완료 이벤트를 drain하여 최종 `current_time=makespan`으로 종료한다.
- [x] RunSpec을 `phase2_run_spec_v4_event_clock`으로 분리해 이전 Phase 2 checkpoint의 잘못된 재개를 차단한다.

## 7. 공통 환경·제약 계약

- [x] `CuttingSimulation`은 custom event-driven DES다. SimPy 기반이 아니다.
- [x] Phase 1/2 학습은 `CommonHierarchicalEnvironment`에서 공통 planning state와 machine clock을 갱신한다.
- [x] DES와 학습 환경은 data/event/constraint schema를 공유한다.
- [x] 새 제약은 `Environment/constraints/`의 registry/profile 경로로 추가한다.
- [x] hard constraint 평가 오류는 fail-open 하지 않는다.
- [x] actual replay와 generated schedule은 같은 event schema를 사용한다.

## 8. 검증 기준

- [x] MIXED generator strict identity 및 seed 재현성 test가 있다.
- [x] Phase 1/2 동일 episode identity test가 있다.
- [x] Phase 1/2 smoke, checkpoint, validation CSV/PNG test가 있다.
- [x] Phase 1 공개 CLI smoke에서 `resource_pool_subproblems_v1`, pair/env `20+8`, 두 자원군의 독립 teacher와 optimizer update 2회를 확인했다.
- [x] 전체 177개 unit test를 통과했다.
- [x] `series_only` validation의 `NP`, `NC`, `NP+NC`, `FN`, `FL`, `FN+FL` view와 없는 계열의 빈 값/N/A 계약을 회귀시험으로 고정했다.
- [x] frozen Phase 1 checkpoint full-flow smoke에서 자원군별 best-of-K 선택 후 assignment 병합과 Phase 2 hard violation 0을 확인했다.
- [x] 전체 189개 unit test와 NP100 SPT/load-balance/playback/factory replay 회귀검증을 통과했다.
- [x] NP100 SPT/load-balance는 각각 scheduled 100, unscheduled 0, event 800을 만족한다.
- [x] actual replay identity error와 event identity error는 0이다.
- [x] 과거 실적 `PROJ_2::NP::BLK_11`의 W/O 12건 Bay split은 planning constraint audit 1건이며 replay identity 오류와 분리한다.
- [x] 고정 JSON profile 재생성 결과가 canonical 파일과 byte-for-byte 동일하다.
- [x] MIXED Phase 1/2 공개 학습 smoke에서 Excel 재적합 없이 고정 JSON만 로드한다.
- [x] Phase 2 W/O-only 및 event-clock 계약은 전체 회귀시험에 포함되며, 3대 병렬 `t=0` 시작과 다음 최소 완료 `t=20` jump를 회귀시험으로 고정했다.
- [ ] 실제 다계열 전체 replay에서 actual-only EQP_3과 planning 15대를 분리한 identity/Bay audit 완료
- [ ] 장시간 Phase 1/2 학습, holdout, full-scale 성능 검증 완료

## 9. 현재 cleanup 범위

- [x] `11th` resource-pool 분리 학습 코드를 반영했다(`resource_pool_subproblems_v1`, pair/env `20+8`, NP_NC/FN_FL 독립 CE update).
- [x] NP 전용 잔재 전면 제거: NP100 입력 xlsx 2종, `config_np_100.yaml`/`config_np_full.yaml`, DES core(`Environment/{environment,simulation,gym_wrapper,state,reward}.py`), `Agent/`, `Utils/reporting/`, `Utils/data/{io,cutting_data_loader,cutting_scenario_builder,factory_builder,scenario_generator,test_data_selection}.py`, legacy CLI 15종, legacy test 5종, `notebooks/`, `착수일 후보 블록.xlsx`
- [x] `config_mixed.yaml` 신설: Phase 2 제약 profile만 제공하며 데이터 경로·공장 topology를 담지 않는다.
- [x] 실적 데이터 2종을 `input/260724_절단블록_데이터_None.xlsx`, `input/260724_절단WO_데이터_None.xlsx` 전처리본으로 교체하고 생성기 상수·script 기본값·test·generation profile `sources.path`를 함께 갱신했다.
- [x] `main.py` 2,861 -> 1,445줄. MIXED 5개 CLI만 유지하고 고아 헬퍼와 죽은 import를 제거했다.
- [x] `Environment/__init__.py`, `Utils/__init__.py`에서 삭제 모듈 export/import를 제거했다.
- [x] README·AGENTS·체크리스트의 삭제된 CLI·config·데이터 경로 안내를 현행화했다.
- [x] `output/`, `tmp/`를 `.gitignore`로 관리한다.

## 10. cleanup 검증 결과

- [x] 전체 Python compile 통과
- [x] `python -m pytest tests/ -q`: 185 passed, 1 skipped
- [x] `generate-phase1-blocks --n-blocks 40`: block-series 49건 / W/O 329건, NP·FL·FN·NC 4계열 생성
- [x] `phase1-plan-multi-series`: 전처리본 block-series 1,137건 / W/O 6,644건 / problem 58건
- [x] Phase 1 학습 2-episode smoke: `resource_pool_subproblems_v1`, NP_NC/FN_FL 두 subproblem 독립 teacher와 CE update 확인
- [x] Phase 2 학습 2-episode smoke 통과
- [x] `phase2-run-full-workflow`: assignments 188, batches 66, events 752, constraint_violations 0
- [x] 전처리 데이터 2종 SHA256이 generation profile 계약값과 일치(W/O `9cfa7dec0ed529ea5910014969510808d4d11f93b6d92ba915143882687992fb`, 블록 `12c099f27fa75832b098d0d66b9406f0bf02b15c2918f31bfd3e09eac743cbcd`)
- [x] 전 소스 CRLF 개행과 파일 끝 개행을 저장소 관례에 맞춰 유지

## 11. cleanup 검증 명령

```bash
python -m py_compile main.py Phase1/*.py Phase2/*.py Environment/*.py Environment/constraints/*.py Utils/data/*.py Utils/learning/*.py Utils/phase1/*.py Train/network/*.py scripts/*.py
python -m pytest tests/ -q
python main.py generate-phase1-blocks --n-blocks 40 --seed 0 --output-dir output/_check/blocks
python main.py phase1-plan-multi-series --block-xlsx input/260724_절단블록_데이터_None.xlsx --wo-xlsx input/260724_절단WO_데이터_None.xlsx --output-dir output/_check/plan
git diff --check
```

## 12. 13th 실적 적합·FL MARK_LTH 계약 반영

- [x] 원격 `13th` 커밋 `0d92130`의 실적 적합 및 FL `MARK_LTH` 생성 계약을 현 프로젝트에 이식했다.
- [x] 실적 원천을 전처리본 `input/260724_절단WO_데이터_None.xlsx`(6,644건), `input/260724_절단블록_데이터_None.xlsx`(1,137건)로 교체하고 코드·test·문서·profile source metadata를 함께 갱신했다.
- [x] 전처리본은 NP/FN/FL/NC 4계열을 유지하며 W/O 계열별 건수는 NP 2,589 / FN 322 / FL 3,470 / NC 263, block-series는 NP 419 / FN 161 / FL 464 / NC 93이다.
- [x] 새 원천 SHA256은 W/O `9cfa7dec0ed529ea5910014969510808d4d11f93b6d92ba915143882687992fb`, 블록 `12c099f27fa75832b098d0d66b9406f0bf02b15c2918f31bfd3e09eac743cbcd`로 검증했다.
- [x] 전 계열 block `MARK_LTH`는 W/O `MARK_LTH` 합으로 적합·생성·실적 loader 검증한다.
- [x] FN/FL/NC 실적 적합 mode는 `pearson`으로 고정하고 고정 generation profile도 같은 mode로 재생성했다.
- [x] FL 기본 `chain`은 `길이 -> CUT_LTH -> MARK_LTH` 순서로 적합·생성한다.
- [x] FL 선택형 `dirichlet`은 실적 block MARK 총량과 W/O 수를 조건부 재추출하고 합계 보존 배분을 수행하며, 사용 method를 profile에 기록한다.
- [x] 공통 Case 6 TACT 계수는 변경하지 않았다. 계열별 TACT_TIME 변화는 계열별 CUT/MARK/THK/PTLST 생성값 변화에서만 발생한다.
- [x] 고정 profile 재빌드 SHA256 `9b7af21d07ac9b5898fd8129303641a942f715f4698d920ca2b70ec641f4dc69`가 byte-for-byte 동일했다.
- [x] 100건 동일 seed 회귀: 물리 블록 100건과 W/O 826건은 보존되고, 전처리 분포 반영으로 block-series 168→134 및 계열별 배분·TACT_TIME 합계가 변경됐다.
- [x] 검증: 40건 4계열 생성, 전체 unit test 185 passed/1 skipped, Phase 1 problem 58건, full-flow hard violation 0.

## 13. Phase 2 validation·teacher target 보강

- [x] validation을 학습과 동일한 Bay별 subproblem candidate bank로 평가
- [x] validation 문제·Phase 1 배정·agent sampling seed를 checkpoint episode와 무관하게 고정
- [x] 고정 validation의 Proposed(best-of-K) 성능으로 `phase2_best.pt` 저장
- [x] 정책 관측값과 휴리스틱 우선순위가 모두 같은 후보를 복수 CE target으로 인정
- [x] Bay별 승/동률/패배와 best checkpoint 선택 근거를 validation CSV에 기록

## 14. Phase 2 크기×분포 Type 고정 validation grid

- [x] `validation-min/max-blocks`와 `validation-block-gap`으로 양 끝을 포함한 고정 물리 블록 크기 grid를 생성
- [x] `validation-episodes`를 크기별 분포 Type 수로 사용하고 총 상위 문제 수를 `크기 수 × Type 수`로 고정
- [x] 동일 Type은 모든 블록 크기에서 같은 정규화 분포 목표를 사용하고 생성 수식·실적 공동분포는 변형하지 않음
- [x] 고정 문제는 `validation/problems/blocks_NNN/type_NN`에 한 번 기록하고 모든 validation checkpoint에서 동일하게 재사용
- [x] 매 `validation-every` 시점 결과를 `validation/evaluations/ep_NNNNNN/blocks_NNN/type_NN/bay_*`에 분리 저장
- [x] 크기별 Type 평균·표준편차 그래프와 method summary를 checkpoint별 `aggregate/`에 저장
- [x] best checkpoint는 Bay row가 아니라 `(block size, Type)` 상위 문제 1건을 동일 가중치로 비교
- [x] resume 시 validation grid의 목표·실제 분포·Job payload SHA-256 계약이 다르면 fallback 없이 실패
- [x] 전역 validation 후보 CSV는 누적하지 않고 최신 checkpoint만 보존하며, 전체 후보는 checkpoint별 Bay 폴더에 저장
- [x] CLI·unit test·smoke test로 문제 수, 고정 seed, 폴더 수, 그래프 및 `phase2_best.pt` 생성 검증
- [x] 검증 결과: 전체 unit test 202 passed/1 skipped, CLI smoke 2크기×1Type×5Bay, 상위 문제 2건 동일 가중치, 지표 PNG 5개 생성

## 15. Phase 2 validation 3단계 그래프

- [x] checkpoint별 `blocks_NNN/by_type/parent`에 Type별 상위 문제 비교 CSV와 목적함수 PNG 5개 생성
- [x] checkpoint별 `blocks_NNN/by_type/bay_*`에 실제 W/O가 존재하는 Bay만 Type별 비교 CSV와 목적함수 PNG 5개 생성
- [x] checkpoint별 `aggregate`에 block size별 Type 분포를 사용하는 method grouped boxplot 5개 생성
- [x] 그래프 지표는 `makespan`, `CUT_LTH gap`, `W/O gap`, `BV_QTY gap`, `occupancy gap`으로 고정
- [x] hard violation은 CSV score 계약에는 유지하되 validation PNG에서는 제외
- [x] Proposed는 Bay별 agent greedy/sample 전체 중 사전식 best-of-K 한 후보만 표시하고 휴리스틱은 method별 한 후보만 표시
- [x] 빈 Bay에는 기존 raw metadata만 남기고 Type 그래프나 가짜 0값을 생성하지 않음
- [x] raw 문제·후보·해 CSV와 best checkpoint 선택 로직이 변경되지 않는 회귀시험 통과
- [x] CLI smoke `2 block sizes × 2 Types × 5 Bays`: parent PNG 10개, Bay PNG 50개, aggregate boxplot 5개, hard violation PNG 0개
- [x] 전체 Python compile, `git diff --check`, unit test 203 passed/1 skipped

## 16. Phase 2 validation rank·winner 집계

- [x] 상위 `(block size, Type)` 문제의 method별 경쟁 순위를 동률 동일 rank로 계산
- [x] checkpoint별 `method_problem_scores.csv`에 rank, 1위 여부, 공동 1위 method 수 기록
- [x] checkpoint별 전체·block size별·Type별 method 1위 횟수, 단독/공동 1위 횟수, 평균 rank를 `validation_rank_summary.csv`로 저장
- [x] validation 종료 로그에 Proposed 승/동률/패배와 scope별 winner count·평균 rank 출력
- [x] 학습 teacher, score, best checkpoint selection 계약이 변하지 않는 회귀시험 통과
- [x] 검증: 전체 Python compile, validation grid 5 tests, 전체 unit test 204 tests OK
- [x] `validation/validation_rank_history.csv`에 모든 checkpoint의 scope별 rank 집계 누적
- [x] `validation/` 루트에 전체 method 1위 횟수와 Proposed 평균 rank의 checkpoint 추세 PNG 생성
- [x] 실제 `ep_001000` 결과 재생성 확인: 50문제, Proposed 1위 18건, 평균 rank 2.68
- [x] 검증: 전체 Python compile, validation grid 5 tests, 전체 unit test 204 tests OK

## 17. Main(in-distribution) / Generalization validation 분리 (2026-08-03)

- 배경: 기존 단일 validation grid(blocks 10-100, target-matched)는 학습분포(blocks 10-30)보다 크게 일반화되어 best-rate를 과소평가함(전체 50% vs in-dist 88%). 상세: `Phase2_18merge_수렴분석_및_재학습계획.md`.
- [x] `build_phase2_main_validation_grid` 추가: 학습분포 자연표본(target-matching 없음), `(block,type)` 유니크 grid, held-out seed
- [x] **MAIN validation = 정식 지표** → `<output>/validation/`, **best checkpoint 선택 기준**; 기존 grid = generalization test → `<output>/validation_generalization/`, 리포팅 전용
- [x] `train_phase2_batch_machine_self_labeling`에 `secondary_validation_problems` 추가(primary 경로 동작 불변, additive)
- [x] CLI: `--main-validation-{min-blocks,max-blocks,block-gap,episodes}` 추가(기본=학습 블록범위/`--validation-episodes`); 기존 `--validation-*`는 generalization grid
- [x] 학습/validation **겹침 없음**: MAIN seed `seed+500_000+i·7919`, 불변식 `<1_000_003`로 학습·generalization seed와 disjoint(증명+jobs 해시 확인); main.py가 `validation_overlap_check: passed` assert·출력
- [x] 검증: py_compile clean, unit **218 tests OK**, smoke run에서 두 validation 디렉토리 생성 및 `best_checkpoint.json`은 `validation/`에만
- [x] 대시보드에 in-dist MAIN best-rate 반영(참조선/타일)
