# 현재과제 진행 체크리스트

이 파일은 현재 상태와 다음 작업만 기록하는 단일 master checklist다. 완료된 상세 이력은 Git history에서 확인하며, 폐기된 설계와 과거 실행 로그는 이 파일에 누적하지 않는다.

## 1. 현재 공개 경로

- [x] Phase 1 공개 학습: `phase1-train-pair-self-labeling`
- [x] merged Phase 2 공개 학습: `phase2-train-batch-machine-self-labeling`
- [x] 전체 실행: `phase2-run-full-workflow`
- [x] 합성데이터 생성: `generate-phase1-blocks`
- [x] 공장 DES/replay: `simulate`, `playback`, `actual-replay`, `factory-replay`
- [x] 현재 계약 문서: `다계열_Phase1_정책_및_합성데이터_생성_계약.md`
- [x] Notion용 합성데이터 상세 문서: `다계열_학습데이터_생성_Notion_정리.md`
- [x] 학습 실행 문서: `다계열_Phase1_Phase2_학습_실행_가이드.md`
- [x] 빠른 진입점: `README.md`
- [x] legacy CLI·코드·설정·중복 문서와 생성 산출물 정리

## 2. 데이터 계약

- [x] 100건 회귀 기준은 `config_np_100.yaml`과 `input/절단03~04_NP물량_마스킹_WO_수정_260618.xlsx`다.
- [x] 전체 NP scale 기준은 `config_np_full.yaml`이며 100건 회귀 통과 후 사용한다.
- [x] MIXED 합성 원천은 `변경사항/절단블록_데이터.xlsx`, `변경사항/절단WO_데이터.xlsx`다.
- [x] actual 8일 평가의 authoritative block universe는 `착수일 후보 블록.xlsx`의 8개 sheet다.
- [x] `WK_ORD_NO`가 W/O scheduling identity다.
- [x] `CUT_BAY`와 `downstream_bay`를 구분한다.
- [x] `TACT_TIME`은 분 단위 planning 처리시간이다. actual elapsed time으로 대체하지 않는다.
- [x] 필수 컬럼·수식·날짜·매핑 오류는 출력 후 예외를 발생시키며 silent fallback을 사용하지 않는다.

## 3. MIXED 합성데이터 계약

- [x] 실행 시 Excel 적합을 제거하고, 검증된 고정 `multi_series_generation_profile.json`만 읽도록 전환
- [x] profile 재생성은 별도 명령에서만 Excel을 읽으며 source SHA256과 schema version을 기록
- [x] profile 누락·schema 불일치·계열 파라미터 누락은 fallback 없이 실패
- [x] 생성 순서는 `물리 블록 특성 -> 전체 WO_QTY -> 계열 조합 -> 계열별 WO_QTY -> 계열별 W/O 특성 -> block-series 역집계`다.
- [x] 계열 조합과 계열별 W/O 수 벡터는 실적 물리 블록의 조건부 공동분포에서 선택한다.
- [x] 선택 계열의 W/O 수는 1 이상이고 `계열별 WO_QTY 합 = 물리 블록 전체 WO_QTY = 생성 W/O 행 수`다.
- [x] `WO_QTY`와 `STL_QTY`는 별도 변수다.
- [x] block-series 집계는 `WO_QTY=count`, `LTH/BTH/THK/MARK_LTH/TACT_TIME=max`, `CUT_LTH/BVL_LTH/STL_QTY/BV_QTY/PTLST_QTY=sum`이다.
- [x] NP는 발표자료 고정 수식을 사용한다.
- [x] FN/FL/NC는 같은 생성 흐름과 식 형태를 사용하되 계열별 계수·잔차·지원 규격을 사용한다.
- [x] W/O BTH는 계열별 로그선형식으로 생성하고 block BTH는 W/O BTH 최댓값이다.
- [x] 전 계열 TACT_TIME은 공통 Case 6 식 `0.3037*CUT_LTH + 0.1325*MARK_LTH + 0.4790*THK + 0.3840*PTLST_QTY`를 사용한다.
- [x] 사후 block swap, 계열 교환, 상관관계 맞춤용 값 재배치를 사용하지 않는다.
- [x] Phase 1과 Phase 2는 같은 seed에서 동일 episode job identity를 사용한다.

## 4. 공장·설비 계약

- [x] MIXED Phase 1/2 planning machine은 총 15대다.
- [x] Bay 22: `PLS21`, `PLS22`, `PLS23`, `PLS24`
- [x] Bay 23: `PLS31`, `PLS32`, `PLS33`
- [x] Bay 24: `PLS41`, `PLS42`, `PLS43`, `PLS44`
- [x] Bay 25: `PLS51`, `PLS52`
- [x] trans: `PLP01`, `PLP02`
- [x] 실제 `EQP_1..16`은 확정 PLS/PLP identity로 변환한다.
- [x] `EQP_3`은 실적에서 NC/trans로만 관측된 actual-only 설비이며 planning 후보에서는 제외한다.
- [x] machine 수와 Bay capacity weight는 위 machine identity에서 계산하며 별도 숫자로 중복 관리하지 않는다.
- [x] `config_np_100.yaml`/`config_np_full.yaml`의 NP DES/replay는 실적 scope의 13대 factory를 사용하며 MIXED planning 15대와 분리한다.

## 5. Phase 1 계약

- [x] action은 `(block-series, Bay)` pair 선택이다.
- [x] 동일 `PROJ_NO+GYEL+BLK_NO`의 W/O는 같은 Bay로 간다.
- [x] 한 episode는 NP/FN/FL/NC와 Bay `22/23/24/25/trans`를 함께 포함할 수 있다.
- [x] 계열별 eligibility와 NP 광폭/CNT/장척 규칙은 action mask에서 적용한다.
- [x] NP 장척은 동일 `PROJ_NO+GYEL+BLK_NO` W/O의 `CUT_LTH` 합 `>= 1000`으로 판정하며 Bay 22/23 hard constraint로 적용한다.
- [x] NP `BTH > 4500`은 Bay 22/23 hard constraint다.
- [x] 한 물리 episode는 학습 직전에 `NP_NC(22/23/24)`와 `FN_FL(25/trans)` 두 독립 자원군 subproblem으로 분리한다.
- [x] `shared_and_series`는 각 자원군 안에서만 `자원군 전체 gap -> 자원군 내 계열별 gap 합`을 W/O, CUT_LTH, BV_QTY 순서로 비교한다.
- [x] `series_only`는 `NP gap + NC gap` 또는 `FN gap + FL gap`을 W/O, CUT_LTH, BV_QTY 순서로 비교하며 다른 자원군 gap을 teacher score에 넣지 않는다.
- [x] Phase 1 checkpoint의 model/optimizer를 다른 objective scope로 이어서 학습할 수 있게 하고, 전환 시 기존 best validation score를 재사용하지 않으며 전환 사실을 로그/checkpoint에 남긴다.
- [x] 학습 실행 가이드에 두 objective scope의 최초 실행·재개·validation·Phase 2 연동 계약을 명시한다.
- [x] policy는 현재 MIXED pair/env feature schema와 checkpoint contract를 엄격히 검사한다.
- [x] 서로 다른 자원군의 후보와 gap을 같은 teacher 비교에 섞지 않는다.
- [x] 한 정책을 공유하되 두 subproblem이 모두 존재하면 `NP_NC teacher -> CE update`, `FN_FL teacher -> CE update`를 순서대로 수행한다.
- [x] 없는 subproblem과 없는 계열은 `0`으로 채우지 않고 학습 row·validation field에서 미존재/빈 값으로 기록한다.
- [x] validation을 `NP`, `NC`, `NP+NC`, `FN`, `FL`, `FN+FL` view로 분리하고 계열별 gap과 자원군 전체 gap을 CSV/PNG로 저장한다.
- [x] frozen Phase 1 agent와 full-flow 추론은 두 subproblem에서 best-of-K를 독립 선택한 뒤 assignment를 합친다.
- [x] FN/FL 구분 state와 subproblem checkpoint contract를 `resource_pool_subproblems_v1`, pair/env `20+8`로 고정하고 이전 scope checkpoint를 명시적으로 거절한다.
- [x] Phase 1 subproblem 단위 회귀시험, 전체 unit test, NP100 DES/replay 회귀를 통과했다.

## 6. merged Phase 2 계약

- [x] 입력은 Phase 1의 block-series Bay 배정 결과다.
- [x] 한 episode 안에서 Bay별 subproblem을 독립 self-labeling update로 학습한다.
- [x] 학습 action은 `SELECT_WO` 하나만 사용하고 `SELECT_MACHINE` transition/CE target은 생성하지 않는다.
- [x] 환경 dispatcher는 현재 전역 시각의 실행 가능한 유휴 설비 중 `machine_id` 오름차순으로 설비를 확정하고, 없을 때만 전체 설비의 다음 최소 완료 시각으로 event jump한다.
- [x] 선택된 설비는 학습 action이 아니라 W/O pointer query와 state에 들어가는 환경 context다.
- [x] open batch는 환경 상태에 유지되며 선택할 때마다 W/O 수, LTH, CUT_LTH, BV_QTY, TACT 점유가 갱신된다.
- [x] batch는 W/O 최대 3개, LTH 합 최대 55,000이다.
- [x] batch 처리시간은 batch 안 W/O의 최대 TACT_TIME이다.
- [x] 한 batch에 다른 블록·계열 W/O가 섞일 수 있지만 선택 machine의 계열 eligibility를 모두 만족해야 한다.
- [x] Phase 2 사전식 score는 `hard violation -> makespan -> Bay 내부 CUT_LTH gap 합 -> W/O 수 gap 합 -> BV_QTY gap 합 -> 점유시간 gap 합`이다.
- [x] raw/normalized score mode, RunSpec, checkpoint resume exact-match 검증을 지원한다.
- [x] Phase 1은 고정 휴리스틱 또는 frozen checkpoint best-of-K로 공급할 수 있다.
- [x] Phase 1 frozen feedback 방식의 계층 학습을 지원한다.
- [x] Phase 2 Set-Pointer state/policy에서 stage one-hot과 Machine 후보 scoring 분기를 제거하고 W/O 후보만 scoring한다.
- [x] 기존 2-stage Phase 2 checkpoint는 새 state schema와 호환되지 않으므로 새 학습이 필요하다는 계약을 checkpoint/문서에 명시한다.
- [x] 전체 유휴 상태와 가용시간 동률에서 동일 설비가 재현성 있게 선택되고, batch 1~3개·LTH 55,000·machine clock 갱신이 유지되는 회귀시험을 통과한다.
- [x] `Environment/gym_wrapper.py`의 2-stage adapter는 baseline/debug 전용이며 merged Phase 2 공개 학습 경로에서 호출하지 않는다.
- [x] Phase 2 `current_time`을 실제 전역 event clock으로 사용하고, batch 예약만으로 미래 종료시각까지 이동하지 않는다.
- [x] 현재 시각에 실행 가능한 유휴 설비가 있으면 같은 시각에 병렬 배정하고, 없으면 전체 설비의 다음 최소 완료시각으로 event jump한다.
- [x] 여러 Bay의 첫 batch는 t=0에서 병렬 시작하며 Bay 순회 순서 때문에 직렬화되지 않는다.
- [x] 모든 W/O 배정 후 남은 완료 이벤트를 drain하여 최종 `current_time=makespan`으로 종료한다.
- [x] RunSpec을 `phase2_run_spec_v4_event_clock`으로 분리해 이전 Phase 2 checkpoint의 잘못된 재개를 차단한다.

## 7. 공통 환경·제약 계약

- [x] `CuttingSimulation`은 custom event-driven DES다. SimPy 기반이 아니다.
- [x] Phase 1/2 학습은 `CommonHierarchicalEnvironment`에서 공통 planning state와 machine clock을 갱신한다.
- [x] DES와 학습 환경은 data/event/constraint schema를 공유한다.
- [x] 새 제약은 `Environment/constraints/`의 registry/profile 경로로 추가한다.
- [x] hard constraint 평가 오류는 fail-open 하지 않는다.
- [x] actual replay와 generated schedule은 같은 event schema를 사용한다.

## 8. 검증 기준

- [x] MIXED generator strict identity 및 seed 재현성 test가 있다.
- [x] Phase 1/2 동일 episode identity test가 있다.
- [x] Phase 1/2 smoke, checkpoint, validation CSV/PNG test가 있다.
- [x] Phase 1 공개 CLI smoke에서 `resource_pool_subproblems_v1`, pair/env `20+8`, 두 자원군의 독립 teacher와 optimizer update 2회를 확인했다.
- [x] `series_only` validation의 `NP`, `NC`, `NP+NC`, `FN`, `FL`, `FN+FL` view와 없는 계열의 빈 값/N/A 계약을 회귀시험으로 고정했다.
- [x] frozen Phase 1 checkpoint full-flow smoke에서 자원군별 best-of-K 선택 후 assignment 병합과 Phase 2 hard violation 0을 확인했다.
- [x] 전체 189개 unit test와 NP100 SPT/load-balance/playback/factory replay 회귀검증을 통과했다.
- [x] NP100 SPT/load-balance는 각각 scheduled 100, unscheduled 0, event 800을 만족한다.
- [x] actual replay identity error와 event identity error는 0이다.
- [x] 과거 실적 `PROJ_2::NP::BLK_11`의 W/O 12건 Bay split은 planning constraint audit 1건이며 replay identity 오류와 분리한다.
- [x] 고정 JSON profile 재생성 결과가 canonical 파일과 byte-for-byte 동일하다.
- [x] MIXED Phase 1/2 공개 학습 smoke에서 Excel 재적합 없이 고정 JSON만 로드한다.
- [x] Phase 2 W/O-only 및 event-clock 계약은 전체 회귀시험에 포함되며, 3대 병렬 `t=0` 시작과 다음 최소 완료 `t=20` jump를 회귀시험으로 고정했다.
- [ ] 실제 다계열 전체 replay에서 actual-only EQP_3과 planning 15대를 분리한 identity/Bay audit 완료
- [ ] 장시간 Phase 1/2 학습, holdout, full-scale 성능 검증 완료

## 9. 현재 cleanup 범위

- [x] `10th` 문서 동기화: Phase 1 pair action·NP 장척 합계 hard mask·두 자원군 독립 학습과 Phase 2 event-clock·6개 사전식 score를 모든 활성 문서에 동일하게 반영
- [x] 과거 static HTML package, learning-data smoke, Pygame viewer 코드와 CLI 제거
- [x] standalone Phase 1/2 communication CLI와 obsolete hierarchical trace CLI 제거
- [x] 존재하지 않는 clean slice를 가리키는 YAML과 중복 기본 config 제거
- [x] README·AGENTS·canonical 계약을 현재 공개 경로와 동기화
- [x] 중복·과거 설계·회의 문서 제거
- [x] 추적된 과거 분석 PNG와 untracked 생성 산출물 제거
- [x] `output/`, `tmp/`, notebook PNG를 `.gitignore`로 관리
- [x] 사용자가 수정한 `notebooks/phase1_self_labeling_live_train.ipynb` 보존

## 10. cleanup 검증 결과

- [x] 전체 Python compile 통과
- [x] `python3 -m unittest discover -s tests -p 'test_*.py'`: 189개 통과
- [x] NP100 SPT: scheduled 100, unscheduled 0, event 800
- [x] NP100 load-balance: scheduled 100, unscheduled 0, event 800
- [x] NP100 playback: hard violation 0, event identity error 0
- [x] NP100 factory replay: actual identity error 0, event identity error 0
- [x] 과거 실적 constraint audit 1건, 영향 W/O 12건은 replay identity와 분리해 유지
- [x] Windows `simenv` Phase 1 MIXED 1-episode smoke 통과, uint32 seed 음수 overflow 회귀시험 추가

## 11. cleanup 검증 명령

```bash
python3 -X pycache_prefix=/tmp/pmsp_pycache -m py_compile main.py Agent/*.py Phase1/*.py Phase2/*.py Environment/*.py Environment/constraints/*.py Utils/data/*.py Utils/learning/*.py Utils/phase1/*.py Utils/reporting/*.py Train/network/*.py scripts/*.py
python3 -m unittest discover -s tests -p 'test_*.py'
python3 main.py show-config --config config_np_100.yaml
python3 main.py factory-summary --config config_np_100.yaml
python3 main.py simulate --config config_np_100.yaml --heuristic spt
python3 main.py simulate --config config_np_100.yaml --heuristic load_balance
python3 main.py factory-replay --config config_np_100.yaml
git diff --check
```
