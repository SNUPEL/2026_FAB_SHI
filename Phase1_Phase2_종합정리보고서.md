# 다계열 절단 공장 2단계 스케줄링 — self-labeling 학습 종합 정리 보고서

본 보고서는 선박 블록 절단 공장의 계획 문제를 **두 단계(Phase 1·Phase 2)의 순차적 의사결정 문제**로 정식화하고, 각 단계를 **self-labeling 기반 imitation learning**으로 푸는 방법론을 정리한다. 각 단계에 대해 (1) 문제 정의(상태전이모델), (2) 방법론(self-labeling 절차·정책 신경망 구조·손실함수), (3) 비교 알고리즘(휴리스틱)을 기술한다.

---

## 0. 문제 배경과 전체 구조

절단 공장은 서로 다른 네 계열(NP·NC·FN·FL)의 작업지시(이하 "작업")를 여러 **Bay**의 절단 설비에서 처리한다. 계획은 두 층위로 나뉜다.

- **Phase 1 — 블록-계열의 Bay 배정**: 같은 블록·계열에 속한 작업들을 하나의 단위로 묶어 어느 Bay에서 처리할지 결정한다.
- **Phase 2 — Bay 내부의 배치(batch) 구성 및 설비 배정**: Phase 1이 확정한 Bay 안에서, 작업들을 1~3개 단위의 배치로 묶어 개별 설비에 할당하고 시작·종료 시각을 확정한다.

물리적 설비 구조는 다섯 Bay로 구성되며, Bay별 설비 대수는 각각 **4, 3, 4, 2, 2대(총 15대)**이다. 앞의 세 Bay는 NP·NC 계열이 **공유**하는 설비군을, 뒤의 두 Bay는 FN·FL 계열이 공유하는 설비군을 이룬다. 이 설비 대수의 비(4:3:4:2:2)는 이후 부하 평준화 계산의 정규화 기준이 된다.

두 단계는 모두 **동일한 하나의 정책**을 공유하지 않는다. 각 Phase가 자기 정책을 갖되, 한 Phase 내부에서는 서로 다른 하위문제들이 **하나의 정책 파라미터를 공유**한다.

---

## 1. Phase 1 — 블록-계열의 Bay 배정

### 1.1 문제 정의 (상태전이모델)

의사결정 단위는 **동일한 프로젝트·계열·블록에 속한 작업들의 묶음**(이하 "블록-계열")이다. 같은 물리 블록이라도 계열이 다르면 서로 다른 의사결정 단위이며, 한 블록-계열의 모든 작업은 반드시 같은 Bay로 배정된다.

문제는 순차적 결정 과정으로 다음과 같이 정식화된다.

- **상태**: 아직 배정되지 않은 블록-계열의 집합, Bay별 누적 부하, 현재 부하 편차.
- **행동**: **(블록-계열, Bay)의 쌍 하나**를 선택한다. 즉 "어떤 블록을 다음에 배정할지"(선택 순서)와 "어느 Bay에 넣을지"를 **동시에** 결정한다.
- **상태전이**: 선택한 블록-계열의 부하를 해당 Bay에 더하고, 그 블록-계열을 남은 후보에서 제거한다.
- **목적**: 계열별 작업 수·절단 길이·베벨(개선) 수량의 **Bay 간 부하 편차**를 최소화한다.
- **종료**: 현재 하위문제의 모든 블록-계열이 배정되면 종료한다.

한 물리 에피소드는 **설비를 공유하는 두 자원군**으로 분할되어 독립적으로 풀린다.

| 자원군 | 포함 계열 | 사용 Bay |
|---|---|---|
| 자원군 A | NP, NC | 앞의 세 Bay |
| 자원군 B | FN, FL | 뒤의 두 Bay |

두 자원군은 공유 설비가 없으므로 후보 해와 목적함수를 서로 섞어 비교하지 않는다. 하나의 정책 파라미터를 공유하되 각 자원군의 teacher 해로 **즉시 독립 갱신**한다. 한 에피소드에 한 자원군만 있으면 갱신은 1회, 둘 다 있으면 같은 정책에 2회 일어난다.

### 1.2 실행 가능 후보 제약 (Hard Mask)

각 블록-계열이 갈 수 있는 Bay는 계열 자격과 특성 제약의 **교집합**으로 정해진다. 제약을 위반하는 (블록, Bay) 쌍은 점수를 낮추는 것이 아니라 **후보 자체에서 제거**한다.

| 조건 | 허용 Bay |
|---|---|
| NP·NC 계열 기본 | 앞의 세 Bay |
| FN·FL 계열 기본 | 뒤의 두 Bay |
| NP이고 폭이 임계값 초과 | 앞의 두 Bay로 축소 |
| NP이고 특정 유형(연결 블록) | 앞의 두 Bay로 축소 |
| NP이고 **블록-계열의 절단 길이 합이 임계값 이상**(장척) | 앞의 두 Bay로 축소 |

장척 판정은 개별 작업의 최댓값이 아니라 **같은 블록-계열에 속한 작업들의 절단 길이 합**으로 계산하며, 합이 임계값 이상이면 그 블록-계열 전체의 후보 Bay가 제한된다.

### 1.3 목적함수 — 설비 대수 정규화 사전식 부하 평준화

부하 편차는 **Bay 총량이 아니라 "Bay 부하 ÷ 설비 대수"**를 기준으로 계산한다. 예를 들어 설비 대수가 다른 세 Bay의 작업 수가 설비당 동일하다면 편차는 0이다. 각 지표의 편차는 해당 Bay들의 설비당 부하에 대한 **최댓값−최솟값**으로 정의한다.

비교는 가중합이 아니라 **사전식(lexicographic) 순서**로 이루어진다. 우선순위는 **작업 수 → 절단 길이 → 베벨 수량** 순이다. 즉 작업 수 편차가 더 작은 해가 항상 우선하고, 같을 때만 다음 지표로 넘어간다.

목적함수의 범위는 두 가지로 구분된다.

- **계열별 범위(권장)**: 각 계열(NP·NC 또는 FN·FL)을 자기 Bay들에 얼마나 고르게 폈는지만 본다. 각 자원군 안에서 "계열별 편차의 합"을 작업 수·절단 길이·베벨 순으로 비교한다.
- **공유+계열 범위(비교용)**: 위의 계열별 편차 앞에, **자원군 전체(계열 합산) 총부하의 Bay 간 편차**를 지표별로 한 단계 더 높은 우선순위로 끼워 넣는다.

여기서 "공유(shared)"란 **설비를 공유하는 한 자원군 내부 Bay들의 계열 합산 총부하 평준화**를 뜻한다. 한 Bay의 설비는 두 계열을 함께 처리하므로, 실제 설비 혼잡(및 후속 단계의 makespan)에 직접 관계되는 것은 계열 합산 총부하다. 반면 "계열(series)"은 각 계열을 독립적으로 고르게 펴는 형평 지향의 지표다. 두 지표는 상충할 수 있다 — 예컨대 한 계열을 A Bay에, 다른 계열을 B Bay에 몰아넣으면 총부하는 균형이어도 계열별로는 편중된다. 이론적으로 계열별 균형이 완전하면 총부하 균형도 따라오므로 계열별 범위가 더 세밀한 조건이며, 그래서 이를 기본으로 삼고 공유+계열 범위는 대조 실험 옵션으로 둔다.

### 1.4 상태 표현 (특징 벡터)

상태는 두 종류의 실수 벡터로 인코딩된다. 하나는 **실행 가능한 (블록-계열, Bay) 후보마다 하나씩 붙는 20차원 후보 특징**이고, 다른 하나는 현재 하위문제 전체를 요약하는 **8차원 환경 특징**이다. 모든 값은 비율·정규화된 실수라 규모가 다른 지표를 함께 다룰 수 있다.

#### (가) 후보 특징 — 20차원 (후보 = "이 블록-계열을 이 Bay에 넣는다")

| # | 원소 | 의미 | 예시값 |
|---|---|---|---|
| 1 | 블록 작업 수 비율 | 이 블록-계열의 작업 수 ÷ 하위문제 전체 작업 수 | 0.20 |
| 2 | 블록 절단 길이 비율 | 이 블록-계열의 절단 길이 합 ÷ 전체 절단 길이 합 | 0.03 |
| 3 | 블록 베벨 수량 비율 | 이 블록-계열의 베벨 수량 ÷ 전체 베벨 수량 | 0.12 |
| 4–7 | **계열 원-핫** | 이 블록의 계열을 원-핫으로 표시. **순서 = (NP, NC, FN, FL)**. NP 블록이면 (1,0,0,0) | 1, 0, 0, 0 |
| 8 | 특성 제약: 광폭 | 폭이 임계값을 넘어 Bay가 제한되면 1, 아니면 0 (NP에서만 1 가능) | 0 |
| 9 | 특성 제약: 연결 블록 | 특정 유형(연결 블록)이면 1 (NP에서만 1 가능) | 0 |
| 10 | 특성 제약: 장척 | 블록-계열 절단 길이 합이 임계값 이상이면 1 (NP에서만 1 가능) | 0 |
| 11 | 후보 Bay 작업 부하 | 후보 Bay의 이 계열 설비당 작업 수 부하 ÷ 계열 평균 | 1.10 |
| 12 | 후보 Bay 절단 부하 | 후보 Bay의 이 계열 설비당 절단 길이 부하 ÷ 계열 평균 | 0.95 |
| 13 | 후보 Bay 베벨 부하 | 후보 Bay의 이 계열 설비당 베벨 부하 ÷ 계열 평균 | 1.05 |
| 14 | 후보 Bay 설비 대수 비율 | 후보 Bay 설비 대수 ÷ 하위문제 전체 설비 대수 | 0.36 |
| 15 | 선택 후 자원군 전체 작업 편차 | 이 후보를 넣었을 때 예상되는 공유(계열 합산) 작업 수 편차 | 0.30 |
| 16 | 선택 후 계열별 작업 편차 | 이 후보를 넣었을 때 예상되는 계열별 작업 수 편차 합 | 0.45 |
| 17 | 선택 후 자원군 전체 절단 편차 | 공유 절단 길이 편차 | 0.20 |
| 18 | 선택 후 계열별 절단 편차 | 계열별 절단 길이 편차 합 | 0.25 |
| 19 | 선택 후 자원군 전체 베벨 편차 | 공유 베벨 편차 | 0.15 |
| 20 | 선택 후 계열별 베벨 편차 | 계열별 베벨 편차 합 | 0.18 |

- **"계열 표시자"(4–7)** = 계열 원-핫. 네 원소 중 그 블록의 계열 자리만 1이다. 예컨대 NC 블록이면 (0,1,0,0).
- **"특성 제약 표시자"(8–10)** = 세 개의 0/1 값. 각각 광폭·연결 블록·장척 여부이며, 이 중 하나라도 1이면 그 블록은 앞의 두 Bay로만 배정될 수 있다(§1.2). NP 계열이 아니면 항상 (0,0,0).
- **선택 후 편차(15–20)** = "이 후보를 실제로 골랐다고 가정하고" 다시 계산한 예상 편차로, 정책이 한 수 앞을 보게 하는 항이다.

> **예시 후보 벡터** (NP 블록 B1을 앞쪽 첫 Bay에 배정하는 후보):
> `[0.20, 0.03, 0.12,  1,0,0,0,  0,0,0,  1.10, 0.95, 1.05, 0.36,  0.30, 0.45, 0.20, 0.25, 0.15, 0.18]`
> → "전체의 20%에 해당하는 작업 수를 가진 NP 블록(제약 없음)을, 현재 작업 부하가 계열 평균의 1.1배인 설비 4대짜리 Bay에 넣으면, 계열별 작업 편차 예상치가 0.45가 된다"로 읽는다.

#### (나) 환경 특징 — 8차원 (하위문제 전역 요약)

| # | 원소 | 의미 | 예시값 |
|---|---|---|---|
| 1 | 진행률 | 배정 완료 블록 수 ÷ 전체 블록 수 | 0.42 |
| 2 | 잔여 블록 비율 | 남은 블록 수 ÷ 전체 블록 수 | 0.58 |
| 3 | 현재 자원군 전체 작업 편차 | 지금까지 배정 기준 공유 작업 수 편차 | 0.35 |
| 4 | 현재 계열별 작업 편차 | 계열별 작업 수 편차 합 | 0.50 |
| 5 | 현재 자원군 전체 절단 편차 | 공유 절단 길이 편차 | 0.22 |
| 6 | 현재 계열별 절단 편차 | 계열별 절단 길이 편차 합 | 0.28 |
| 7 | 현재 자원군 전체 베벨 편차 | 공유 베벨 편차 | 0.16 |
| 8 | 현재 계열별 베벨 편차 | 계열별 베벨 편차 합 | 0.20 |

> **예시 환경 벡터**: `[0.42, 0.58, 0.35, 0.50, 0.22, 0.28, 0.16, 0.20]`
> → "블록의 42%가 배정된 시점에, 현재 계열별 작업 편차가 0.50으로 가장 크다"로 읽는다.

### 1.5 정책 신경망 구조 (구체적 구성)

정책은 **다층 퍼셉트론(MLP) 기반 포인터 네트워크**이며 그래프 메시지 전달은 쓰지 않는다. 은닉 차원은 128이다. 세 개의 인코더와 하나의 포인터 선형층으로 구성된다. 모든 인코더는 `선형(입력→128) → ReLU → 선형(128→128)`의 2층 구조이며, 마지막 선형층 뒤에는 활성화를 두지 않는다.

| 구성 요소 | 입력 차원 | 구조 | 출력 |
|---|---|---|---|
| 후보 인코더 | 20 (후보당) | 선형(20→128) → ReLU → 선형(128→128) | 후보별 128차원 임베딩 |
| 환경 인코더 | 8 | 선형(8→128) → ReLU → 선형(128→128) | 128차원 |
| 질의 인코더 | 256 | 선형(256→128) → ReLU → 선형(128→128) | 128차원 질의 |
| 포인터 | 128 (후보당) | 선형(128→1) | 후보별 스칼라 점수 |

**순전파** (실행 가능 후보 수를 N이라 하자):

```
후보 임베딩   E = 후보인코더(후보특징[N×20])        ∈ R^(N×128)
환경 임베딩   g = 환경인코더(환경특징[8])            ∈ R^128
질의         q = 질의인코더( [ mean_N(E) ; g ] )      ∈ R^128     (입력 256차원)
후보 점수    z_i = 포인터( tanh( E_i + q ) )          ∈ R  (i=1..N)
선택 확률    π(i) = softmax(z)_i                       (실행 가능 후보에 대해서만)
```

즉 "모든 후보의 평균 맥락 + 전역 환경"으로 하나의 질의를 만들고, 각 후보 임베딩에 그 질의를 더해 점수를 낸다. 후보 수 N이 달라져도 같은 인코더를 반복 적용하므로 파라미터 수는 불변이다. 실행 불가능한 (블록, Bay)는 애초에 후보 집합에 없으므로 별도 마스킹 항이 필요 없다.

### 1.6 self-labeling 학습

핵심은 **정책이 스스로 만든 해와 휴리스틱이 만든 해를 한데 모아, 목적함수가 가장 좋은 해를 teacher(pseudo-label)로 삼아 그 선택 시퀀스를 모방**하는 것이다.

절차는 다음과 같다.

1. 실제 데이터의 물리 블록 공동분포를 보존한 합성 에피소드를 생성한다.
2. 에피소드를 두 자원군 하위문제로 나눈다.
3. 각 자원군에서 **후보 해 집합**을 만든다: 정책의 탐욕적 해 1개 + 정책의 확률적 표본 해 다수 + 배정 휴리스틱들의 해.
4. 모든 후보를 사전식 목적함수로 채점하고, **가장 좋은 후보를 teacher로 선정**한다.
5. teacher 해가 각 단계에서 선택한 (블록, Bay)의 인덱스를 정답으로 하여 **교차 엔트로피 손실**로 정책을 갱신한다.

정책의 확률적 표본은 각 자원군에서 다수를 뽑아 그중 최선(best-of-K)을 취하며, 두 자원군의 최선 해를 하나의 부모 계획으로 병합해 감사·후속 단계 입력으로 사용한다. teacher 선정은 자원군별로 독립이며, 두 자원군의 점수를 합쳐 하나의 teacher를 뽑지 않는다.

**손실함수**는 teacher 시퀀스에 대한 단계별 교차 엔트로피의 평균이다. 각 단계에서 정책이 산출한 후보 점수 분포와 "teacher가 고른 후보 인덱스" 사이의 교차 엔트로피를 계산하고, 시퀀스 길이로 평균한 뒤 한 번 역전파·갱신한다.

**self-labeling 해(pseudo-label)의 예시 형태.** 한 자원군(NP·NC, 앞의 세 Bay)에 블록-계열 네 개가 있다고 하자(B1·B2·B4는 NP, B3는 NC). teacher 후보가 만든 해는 "(블록-계열, Bay)를 하나씩 고른 선택 시퀀스"다.

| 단계 | 선택한 (블록-계열, Bay) |
|---|---|
| 1 | (B2, 첫째 Bay) |
| 2 | (B4, 셋째 Bay) |
| 3 | (B1, 둘째 Bay) |
| 4 | (B3, 첫째 Bay) |

이 시퀀스가 만든 최종 배정은 `B1→둘째 Bay, B2→첫째 Bay, B3→첫째 Bay, B4→셋째 Bay`이다. **학습에 쓰는 pseudo-label**은 각 단계에서 "그때 실행 가능한 (블록, Bay) 후보 목록 중 선택된 것의 인덱스"다. 예컨대 1단계 후보 목록이 `[(B1,첫째),(B1,둘째),(B1,셋째),(B2,첫째),(B2,둘째),…]` 순서라면, 선택된 (B2, 첫째 Bay)의 위치(위 예에서 4번째)가 그 단계의 정답 인덱스가 된다. 즉 pseudo-label은 이 인덱스들의 **순서열**이며, 여기에 블록 선택 순서와 Bay 배정이 **동시에** 담긴다.

검증은 별도의 홀드아웃 에피소드를 여섯 관점(NP·NC·NP+NC·FN·FL·FN+FL)으로 나누어 수행한다. 최적 체크포인트는 **정책 자체 해의 성능**을 기준으로 선정하며, 이때 휴리스틱은 비교 대상으로만 표시되고 체크포인트 선정 점수에는 들어가지 않는다.

### 1.7 비교 알고리즘 (배정 휴리스틱, 3종)

세 휴리스틱은 모두 **"블록 정렬 규칙 + 공통 Bay 선택 규칙"**의 2단 구조를 가지며, 정렬 우선 지표만 다르다. Bay 선택 규칙은 공통으로 **"각 허용 Bay에 가상 배정했을 때 사전식 편차가 가장 작아지는 Bay"**를 고르는 탐욕적 최적 적합 방식이다.

| 휴리스틱 | 블록 처리 순서(내림차순 우선 지표) |
|---|---|
| 작업 수 우선 | 작업 수 → 절단 길이 → 베벨 수량 |
| 절단 길이 우선 | 절단 길이 → 작업 수 → 베벨 수량 |
| 베벨 수량 우선 | 베벨 수량 → 작업 수 → 절단 길이 |

즉 "큰 블록부터 먼저 배정하되, 넣은 뒤 편차가 가장 작아지는 Bay로 보낸다"는 공통 원리에서 "무엇을 큰 블록으로 볼지"만 다르다.

---

## 2. Phase 2 — 배치 구성 및 설비 배정

### 2.1 문제 정의 (상태전이모델)

Phase 1이 Bay를 확정한 뒤, Phase 2는 각 **Bay를 독립적인 하위문제**로 나누어 푼다. 하나의 작업은 배정된 한 Bay에만 속하며 여러 Bay를 넘나들지 않고, 각 설비도 한 Bay 전용이다. 모든 Bay 하위문제는 **동일한 하나의 정책**을 공유한다.

문제 정식화는 다음과 같다.

- **상태**: 남은 작업, 설비별 누적 부하와 종료 시각, 현재 열린 배치.
- **행동**: **작업 하나를 선택**한다(현재 설비의 열린 배치에 추가). 후술하듯 설비 선택은 정책의 행동이 아니라 환경의 결정론적 규칙이다.
- **상태전이**: 환경이 설비와 목표 배치 크기를 정해 배치를 열고, 정책이 작업을 하나씩 추가하며, 배치가 닫히면 그 설비의 완료 시각을 예약한다.
- **목적**: 제약을 지키며 makespan과 설비 간 부하 편차를 최소화한다.
- **종료**: 모든 Bay의 작업이 배치와 설비에 배정되면 종료한다.

### 2.2 상태전이 상세 — 정책 행동과 환경 결정의 분리

Phase 2의 학습 행동은 **작업 선택 하나뿐**이다. "어느 설비에 넣을지"는 정책이 결정하지 않고, **환경이 상태로부터 결정론적으로** 정한다. 이것이 Phase 1(쌍 선택)과의 핵심 차이다.

**환경의 설비·배치 크기 결정 규칙**은 다음과 같다.

- 현재 시각에 유휴이면서 남은 작업을 처리할 수 있는 설비만 후보가 된다.
- 그중 설비 식별자 오름차순으로 하나를 고른다.
- 목표 배치 크기 = min(3, ⌈해당 Bay 잔여 작업 수 ÷ 현재 유휴·실행가능 설비 수⌉, 그 설비의 실행가능 작업 수).
- 유휴 설비가 하나도 없으면 전역 시각을 **가장 빠른 완료 시각으로 점프**시켜 설비를 다시 유휴로 만든다.
- 배치가 닫히면 그 설비의 완료 시각만 미래로 예약하며 전역 시각은 이동하지 않는다.

**정책의 작업 선택**은 환경이 연 배치에 작업을 **한 개씩** 추가하는 것이다. 목표 크기에 도달하거나(자동 닫힘) 더 넣을 수 있는 작업이 없으면(제약 충돌·잔여 소진) 그 자리에서 배치를 닫는다. 배치가 닫힐 때 그 설비의 종료 시각과 누적 부하가 확정된다.

이 설계의 중요한 성질: **초기 상태(모든 설비 유휴)에서의 설비 순서는 작업 선택과 무관하게 결정된다.** 모든 설비의 시각이 동일하므로 식별자 오름차순 규칙이 그대로 적용되어, 각 Bay 내부에서 설비들이 순서대로 한 배치씩 받는다. 작업 선택이 설비 순서에 영향을 주기 시작하는 것은 **두 번째 파동부터**다 — 초기 배치 구성이 종료 시각을 정하고, 그 종료 시각이 시각 점프의 목적지(다음에 유휴가 되는 설비)를 결정하기 때문이다.

한편 전체 Bay를 하나의 통합 스케줄(전역 시각축·통합 makespan)로 산출하는 실행 모드도 존재한다. 이 모드에서는 여러 Bay의 설비가 하나의 전역 시각 위에서 식별자 순으로 섞여 호출되지만, **작업이 Bay를 넘나들지는 않으며** 결합은 공유 시각축과 통합 지표에 국한된다. 학습 자체는 어디까지나 Bay별 독립 하위문제로 이루어진다.

### 2.3 배치 제약

| 제약 | 기준 |
|---|---|
| 배치 작업 수 | 최대 3개 |
| 배치 절단 길이 합 | 상한 이하 |
| 배치 처리 시간 | 배치 내 작업 처리 시간의 **최댓값** |
| Bay·계열 | Phase 1 배정 Bay 및 설비 처리 자격과 일치 |

배치 처리 시간이 구성 작업 처리 시간의 최댓값이고 순서 의존 준비 시간이 없으므로, **배치 내부의 작업 순서는 결과에 무의미**하다. 실질적으로 의미가 있는 것은 "어떤 작업끼리 한 배치를 이루고, 어느 설비의 몇 번째 배치로 투입되는가"다.

### 2.4 목적함수 — 사전식

Phase 2의 후보 비교는 다음 사전식 순서를 따른다.

1. 강한 제약 위반 수
2. makespan(모든 활성 설비 최종 완료 시각의 최댓값)
3. Bay 내부 설비별 절단 길이 편차 합
4. Bay 내부 설비별 작업 수 편차 합
5. Bay 내부 설비별 베벨 수량 편차 합
6. Bay 내부 설비별 점유 시간 편차 합

편차는 원시 기준(최댓값−최솟값) 또는 정규화 기준(최댓값−최솟값÷평균)으로 계산한다. Bay별 하위문제에서는 해당 Bay의 편차로 teacher를 뽑고, 여러 Bay를 합친 통합 리포트에서는 편차 합을 사용하되 makespan만 전 설비 최종 완료 시각의 최댓값으로 계산한다.

### 2.5 상태 표현 (특징 벡터)

한 결정 시점의 상태는 **다섯 종류의 특징**으로 인코딩된다. Bay·열린 배치는 각각 하나의 벡터이고, 설비·작업·후보는 개수에 따라 가변 길이(설비당/작업당/후보당 한 벡터)다. 모든 값은 비율·정규화된 실수다. 아래는 NP·NC 계열을 처리하는 설비 3대짜리 Bay를 예로 든다.

#### (가) Bay 맥락 — 11차원 (현재 Bay 전체 요약)

| # | 원소 | 의미 | 예시값 |
|---|---|---|---|
| 1 | 진행률 | 처리 완료 작업 수 ÷ 이 Bay 전체 작업 수 | 0.36 |
| 2 | 잔여 작업 비율 | 남은 작업 수 ÷ 전체 작업 수 | 0.64 |
| 3 | 잔여 처리시간 비율 | 남은 처리시간 합 ÷ 전체 | 0.60 |
| 4 | 잔여 길이 비율 | 남은 판 길이 합 ÷ 전체 | 0.62 |
| 5 | 잔여 절단 비율 | 남은 절단 길이 합 ÷ 전체 | 0.58 |
| 6 | 잔여 베벨 비율 | 남은 베벨 수량 ÷ 전체 | 0.65 |
| 7 | 설비 작업 편차 | 설비 간 작업 수 정규화 편차 | 0.22 |
| 8 | 설비 절단 편차 | 설비 간 절단 길이 정규화 편차 | 0.30 |
| 9 | 설비 베벨 편차 | 설비 간 베벨 정규화 편차 | 0.18 |
| 10 | 설비 점유 편차 | 설비 간 종료시각 정규화 편차 | 0.25 |
| 11 | 설비 대수 비율 | 이 Bay 설비 대수 ÷ 전체 설비 대수 | 0.20 |

> 예시: `[0.36, 0.64, 0.60, 0.62, 0.58, 0.65, 0.22, 0.30, 0.18, 0.25, 0.20]`

#### (나) 설비 노드 — 설비당 11차원 (설비 개수만큼 반복)

| # | 원소 | 의미 | 예시값 |
|---|---|---|---|
| 1 | 종료시각/목표 | 이 설비의 예약 종료시각 ÷ 목표 점유시간 | 0.40 |
| 2 | 작업 부하/목표 | 누적 작업 수 ÷ 설비당 목표 작업 수 | 0.90 |
| 3 | 절단 부하/목표 | 누적 절단 길이 ÷ 목표 | 1.05 |
| 4 | 베벨 부하/목표 | 누적 베벨 ÷ 목표 | 0.80 |
| 5 | 배치 수/기대 | 누적 배치 수 ÷ 기대 배치 수 | 1.00 |
| 6 | 처리가능 잔여 비율 | 이 설비가 처리 가능한 남은 작업 수 ÷ 남은 작업 수 | 1.00 |
| 7 | **현재 선택 설비 여부** | 환경이 이번에 고른 설비면 1, 아니면 0 | 1.0 |
| 8–11 | **계열 처리자격 원-핫** | 이 설비가 처리 가능한 계열. **순서 = (NP, FN, FL, NC)**. NP·NC Bay면 (1,0,0,1) | 1,0,0,1 |

> 예시(선택된 NP·NC 설비): `[0.40, 0.90, 1.05, 0.80, 1.00, 1.00, 1.0,  1,0,0,1]`
> — 7번 값이 1이라 "이번에 환경이 고른 설비"임을 나타낸다. 정책 입력에 이 설비 임베딩이 들어가므로, 같은 작업이라도 어느 설비의 배치를 구성하느냐에 따라 점수가 달라진다.

#### (다) 작업 노드 — 작업당 9차원 (남은 작업 개수만큼 반복)

| # | 원소 | 의미 | 예시값 |
|---|---|---|---|
| 1 | 처리시간 비율 | 이 작업 처리시간 ÷ Bay 전체 처리시간 합 | 0.08 |
| 2 | 길이/상한 | 판 길이 ÷ 배치 길이 상한 | 0.22 |
| 3 | 절단 비율 | 이 작업 절단 길이 ÷ 전체 | 0.09 |
| 4 | 베벨 비율 | 이 작업 베벨 ÷ 전체 | 0.10 |
| 5 | 열린 배치 포함 여부 | 지금 열린 배치에 이미 들어가 있으면 1 | 0.0 |
| 6–9 | **계열 원-핫** | 이 작업의 계열. **순서 = (NP, FN, FL, NC)**. NP 작업이면 (1,0,0,0) | 1,0,0,0 |

> 예시(NP 작업): `[0.08, 0.22, 0.09, 0.10, 0.0,  1,0,0,0]`

#### (라) 열린 배치 — 7차원 (지금 채우는 중인 배치)

| # | 원소 | 의미 | 예시값 |
|---|---|---|---|
| 1 | 작업 수 비율 | 현재 배치 작업 수 ÷ 최대(3) | 0.33 |
| 2 | 길이 합 비율 | 배치 길이 합 ÷ 길이 상한 | 0.22 |
| 3 | 최대 처리시간 비율 | 배치 최대 처리시간 ÷ 전체 처리시간 합 | 0.08 |
| 4 | 절단 합 비율 | 배치 절단 합 ÷ 전체 | 0.04 |
| 5 | 베벨 합 비율 | 배치 베벨 합 ÷ 전체 | 0.03 |
| 6 | 남은 슬롯 비율 | (3 − 현재 작업 수) ÷ 3 | 0.67 |
| 7 | 남은 길이 용량 비율 | (길이 상한 − 배치 길이 합) ÷ 길이 상한 | 0.78 |

> 예시(작업 1개가 들어간 배치): `[0.33, 0.22, 0.08, 0.04, 0.03, 0.67, 0.78]`
> — 아직 열린 배치가 없으면 모두 0인 7차원 영벡터.

#### (마) 예상 결과 특징 — 후보당 7차원 (선택 가능한 각 작업에 대해)

각 후보 작업을 "실제로 넣었다고 가정"했을 때의 예상 결과다.

| # | 원소 | 의미 | 예시값 |
|---|---|---|---|
| 1 | 처리시간 증가분 비율 | 배치 처리시간(최댓값) 증가분 ÷ 전체 처리시간 | 0.00 |
| 2 | 예상 종료시각 비율 | 이 설비 예상 종료시각 ÷ 전체 처리시간 | 0.55 |
| 3 | 예상 makespan 비율 | 전 설비 예상 최종 완료시각 ÷ 전체 처리시간 | 0.70 |
| 4 | 예상 작업 편차 | 선택 후 설비 간 작업 수 정규화 편차 | 0.15 |
| 5 | 예상 절단 편차 | 선택 후 절단 길이 정규화 편차 | 0.20 |
| 6 | 예상 베벨 편차 | 선택 후 베벨 정규화 편차 | 0.12 |
| 7 | 예상 점유 편차 | 선택 후 종료시각 정규화 편차 | 0.18 |

> 예시(현재 배치 최대 처리시간보다 짧은 작업을 넣는 후보): `[0.00, 0.55, 0.70, 0.15, 0.20, 0.12, 0.18]`
> — 1번이 0이면 "이 작업을 넣어도 배치 처리시간이 늘지 않는다"(즉 배치 종료가 늦어지지 않음)를 뜻한다.

### 2.6 정책 신경망 구조 — 집합 포인터 (구체적 구성)

정책은 **가변 길이의 설비·작업 집합을 인코딩하는 MLP 기반 집합 포인터 네트워크**이며 그래프 메시지 전달은 쓰지 않는다. 은닉 차원은 128이다. 일곱 개의 인코더와 하나의 포인터 선형층으로 구성되며, 모든 인코더는 `선형(입력→128) → ReLU → 선형(128→128)`의 2층 구조다.

| 구성 요소 | 입력 차원 | 구조 | 출력 |
|---|---|---|---|
| 설비 인코더 | 11 (설비당, 공유) | 선형(11→128)→ReLU→선형(128→128) | 설비별 128차원 |
| 작업 인코더 | 9 (작업당, 공유) | 선형(9→128)→ReLU→선형(128→128) | 작업별 128차원 |
| Bay 맥락 인코더 | 11 | 선형(11→128)→ReLU→선형(128→128) | 128차원 |
| 열린 배치 인코더 | 7 | 선형(7→128)→ReLU→선형(128→128) | 128차원 |
| 예상 결과 인코더 | 7 (후보당, 공유) | 선형(7→128)→ReLU→선형(128→128) | 후보별 128차원 |
| 맥락 융합 인코더 | 768 | 선형(768→128)→ReLU→선형(128→128) | 128차원 맥락 |
| 질의 인코더 | 256 | 선형(256→128)→ReLU→선형(128→128) | 128차원 질의 |
| 포인터 | 128 (후보당) | 선형(128→1) | 후보별 스칼라 점수 |

**순전파** (설비 M대, 남은 작업 N개, 실행 가능 후보 K개):

```
설비 임베딩    H_M = 설비인코더(설비특징[M×11])     ∈ R^(M×128)
작업 임베딩    H_W = 작업인코더(작업특징[N×9])      ∈ R^(N×128)
Bay 임베딩     h_bay = Bay인코더(Bay맥락[11])        ∈ R^128
배치 임베딩    h_open = 배치인코더(열린배치[7])       ∈ R^128

전역 맥락  c = 맥락융합인코더(
     [ h_bay ; h_open ; mean(H_M) ; max(H_M) ; mean(H_W) ; max(H_W) ] )   ∈ R^128   (입력 768차원)

선택 설비 임베딩  m = H_M[환경이 고른 설비]           ∈ R^128
질의             q = 질의인코더( [ c ; m ] )           ∈ R^128   (입력 256차원)

후보 i(=어떤 작업)에 대해:
   후보 작업 임베딩   h_i = H_W[그 작업]                ∈ R^128
   예상 결과 임베딩   p_i = 예상결과인코더(예상결과_i[7]) ∈ R^128
   후보 점수         z_i = 포인터( tanh( h_i + p_i + q ) ) ∈ R
선택 확률   π(i) = softmax(z)_i     (후보 집합은 이미 실행 가능 작업으로만 구성)
```

- 설비·작업 집합은 순서에 무관하도록 **평균 풀링과 최대 풀링을 함께** 취해 맥락에 넣는다(집합 불변성).
- 최종 점수에는 **후보 작업 자체(h_i)·선택 시 예상 변화(p_i)·전역 맥락과 선택 설비를 담은 질의(q)**가 모두 직접 더해진다.
- 설비는 환경이 정하지만 선택 설비 임베딩 m이 질의에 들어가므로 설비 인코더도 학습되며, 같은 작업이라도 어느 설비의 배치를 구성하느냐에 따라 점수가 달라진다.
- 후보 집합이 실행 가능 작업으로만 구성되므로 softmax는 실행 가능 후보에 대해서만 계산된다(형식적으로는 실행 불가능 후보에 음의 무한대를 더하는 마스킹과 동치).

### 2.7 self-labeling 학습

절차는 Phase 1과 동형이되 하위문제 단위가 Bay다.

1. Phase 1 배정 결과로 각 Bay 하위문제를 만든다.
2. 각 Bay에서 후보 해 집합을 만든다: 정책의 탐욕적 해 + 정책의 확률적 표본 해 + 배치·설비 휴리스틱들의 해.
3. 각 후보를 사전식 목적함수로 채점하고, Bay별로 **가장 좋은 후보를 teacher**로 삼는다.
4. teacher의 작업 선택 시퀀스를 정답으로 교차 엔트로피 갱신한다. 하나의 에피소드에서 비어 있지 않은 Bay 수만큼 순차적으로 같은 정책을 갱신한다.

**학습 정답(라벨)의 형식**은 "완성된 스케줄" 자체가 아니라, 그 스케줄을 만든 **작업 선택 시퀀스**다. 각 단계의 정답은 "현재 실행가능 작업 후보 중 선택된 작업의 인덱스"라는 정수 하나이며, 한 Bay의 라벨은 이 인덱스들의 순서열이다. 설비 선택·목표 배치 크기·시각 점프는 환경의 결정론적 사건이므로 라벨이 아니다. 완성 스케줄(설비 배정·배치 구성·시각)은 이 작업 시퀀스를 환경 규칙으로 결정론적으로 재생해 얻는다. 따라서 작업 시퀀스만으로 해가 **무손실로 복원**되며, 통제 가능한 유일한 자유도가 작업 선택이라는 점에서 이 축소된 행동공간은 손실이 아니다.

**손실함수**는 teacher 작업 시퀀스에 대한 단계별 교차 엔트로피의 평균이며, Bay 하위문제마다 한 번 역전파·갱신한다.

**self-labeling 해(pseudo-label)의 예시 형태.** 어떤 Bay에 설비 세 대(A·B·C)와 남은 작업 일곱 개(W1~W7)가 있다고 하자. 이 Bay 하위문제의 전개는 "환경의 설비·목표 크기 결정"과 "정책의 작업 선택"이 번갈아 일어난다.

| 순서 | 주체 | 내용 |
|---|---|---|
| 1 | 환경 | 설비 A 선택, 목표 크기 3 → 배치 열림 |
| 2 | 정책 | W1 → W4 → W2 (세 개 차 자동 닫힘) |
| 3 | 환경 | 설비 B 선택, 목표 크기 2 → 배치 열림 |
| 4 | 정책 | W3 → W6 (자동 닫힘) |
| 5 | 환경 | 설비 C 선택, 목표 크기 2 → 배치 열림 |
| 6 | 정책 | W5 → W7 (자동 닫힘) |

이 전개가 만든 **완성 스케줄(해)**은 다음과 같다.

```
설비 A : 배치 {W1, W4, W2}
설비 B : 배치 {W3, W6}
설비 C : 배치 {W5, W7}
```

**학습에 쓰는 pseudo-label**은 정책이 만든 **작업 선택 토큰 시퀀스** `[W1, W4, W2, W3, W6, W5, W7]`뿐이다(각 토큰의 정답은 그 단계에서 실행 가능한 작업 목록에서의 인덱스). 설비 선택·목표 배치 크기·배치 닫힘·시각 점프는 환경의 결정론적 사건이므로 pseudo-label에 포함되지 않는다. 완성 스케줄은 이 토큰 시퀀스를 환경 규칙으로 재생해 무손실로 복원한다. 또한 배치 내부 순서는 결과에 무의미하므로(§2.3) `W1→W4→W2`와 `W2→W4→W1`은 같은 해다.

### 2.8 비교 알고리즘 (배치·작업 선택 휴리스틱, 6종)과 중복성 분석

여섯 휴리스틱은 모두 "현재 설비의 열린 배치에 넣을 작업을 **사전식 우선순위로 하나 고르는**" 규칙이며, 정렬 키만 다르다. 배치 처리시간은 구성 작업 처리시간의 **최댓값**이므로, 어느 규칙이든 그 배치의 처리시간을 정하는 **첫 작업의 선택**이 결과를 크게 좌우한다.

| 휴리스틱 | 우선 원리(요약) |
|---|---|
| **Workload Makespan** | 빈 배치는 최장 처리시간으로 열고, 이후에도 남은 최장 작업을 붙임(사실상 LPT) |
| **Min Makespan** | 선택 후 전체 makespan이 최소가 되는 작업 — 사실상 최단 처리시간으로 배치를 엶 |
| **Lookahead Min Makespan** | 잔여 물량까지 반영한 makespan 하한이 최소인 작업 |
| **Best Fit LTH** | 배치의 길이 용량(LTH)을 최대한 채우는(가장 긴 판) 작업 |
| **SPT** | 처리시간이 짧은 작업 우선 |
| **LPT** | 처리시간이 긴 작업 우선 |

> **Workload Makespan과 LPT의 차이.** 둘 다 최장 처리시간으로 배치를 여는 LPT 계열이라 대부분의 인스턴스에서 **동일한 스케줄**을 낸다. 차이는 **처리시간이 같은 작업이 여럿일 때의 tie-break**다. Workload Makespan은 (배치시간 증가분 0-동점 → makespan 하한 → 절단 길이·베벨이 큰 것)으로, LPT는 (예상 makespan → 작업 식별자)로 가른다. 이 동점이 배치 길이 상한(LTH) 경계와 겹치면 서로 다른 작업이 한 배치로 묶여 배치 구성·설비 배정·부하 편차가 갈릴 수 있다(makespan은 대개 동일). 실증상 약 99% 동일하나 **완전 동치는 아니다**(300 에피소드 중 4건에서 결과 상이).

#### 주요 휴리스틱 상세

**Workload Makespan.** 두 국면으로 작동한다. ① **빈 배치를 열 때**는 처리시간이 가장 긴 작업을 고른다(동점이면 절단 길이·베벨이 큰 것). 이 첫 작업이 그 배치의 처리시간을 결정한다. ② **배치에 더 넣을 때**의 1차 기준은 "배치 처리시간 증가분"인데, 첫 작업을 최장으로 열었으므로 남은 후보는 모두 그보다 짧아 **증가분이 항상 0으로 묶인다.** 따라서 실제 판별은 다음 기준들 — 잔여 물량을 반영한 makespan 하한, 예상 makespan, 예상 종료시각, 남은 슬롯 수, 처리시간(큰 것) — 로 넘어가고, 이들은 모두 **남은 것 중 가장 긴 작업**을 선호한다. 결국 첫 작업뿐 아니라 전 구간이 사실상 LPT다. 의도는 긴 작업들을 한 배치에 모아 배치 처리시간이 불필요하게 누적되지 않게 함으로써 설비들이 빨리 끝나도록(= makespan 단축) 하는 것이다.

**Min Makespan.** 1차 기준은 "이 작업을 넣었을 때의 예상 전체 makespan", 2차 기준은 "이 설비의 예상 종료시각"이다. 예상 종료시각 = 시작시각 + max(현재 배치 처리시간, 후보 처리시간)이므로, 빈 배치(현재 배치 처리시간 0)에서는 **처리시간이 가장 짧은 작업**을 고르는 것과 같다(짧을수록 종료가 빠르고 makespan이 낮음). 즉 최단 처리시간으로 배치를 여는 SPT 계열이다. 참고로 이 규칙은 앞서 제거한 규칙(설비 종료시각만 보는 부하 균형 규칙)과 "한 결정 단계에서 상수가 아닌 값이 처리시간뿐"이라는 대수적 이유로 **완전히 동일함이 증명**되었고, 실증적으로는 Lookahead Min Makespan·SPT와도 대개 같은 선택을 한다.

**Lookahead Min Makespan.** Min Makespan과 같은 makespan 지향이되, 1차 기준을 "즉각적 예상 makespan"이 아니라 **잔여 미배정 물량까지 반영한 makespan 하한**으로 바꾼다. 이 하한은 (모든 설비의 예상 종료시각 최댓값)과 (각 Bay의 설비 종료시각 합 + 그 Bay 남은 처리시간 합 ÷ 최대 배치크기, 를 설비 수로 나눈 값)의 최댓값으로, "지금 선택이 앞으로 남길 잔여 부하"까지 내다본다. 즉각 makespan이 같아도 남은 물량을 더 잘 줄이는 선택을 선호한다. 다만 즉각 makespan 항이 대체로 지배적이라 실증에서는 Min Makespan과 거의 같은 결과를 낸다.

**Best Fit LTH.** 배치를 최대한 꽉 채우는 bin-packing식 규칙이다. 1차 기준은 배치 작업 수(3개까지 채움)이지만, 한 결정 단계의 후보들은 모두 "현재 배치에 1개 추가"라 작업 수가 동일하므로 실질 판별은 2차 기준인 **배치 누적 판 길이(LTH)** 로 넘어간다. 즉 넣었을 때 길이 합이 가장 커지는 작업 — 곧 **판 길이가 가장 긴 작업** — 을 골라 길이 상한을 빠르게 채운다(동점이면 예상 makespan). 처리시간이 아니라 길이를 기준으로 묶으므로 makespan은 LPT 계열보다 열위이지만, 배치를 균일하게 꽉 채워 **설비 간 부하 편차(gap)가 가장 작은** 경향이 있다.

**실효 다양성.** 배치 내부 순서가 무의미하고 tie-break만 다른 규칙들은 대부분의 인스턴스에서 겹친다. 실증적으로 여섯 규칙의 결과는 대체로 세 부류 — LPT 계열(Workload Makespan·LPT), 길이 채움(Best Fit LTH), SPT 계열(Min Makespan·Lookahead Min Makespan·SPT) — 로 수렴하며, 첫 작업을 최장으로 잡는 LPT 계열과 최단으로 잡는 SPT 계열 사이에는 실측상 makespan이 약 12% 차이 난다. 다만 **부류 내부의 동치는 대개 실증적이며 완전하지 않다**(예: Workload Makespan과 LPT는 ~99% 일치하나 tie-break가 달라 완전 동치는 아님). 유일하게 **대수적으로 완전 동치가 증명된 쌍**(Min Makespan과 종료시각 기반 부하 균형 규칙)만 후보군에서 제거했다(현 후보군 6종). teacher는 후보군 전체의 최적 해이므로 기능적으로 중복인 규칙은 같은 pseudo-label을 낼 뿐 teacher 다양성에 기여하지 못하며, 따라서 후보군은 서로 실질적으로 다른 규칙으로 구성해야 한다.

---

## 3. 방법론 공통 특성과 논의

### 3.1 self-labeling의 위상 — 최적 후보 모방

두 Phase 모두 강화학습의 보상 역전파가 아니라 **"후보군 중 목적함수 최적 해를 teacher로 삼는 imitation learning"**이다. 후보군은 항상 **정책 자신의 해(탐욕·표본)와 휴리스틱 해**를 함께 포함하며, 그중 최적이 teacher가 된다. 따라서:

- 휴리스틱 해는 pseudo-label 후보이며, **이기면 그대로 학습 신호가 된다.** 순수 자기표본 학습이 아니다.
- 미학습 초기에는 정책이 약하므로 휴리스틱이 teacher의 다수를 차지하여 **성능 하한선(teacher floor)** 역할을 한다. 학습이 진전되면 정책 자신의 표본이 teacher로 이기는 비율이 늘어난다.
- 어떤 인스턴스에서도 최적이 되지 못하는(완전히 지배당하는) 휴리스틱은 teacher로 뽑히지 않으므로 학습 결과에 영향을 주지 못한다. 다만 "평균적으로 약함"과 "한 번도 최적이 아님"은 다르다 — 인스턴스마다 최적 규칙이 갈리므로, 평균이 낮아도 특정 인스턴스에서 최적이면 teacher가 되어 학습에 기여한다.

### 3.2 결정론적 환경과 행동공간 축소

Phase 2에서 설비 선택을 정책 행동에서 제외한 것은 정보 손실이 아니다. 설비 배정은 상태의 결정론적 함수이자 정책의 입력이며, 작업 선택 시퀀스와 결정론적 환경만으로 완성 스케줄이 무손실 재생된다. 다만 이는 **표현의 무손실성**이지 **학습 동역학의 무결성**을 뜻하지는 않는다. 배포 시 정책이 teacher와 다른 선택을 하면 이후 상태(어느 설비가 다음에 유휴가 되는지 등)가 teacher가 방문하지 않은 영역으로 이탈할 수 있다(imitation learning 특유의 분포 이동). 본 방법은 후보군에 정책 자신의 전개 해를 포함시켜 이 이탈을 부분적으로 교정하지만, 명시적 재라벨링은 두지 않으므로 잔여 위험은 남는다.

### 3.3 두 Phase의 연결

Phase 1이 두 자원군의 배정을 병합해 "블록-계열 → Bay"를 확정하면 해당 블록-계열의 모든 작업이 같은 Bay를 상속한다. Phase 2는 이 작업들을 Bay 내부의 설비와 배치에 할당한다. 선택적으로, 학습이 끝난 Phase 2 정책을 고정한 채 각 Phase 1 후보를 실제로 스케줄링해 얻은 성능을 Phase 1 teacher 선정 점수 앞에 반영하는 연결(하류 피드백)도 가능하다.

---

## 4. 요약

| 구분 | Phase 1 | Phase 2 |
|---|---|---|
| 결정 단위 | 블록-계열 | 작업(배치 구성) |
| 하위문제 분할 | 두 자원군(설비 공유 단위) | Bay별 |
| 행동 | (블록-계열, Bay) **쌍** | **작업 하나** (설비는 환경 결정) |
| 목적(사전식) | 작업 수 → 절단 길이 → 베벨(설비 대수 정규화 편차) | 제약 위반 → makespan → 설비별 4종 편차 |
| 정책망 | MLP 포인터(후보 쌍 점수화) | MLP 집합 포인터(설비·작업 집합 인코딩) |
| 학습 | self-labeling(최적 후보 teacher) + 교차 엔트로피 | 동일 |
| 비교 휴리스틱 | 3종(정렬 기준별 균형 배정) | 6종(Workload Makespan·Min Makespan·Lookahead Min Makespan·Best Fit LTH·SPT·LPT; 결과는 대체로 3부류로 수렴하나 부류 내 완전 동치는 아님) |

두 단계 모두 **후보군의 사전식 최적 해를 teacher로 삼는 self-labeling imitation learning**으로 하나의 공유 정책을 갱신하며, 휴리스틱은 학습의 성능 하한이자 초기 teacher로 기능한다. Phase 1은 쌍 선택으로 배정 순서와 위치를 동시에 정하고, Phase 2는 설비 배정을 결정론적 환경에 위임한 채 작업 선택에만 집중하는 것이 두 단계의 구조적 차이다.
